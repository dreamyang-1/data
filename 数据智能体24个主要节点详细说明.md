# 数据智能体24个主要节点详细说明

> 适用场景：企业经营数据查询、趋势分析、对比分析、异常分析、归因分析、预测分析和报表生成。  
> 说明：这里的“24个节点”是数据智能体工作流中的24个处理步骤，并不代表需要部署24个大模型或24个独立智能体。

## 1. 总体设计思路

### 1.1 语义模型与业务域路由（2026-08-26更新）

- `semantic_model_id`必填，用于确定整套语义模型。
- `business_domain_id`保留为可选旧字段，兼容现有Java调用。
- `business_domain_ids`是新的可选数组；旧单值会自动规范化为单元素数组。
- 两个业务域字段都不传时，智能体向Oagnet传递`business_domain_id=null`，由Oagnet在指定语义模型的全部业务域中自动检索并生成ASL，因此能够处理跨业务域问题。
- 同时传入旧、新字段但内容不一致时直接返回422，不静默猜测。
- 自动选择业务域不等于可以随意Join。涉及多个业务域时，语义层必须存在经过配置的实体关系、关联字段和基数，否则应澄清或安全终止。

推荐普通聊天请求只传`semantic_model_id`；只有调试或确定要缩小范围时才传业务域。响应通过`business_domain_selection_mode`回显`AUTO/EXPLICIT`，并通过`requested_business_domain_ids`回显调用方主动指定的范围。

完整链路可以概括为：

```text
接收问题
→ 恢复会话
→ 识别意图并抽取条件
→ 确认指标、维度和实体的权威含义
→ 判断信息是否完整
→ 拆分和规划任务
→ 生成受控查询
→ 查询数据库
→ 检查数据质量
→ 使用确定性算法分析
→ 校验计算结果
→ 生成图表和文字总结
→ 判断结论是否可靠
```

核心原则：

```text
大模型负责理解、规划和表达；
确定性程序负责查询、计算、校验和关键决策；
语义层负责确认指标、实体、维度和业务含义；
信息不足先追问，数据或证据不足不生成确定性结论。
```

大模型重点参与05、10、11、23节点；语义检索服务重点参与06、07、19节点；其余节点应尽量使用确定性程序实现。真正查询数据库的是14、15节点，真正计算结果的是20节点。

## 2. 统一状态对象

节点之间必须传递结构化状态，不能依靠自然语言随意传值。可以使用Pydantic定义统一的`AgentState`：

```python
from typing import Optional
from pydantic import BaseModel, Field


class AgentState(BaseModel):
    request_id: str
    conversation_id: str
    message_id: str
    tenant_id: str
    user_id: str
    application_id: str

    original_question: str
    rewritten_question: Optional[str] = None

    conversation_context: dict = Field(default_factory=dict)
    conversation_control: Optional[dict] = None

    intent_candidate: Optional[dict] = None
    semantic_candidates: list[dict] = Field(default_factory=list)
    canonical_request: Optional[dict] = None

    completeness_report: Optional[dict] = None
    pending_task: Optional[dict] = None

    atomic_tasks: list[dict] = Field(default_factory=list)
    task_plan: Optional[dict] = None
    approved_plan: Optional[dict] = None
    execution_state: dict = Field(default_factory=dict)

    validated_asl: Optional[dict] = None
    raw_query_result: Optional[dict] = None
    dataset: Optional[dict] = None
    dataset_reference: Optional[dict] = None

    applicability_report: Optional[dict] = None
    knowledge_context: Optional[dict] = None
    analysis_facts: list[dict] = Field(default_factory=list)
    validation_report: Optional[dict] = None

    chart_specs: list[dict] = Field(default_factory=list)
    insights: list[dict] = Field(default_factory=list)
    narrative: Optional[str] = None
    reliability_report: Optional[dict] = None

    status: str = "RUNNING"
    errors: list[dict] = Field(default_factory=list)
```

每个节点只做一件事：读取所需字段、执行固定职责、写回结构化结果、决定下一个节点。

## 3. 01～04：请求与会话处理层

### 01 API接入与可信身份

**作用**：接收平台请求，并确定租户、用户、应用、角色和可访问数据范围，为后续所有权限判断提供可信上下文。

**输入**：`conversation_id`、`message_id`、`question`，以及网关注入的`tenant_id`、`user_id`、`application_id`、`roles`和`trace_id`。

**输出**：`TrustedContext`，包括用户身份、业务域、可访问数据源和角色权限。

**实现方式**：

1. Java网关或统一认证网关验证登录Token。
2. 网关从服务端账户信息中取得租户和角色。
3. FastAPI只读取可信Header，不接受用户自己提交租户和权限。
4. 将身份上下文写入`AgentState`。

```python
async def api_access_node(state: AgentState, headers: dict):
    state.tenant_id = headers["X-Trusted-Tenant"]
    state.user_id = headers["X-Trusted-User"]
    state.application_id = headers["X-Application-Id"]
    return state
```

**风险点**：不能相信请求体中由用户自行填写的`tenant_id`和`roles`，否则可能产生跨租户访问。

### 02 请求校验与幂等

**作用**：检查请求是否合法，并防止重复点击、网络重试造成同一任务多次执行。

**主要检查**：问题是否为空、长度是否超限、Schema是否合法、消息ID是否合法、文件大小和类型是否允许，以及相同消息ID是否对应同一请求。

**幂等键**：

```text
tenant_id + user_id + application_id + conversation_id + message_id
```

**实现方式**：对请求体做稳定序列化后计算SHA-256指纹，并存入Redis。相同消息ID和相同指纹直接返回原结果；相同消息ID但指纹不同则返回冲突错误。

```python
import hashlib
import json


def request_fingerprint(payload: dict) -> str:
    raw = json.dumps(payload, sort_keys=True, ensure_ascii=False)
    return hashlib.sha256(raw.encode("utf-8")).hexdigest()
```

**输出**：`ValidatedRequest`，包含规范化请求、请求指纹和幂等状态。

### 03 会话与记忆恢复

**作用**：恢复上一轮的规范化请求、待补任务、最近数据集和用户偏好，让系统能够理解“那上个月呢”“按门店看”等上下文表达。

**输入**：可信身份、`conversation_id`和当前问题。

**输出**：`ConversationContext`。

**实现方式**：使用Redis保存短期状态：

```text
conversation:{tenant}:{user}:{application}:{conversation_id}
```

可恢复内容包括上一规范化请求、待补槽位、最近`dataset_id`和任务状态。不能从历史继承当前身份、权限、已经解绑的知识库、敏感明细和过期数据集。

### 04 会话控制识别

**作用**：判断当前消息是回答追问、继续分析、修改条件、取消任务还是开始新任务。

| 用户表达 | 会话控制类型 |
|---|---|
| “按月” | 回答追问 |
| “改成订单量” | 修改条件 |
| “继续分析原因” | 继续任务 |
| “不用了” | 取消任务 |
| “换个问题，查询客户数” | 新任务 |

**实现方式**：优先用显式控制词规则识别，规则不确定时再调用轻量分类模型。输出`control_type`以及需要覆盖的槽位。

```python
CONTROL_PATTERNS = {
    "CANCEL": ["不用了", "取消", "先不查了"],
    "MODIFY": ["改成", "换成", "不是", "按"],
    "NEW_TASK": ["换个问题", "另外查询", "重新分析"],
    "CONTINUE": ["继续", "接着分析", "再看看"]
}
```

## 4. 05～09：意图、语义和追问层

### 05 意图识别与槽位抽取

**作用**：识别用户要执行的业务任务，并抽取指标、时间、维度、实体、过滤条件、返回字段、预测步长等参数。

典型意图包括指标查询、明细查询、趋势、对比、占比、异常、归因、预测、报表、指标口径、数据血缘和数据质量。

**示例输入**：

```text
对比今年7月和6月华东地区销售额
```

**示例输出**：

```json
{
  "intent": "COMPARISON_ANALYSIS",
  "confidence": 0.97,
  "slots": {
    "metric_text": "销售额",
    "time_ranges": ["今年7月", "今年6月"],
    "entity_text": "华东地区",
    "comparison_type": "环比"
  }
}
```

**实现方式**：使用“强规则信号＋微调分类模型＋结构化槽位抽取”。12类意图可用MacBERT等模型微调；槽位抽取可用NER模型或支持JSON Schema的大模型。输出必须经过Pydantic校验。

本节点只得到用户原始表达，不能直接用“销售额”生成SQL。

### 06 语义候选召回

**作用**：把用户表达映射为可能的企业业务对象。比如“销售额”可能对应含税销售额、不含税销售额、实收销售额等多个内部指标。

**实现方式**：使用关键词/BM25、向量召回、同义词和业务域过滤进行混合检索。在召回前先按租户、应用绑定语义模型、业务域、已发布状态和权限过滤。

**输出**：`SemanticCandidates`，包含指标、维度、实体和样例候选及其分数。

```json
{
  "metric_candidates": [
    {"metric_id": "metric_001", "name": "含税销售额", "score": 0.93},
    {"metric_id": "metric_002", "name": "不含税销售额", "score": 0.86}
  ]
}
```

向量检索只负责找候选，不能替代权威语义确认。

### 07 权威语义确认与参数规范化

**作用**：将候选对象转换成唯一、可执行的标准业务对象，确认指标ID、版本、单位、维度、实体标准值、时间范围、粒度及合法组合。

**示例输出**：

```json
{
  "intent": "METRIC_QUERY",
  "metric": {
    "metric_id": "metric_001",
    "version": "v3",
    "name": "含税销售额",
    "unit": "CNY"
  },
  "time_range": {"start": "2026-07-01", "end": "2026-07-31"},
  "filters": [
    {
      "dimension_id": "sales_region",
      "operator": "=",
      "value_code": "EAST_CHINA"
    }
  ],
  "granularity": "month"
}
```

**实现方式**：调用`resolve_metric`、`normalize_entity_value`、`list_allowed_dimensions`等语义服务接口。无法唯一确认时不允许猜测，转入08、09节点追问。

### 08 信息完整性判断

**作用**：根据不同意图检查必填槽位是否完整、是否存在冲突和非法组合。

```python
REQUIRED_SLOTS = {
    "METRIC_QUERY": ["metric", "time_range"],
    "DETAIL_QUERY": ["entity", "time_range", "return_fields"],
    "TREND_ANALYSIS": ["metric", "time_range", "granularity"],
    "COMPARISON_ANALYSIS": ["metric", "comparison_ranges", "comparison_type"],
    "COMPOSITION_ANALYSIS": ["metric", "time_range", "group_dimension"],
    "ANOMALY_ANALYSIS": ["metric", "history_range", "granularity"],
    "ROOT_CAUSE_ANALYSIS": ["metric", "target_period", "baseline_period"],
    "FORECAST_ANALYSIS": [
        "metric", "history_range", "granularity", "forecast_horizon"
    ]
}
```

**输出**：`CompletenessReport`，包含`is_complete`、`missing_slots`、`conflicting_slots`、语义歧义和不支持条件。完整则进入10；不完整则进入09。

### 09 追问并保存待办

**作用**：当信息不完整时，以最少轮次向用户询问关键缺失信息，同时保存尚未完成的任务。

**追问结构**：已理解信息、缺失槽位、冲突条件、候选选项、具体问题、可复制回复示例和当前轮次。

```json
{
  "status": "NEEDS_CLARIFICATION",
  "understood_slots": {
    "metric": "销售额",
    "forecast_horizon": "下个月"
  },
  "missing_slots": ["history_range", "granularity"],
  "clarification_questions": [
    "使用多长时间的历史数据？",
    "按日、周还是月预测？"
  ],
  "reply_example": "使用过去24个月，按月预测"
}
```

**实现方式**：将`PendingTask`保存到Redis，并带上`state_version`进行CAS原子更新。下一轮只抽取当前待补槽位，这种方式称为`expected-slot-aware extraction`，可以避免把“过去两年，按月”误识别成新任务。

## 5. 10～13：任务规划与调度层

### 10 多问题拆分

**作用**：把复合问题拆成可独立执行的原子任务。

例如“查询本月销售额，和上月对比，分析下降原因并生成图表”可拆为：本月查询、上月查询、对比分析、归因分析和图表生成。

**实现方式**：简单问题直接生成单个原子任务；复杂问题让大模型根据可用任务类型和Schema输出JSON，再用Pydantic校验。必须限制最大任务数并禁止模型创造不存在的Skill。

**输出**：`AtomicTasks`。

### 11 任务DAG规划

**作用**：确定子任务的输入、输出、依赖和并行关系。

```text
查询本月 ─┐
          ├→ 对比 → 归因 → 图表
查询上月 ─┘
```

本月和上月查询可以并行；对比必须等待两个查询完成；归因必须等待变化事实确认。

**输出**：`TaskPlan`，每个任务包含`task_id`、`skill`、`depends_on`、输入和输出引用。

**实现方式**：可让大模型提出计划，但必须使用程序检查；用LangGraph执行状态流，用NetworkX检查DAG是否成环，用`asyncio`并行执行无依赖任务。

### 12 计划校验与风险门禁

**作用**：检查计划是否可执行、安全且成本可控。

**检查内容**：循环依赖、非白名单Skill或Tool、权限越界、重复任务、查询次数、扫描量、返回行数、预计耗时、缺少前置数据和写库操作。

```python
MAX_TASKS = 10
MAX_QUERY_TASKS = 5
MAX_TOTAL_ROWS = 100_000
MAX_EXECUTION_SECONDS = 60
```

**输出**：`ApprovedPlan`。未通过时进入安全降级，不能继续执行。

### 13 Skill选择与Tool调度

**作用**：根据批准计划调用业务Skill，再由Skill调用允许的原子Tool。

```text
Framework → Skill → Tool
```

例如趋势分析Skill可以调用查询、数据质量、趋势算法和图表Tool。调度器统一控制白名单、并发、超时、有限重试、取消、熔断、进度和错误分类。

```python
SKILL_REGISTRY = {
    "METRIC_QUERY": metric_query_skill,
    "TREND_ANALYSIS": trend_analysis_skill,
    "COMPARISON_ANALYSIS": comparison_analysis_skill,
    "ROOT_CAUSE_ANALYSIS": root_cause_analysis_skill,
    "FORECAST_ANALYSIS": forecast_analysis_skill
}
```

网络超时可以有限重试；权限不足不能重试；参数缺失回到追问；数据不足进入安全降级。

## 6. 14～17：查询与数据集层

### 14 ASL生成与校验

**作用**：将规范化业务请求转换成安全的结构化查询语言ASL，再由服务端转换为SQL。ASL是业务语义和物理数据库之间的中间层。

```json
{
  "semantic_model_id": "sales_model",
  "metric_ids": ["metric_001"],
  "dimensions": ["sales_region"],
  "time_range": {"start": "2026-07-01", "end": "2026-07-31"},
  "filters": [
    {"field": "sales_region", "operator": "=", "value": "EAST_CHINA"}
  ],
  "granularity": "month",
  "limit": 1000
}
```

**校验内容**：指标是否存在、指标维度组合是否合法、过滤操作符是否合法、时间范围和limit是否超限、用户是否有权限、数据源是否由服务端绑定。

不让大模型直接写SQL，可以减少字段幻觉、错误Join、越权字段、SQL注入和全表扫描风险。

### 15 只读SQL翻译、校验与执行

**作用**：将`ValidatedASL`先通过独立翻译接口转换为SQL，在智能体侧完成只读安全检查后，再调用独立执行接口访问受控数据源。翻译失败和执行失败必须分开记录。

```text
ValidatedASL
→ 服务端元数据映射
→ `/api/translate`生成SQL（不访问数据库）
→ 单语句、只读操作和危险函数检查
→ `/api/execute`使用只读账户执行
```

**安全要求**：只读账号、只允许SELECT、禁止多语句和DDL/DML、参数绑定、AST检查、执行超时、扫描量限制、返回行数限制、服务端数据源绑定。

用户文本不能直接拼接到SQL中，所有条件必须参数化。

### 16 查询结果标准化

**作用**：将不同数据库的返回结果转换成统一的`Dataset`。

需要统一字段名、类型、日期、时区、单位、币种、空值、重复、截断状态、快照、行列数量和脱敏规则。

```json
{
  "columns": [
    {"name": "month", "type": "date"},
    {"name": "sales_amount", "type": "decimal", "unit": "CNY"}
  ],
  "rows": [
    ["2026-06", 1000000],
    ["2026-07", 900000]
  ],
  "row_count": 2,
  "truncated": false,
  "snapshot_id": "snapshot-001"
}
```

如果结果被截断，就不能使用截断数据计算整体平均值、占比或归因。

### 17 数据集登记与历史复用

**作用**：为查询或文件解析结果生成`dataset_id`，供后续追问、二次计算、筛选、排序和绘图复用。

```json
{
  "dataset_id": "dataset-001",
  "query_fingerprint": "sha256...",
  "snapshot_id": "snapshot-001",
  "columns": ["month", "sales_amount"],
  "row_count": 12,
  "storage_ref": "controlled://dataset/001",
  "expires_at": "2026-08-24T12:00:00+09:00"
}
```

二次筛选、排序、聚合、平均值、图表和已有字段的趋势分析可以复用；需要新字段、改变原始过滤、数据过期、权限变化、要求最新快照或原结果被截断时必须重新查询。

## 7. 18～21：分析与校验层

### 18 数据质量与算法适用性

**作用**：在运行算法前判断数据是否足以支持目标分析，避免“只要有数据就硬算”。

**通用检查**：字段、类型、截断、空值率、重复率、时间连续性、新鲜度、单位、币种、时区和样本量。

不同算法有不同要求：趋势需要足够时间点和稳定频率；异常需要历史基线；预测需要足够历史周期、可回测性和合理预测步长。

```json
{
  "applicable": false,
  "quality_score": 0.58,
  "problems": [
    {
      "code": "INSUFFICIENT_HISTORY",
      "message": "仅有3个月历史数据，无法可靠预测未来12个月"
    }
  ],
  "required_supplements": ["至少补充24个月的月度数据"]
}
```

需要区分信息缺失和数据不足：用户没说预测多久属于信息缺失，回09追问；数据库只有3个月数据属于数据不足，应安全降级。

### 19 指标口径和知识检索

**作用**：获取指标权威定义，以及与本次分析相关的业务背景和事件。

权威语义库提供公式、单位、版本、默认过滤和生效时间；知识库提供促销、节假日、门店调整、制度和业务事件。

```json
{
  "metric_definition": {
    "metric_id": "metric_001",
    "formula": "订单含税金额合计-退款金额",
    "unit": "CNY",
    "version": "v3"
  },
  "business_events": [
    {
      "event": "华东地区7月部分门店停业调整",
      "source": "经营通知2026-07",
      "evidence_level": "DOCUMENTED"
    }
  ]
}
```

检索到的业务事件只是候选证据，不能自动判定为数据变化原因。

### 20 确定性算法分析

**作用**：使用Python、SQL或专用算法服务完成真实数值计算。大模型不能在此节点口算。

| 分析类型 | 典型方法 |
|---|---|
| 指标 | sum、count、average、ratio |
| 同比环比 | 绝对差、增长率 |
| 趋势 | 稳健斜率、移动平均、波动率 |
| 排名 | 分组聚合、稳定排序 |
| 占比 | 分组值÷总值、累计占比 |
| 异常 | IQR、Z-score、MAD、Isolation Forest |
| 变点 | PELT、CUSUM |
| 归因 | 维度贡献、驱动贡献、漏斗分解、Price-Volume-Mix |
| 预测 | Naive、ETS、ARIMA、Prophet或树模型 |
| 数据质量 | 空值率、重复率、有效率、连续性 |

```python
def period_comparison(current: float, baseline: float) -> dict:
    absolute_change = current - baseline
    if baseline == 0:
        relative_change = None
        warning = "基期为0，无法计算相对变化率"
    else:
        relative_change = absolute_change / baseline
        warning = None
    return {
        "current": current,
        "baseline": baseline,
        "absolute_change": absolute_change,
        "relative_change": relative_change,
        "warning": warning
    }
```

输出必须是带数据集、算法版本和证据引用的`AnalysisFacts`，不能直接输出自由文本文章。预测应先回测多个候选模型，根据MAE、MAPE或RMSE选模，再输出预测值和区间。

### 21 分析结果交叉校验

**作用**：检查20节点的计算结果是否在数学和业务上成立。

**主要检查**：

1. 各分组之和是否等于总体值。
2. 单位和币种是否一致。
3. 同比、环比的基期是否正确。
4. 比较数据是否来自兼容快照。
5. 关键结果重新计算是否一致。
6. 图表数值是否与事实对象一致。

```json
{
  "passed": true,
  "checks": [
    {"name": "unit_consistency", "passed": true},
    {"name": "comparison_period", "passed": true},
    {"name": "calculation_recompute", "passed": true}
  ],
  "warnings": []
}
```

核心校验失败时不能进入大模型总结。

## 8. 22～24：图表、总结与可靠度层

### 22 图表和结构化洞察

**作用**：把已验证的数据和事实转换成前端可渲染的`ChartSpec`及结构化洞察。

| 分析类型 | 推荐图表 |
|---|---|
| 趋势 | 折线图 |
| 对比 | 柱状图 |
| 占比 | 条形图；类别少时可用饼图 |
| 归因 | 瀑布图或贡献度条形图 |
| 异常 | 带异常标记的折线图 |
| 预测 | 历史线、预测线和置信区间 |
| 排名 | 横向条形图 |

图表应由确定性规则选择，数据必须引用已登记数据集。洞察只能组织已有事实，不能增加未经计算的数字或原因。

### 23 大模型事实总结

**作用**：把经过验证的结构化事实转换成用户易读的自然语言。大模型负责表达，不负责发现或计算数字。

**允许输入**：`CanonicalRequest`、`AnalysisFacts`、`ValidationReport`、`KnowledgeContext`、图表摘要、数据限制和可靠度信息。

**提示词约束**：

```text
1. 只能使用提供的verified_facts。
2. 不得新增、修改或推测数字。
3. 原因必须标记证据等级。
4. 相关关系不得写成因果关系。
5. 数据不足时必须明确说明。
6. 不得隐藏指标口径和时间范围。
```

总结后使用`llm_output_guard`提取回答中的数字和结论，与`AnalysisFacts`逐项比对。第一次不通过可重写一次；仍不通过则使用确定性模板输出。

### 24 证据与可靠度门禁

**作用**：综合判断答案是否可以发布、哪些结论可以展示、哪些结论必须限制。

可靠度可综合意图置信度、语义确认度、数据质量、新鲜度、算法适用性、交叉校验、知识证据和模型事实一致性。

```python
WEIGHTS = {
    "intent": 0.10,
    "semantic": 0.15,
    "data_quality": 0.20,
    "freshness": 0.10,
    "applicability": 0.15,
    "validation": 0.20,
    "evidence": 0.10
}


def calculate_reliability(scores: dict) -> float:
    return sum(scores[name] * weight for name, weight in WEIGHTS.items())
```

可靠度不能只算加权平均，还要设置硬门禁：核心计算校验失败直接阻断；数据质量过低只能输出受限结果；语义不明确回到追问。

```json
{
  "level": "MEDIUM",
  "score": 0.78,
  "passed": true,
  "limitations": [
    "7月数据距离月结还有3天",
    "业务事件只能作为候选解释"
  ],
  "allowed_claims": [
    "销售额环比下降10%",
    "下降主要集中在A类门店"
  ],
  "blocked_claims": [
    "门店停业导致销售额下降"
  ]
}
```

可靠度的主要价值不是显示一个分数，而是控制系统能够输出什么等级的结论。

## 9. LangGraph工作流连接示例

```python
from langgraph.graph import StateGraph, END

graph = StateGraph(AgentState)

nodes = {
    "api_access": api_access_node,
    "request_validation": request_validation_node,
    "memory_restore": memory_restore_node,
    "conversation_control": conversation_control_node,
    "intent_extraction": intent_extraction_node,
    "semantic_retrieval": semantic_retrieval_node,
    "semantic_resolution": semantic_resolution_node,
    "completeness_check": completeness_check_node,
    "clarification": clarification_node,
    "question_split": question_split_node,
    "dag_planning": dag_planning_node,
    "plan_validation": plan_validation_node,
    "skill_dispatch": skill_dispatch_node,
    "asl_generation": asl_generation_node,
    "sql_execution": sql_execution_node,
    "result_normalization": result_normalization_node,
    "dataset_registration": dataset_registration_node,
    "applicability_check": applicability_check_node,
    "knowledge_retrieval": knowledge_retrieval_node,
    "deterministic_analysis": deterministic_analysis_node,
    "cross_validation": cross_validation_node,
    "chart_insight": chart_insight_node,
    "fact_summary": fact_summary_node,
    "reliability_gate": reliability_gate_node
}

for name, func in nodes.items():
    graph.add_node(name, func)

graph.add_edge("api_access", "request_validation")
graph.add_edge("request_validation", "memory_restore")
graph.add_edge("memory_restore", "conversation_control")
graph.add_edge("conversation_control", "intent_extraction")
graph.add_edge("intent_extraction", "semantic_retrieval")
graph.add_edge("semantic_retrieval", "semantic_resolution")
graph.add_edge("semantic_resolution", "completeness_check")


def route_after_completeness(state: AgentState):
    if state.completeness_report["is_complete"]:
        return "question_split"
    return "clarification"


graph.add_conditional_edges(
    "completeness_check",
    route_after_completeness,
    {
        "question_split": "question_split",
        "clarification": "clarification"
    }
)

graph.add_edge("clarification", END)
graph.add_edge("question_split", "dag_planning")
graph.add_edge("dag_planning", "plan_validation")
graph.add_edge("plan_validation", "skill_dispatch")
graph.add_edge("skill_dispatch", "asl_generation")
graph.add_edge("asl_generation", "sql_execution")
graph.add_edge("sql_execution", "result_normalization")
graph.add_edge("result_normalization", "dataset_registration")
graph.add_edge("dataset_registration", "applicability_check")
graph.add_edge("applicability_check", "knowledge_retrieval")
graph.add_edge("knowledge_retrieval", "deterministic_analysis")
graph.add_edge("deterministic_analysis", "cross_validation")
graph.add_edge("cross_validation", "chart_insight")
graph.add_edge("chart_insight", "fact_summary")
graph.add_edge("fact_summary", "reliability_gate")
graph.add_edge("reliability_gate", END)

workflow = graph.compile()
```

正式项目还需要增加异常条件路由：请求非法时拒绝；信息不完整时追问；计划非法、数据不适用或校验失败时安全降级；查询超时可以有限重试；可靠度不足时限制结论。

## 10. 推荐开发顺序

### 第一阶段：基础问数

优先实现：

```text
01、02、03、05、06、07、08、09、13、14、15、16、23、24
```

先支持指标查询、明细查询、排行查询和指标口径查询。

### 第二阶段：基础分析

新增：

```text
17、18、19、20、21、22
```

支持趋势、同比环比、占比、排名和简单异常检测。

### 第三阶段：复杂任务规划

新增：

```text
04、10、11、12
```

支持多问题拆分、多任务依赖、并行查询、跨轮修改、取消和历史数据集复用。

### 第四阶段：高级分析

实现归因、变点检测、预测、跨数据集分析、Excel多Sheet分析和报告生成。归因和预测对数据质量要求最高，不适合作为第一版功能。

## 11. 总结

24个节点中最需要把握的职责边界是：

```text
05负责理解用户意图；
07负责确认业务对象的权威含义；
08和09负责判断信息是否足够并进行追问；
10～13负责任务拆分、计划与调度；
14和15负责安全查询数据；
18负责判断数据能不能支持算法；
20负责真实计算；
21负责验证计算是否正确；
22负责图表和结构化洞察；
23只负责表达已验证事实；
24决定最终能够输出哪些结论。
```

只有把理解、查询、计算、校验和表达分开，数据智能体才能真正做到安全、准确、可追溯和可维护。

---

# 附录：Excel生成后的追问与临时数据集处理方案

> 适用场景：数据智能体生成Excel后，用户继续基于该表进行筛选、加减乘除、增加计算列、分组汇总、排序、合并、对比、绘图和二次分析。

## 1. 核心结论

系统不能在生成Excel并返回用户后就结束，还要同时登记一份可继续分析的结构化数据集。后续用户所说的“这个表”，系统实际操作的是Excel背后的`dataset_id`。

推荐链路：

```text
数据库查询结果
→ 标准化DataFrame
→ 保存为Parquet数据集
→ 登记dataset_id
→ 根据数据集生成Excel
→ 将Excel返回用户
→ 在会话中保存dataset_id引用
→ 后续问题加载dataset_id继续计算
```

推荐原则：

```text
Excel：面向用户查看和下载
Parquet：面向系统读取和计算
dataset_id：连接会话、数据、文件和操作历史
```

## 2. 临时表格存在哪里

建议将会话状态、数据集元数据和真实数据分开保存。

| 内容 | 推荐存储 | 主要内容 |
|---|---|---|
| 会话状态 | Redis | 最近dataset_id、待办、上下文和幂等结果 |
| 数据集元数据 | PostgreSQL | 字段、类型、权限、来源、血缘、快照和过期时间 |
| 实际数据 | MinIO/S3等对象存储 | Parquet、原始Excel和生成Excel |
| 小数据缓存 | Redis，可选 | 几百行以内的短期结果 |
| 权威业务数据 | 数据库或数仓 | 原始业务数据 |

不建议把完整表格长期放在Redis里。Redis适合保存状态和引用，不适合存放几万行数据。

一个数据集的登记信息可以设计为：

```json
{
  "dataset_id": "ds_20260824_001",
  "tenant_id": "tenant_001",
  "user_id": "user_001",
  "conversation_id": "conv_001",
  "source_type": "DATABASE_QUERY",
  "storage_format": "PARQUET",
  "storage_ref": "datasets/tenant_001/ds_20260824_001/data.parquet",
  "excel_ref": "datasets/tenant_001/ds_20260824_001/result.xlsx",
  "columns": [
    {"name": "月份", "type": "date"},
    {"name": "区域", "type": "string"},
    {"name": "销售额", "type": "decimal", "unit": "CNY"}
  ],
  "row_count": 120,
  "snapshot_id": "snapshot_001",
  "parent_dataset_ids": [],
  "created_at": "2026-08-24T10:00:00+09:00",
  "expires_at": "2026-08-25T10:00:00+09:00",
  "status": "ACTIVE"
}
```

字段含义：

- `storage_ref`：系统内部计算用数据。
- `excel_ref`：用户下载的Excel文件。
- `snapshot_id`：查询数据的版本或快照。
- `parent_dataset_ids`：当前数据集由哪些数据集派生。
- `expires_at`：临时数据过期时间。

## 3. 为什么内部使用Parquet

Excel适合展示，但不适合作为内部主要计算格式。Parquet具有以下优势：

- 读取速度更快。
- 保留清晰的数据类型。
- 支持只读取需要的列。
- 文件体积通常更小。
- 可以直接被Pandas、Polars、DuckDB和Spark读取。
- 不易受到合并单元格、样式和公式影响。

因此，同一份结果可以同时保存两个文件：

```text
data.parquet    # 系统后续计算使用
result.xlsx     # 用户查看和下载使用
```

如果用户下载Excel、修改后重新上传，上传文件应重新解析并登记为新的数据集，不能继续视为旧数据集。

## 4. 生成Excel时保存上下文

查询结果标准化后，由“数据集登记节点”保存数据并生成引用：

```python
dataset_ref = register_dataset(
    dataframe=df,
    tenant_id=state.tenant_id,
    user_id=state.user_id,
    conversation_id=state.conversation_id,
    source_type="DATABASE_QUERY",
    snapshot_id=state.dataset["snapshot_id"]
)

state.dataset_reference = dataset_ref
```

同时在Redis会话状态中记录最近的数据集：

```json
{
  "last_dataset_id": "ds_20260824_001",
  "recent_datasets": [
    {
      "dataset_id": "ds_20260824_001",
      "display_name": "2026年销售额明细",
      "created_by_message_id": "msg_001"
    }
  ]
}
```

返回用户的响应也保留文件和数据集引用：

```json
{
  "answer": "已生成2026年销售额明细表。",
  "files": [
    {
      "file_name": "2026年销售额明细.xlsx",
      "file_ref": "controlled-file-ref"
    }
  ],
  "dataset_references": [
    {
      "dataset_id": "ds_20260824_001",
      "display_name": "2026年销售额明细"
    }
  ]
}
```

`dataset_id`不一定需要展示在页面上，但后端必须保留。

## 5. 用户再次提问的处理流程

例如用户说：

> 去掉销售额最高和最低的两个月，再计算平均值。

完整处理过程：

```text
收到新问题
→ 恢复最近dataset_id
→ 判断是新数据库查询还是历史数据集操作
→ 将自然语言转换成结构化数据操作
→ 校验字段、权限和操作白名单
→ 加载Parquet
→ 确定性执行计算
→ 校验结果
→ 登记新的派生数据集
→ 返回结果或生成新Excel
```

建议增加`dataset_follow_up`路由节点，输出：

```json
{
  "operation_scope": "EXISTING_DATASET",
  "target_dataset_id": "ds_20260824_001",
  "operation_type": "FILTER_AND_AGGREGATE",
  "operations": [
    {
      "type": "REMOVE_EXTREMES",
      "column": "销售额",
      "count": 1,
      "direction": "BOTH"
    },
    {
      "type": "AGGREGATE",
      "function": "MEAN",
      "column": "销售额"
    }
  ]
}
```

大模型只负责理解并生成结构化操作，具体计算必须由程序执行。

## 6. 如何安全读取数据集

读取前必须根据`dataset_id`检查租户、用户、权限、状态和有效期。

```python
def load_dataset(dataset_id: str, context: dict):
    metadata = dataset_registry.get(dataset_id)

    if metadata is None:
        raise DatasetNotFoundError()

    if metadata["tenant_id"] != context["tenant_id"]:
        raise PermissionError("无权访问该数据集")

    if metadata["user_id"] != context["user_id"]:
        raise PermissionError("无权访问该数据集")

    if metadata["status"] != "ACTIVE":
        raise DatasetExpiredError()

    return read_parquet(metadata["storage_ref"])
```

使用Polars读取：

```python
import polars as pl

df = pl.read_parquet(storage_path)
```

数据量较大时使用惰性读取：

```python
lazy_df = pl.scan_parquet(storage_path)

result = (
    lazy_df
    .filter(pl.col("销售额") > 0)
    .group_by("区域")
    .agg(pl.col("销售额").sum())
    .collect()
)
```

也可以使用DuckDB查询Parquet：

```sql
SELECT 区域, SUM(销售额) AS 销售额合计
FROM read_parquet('受控文件路径')
GROUP BY 区域
ORDER BY 销售额合计 DESC
```

对象存储路径不能由用户提供，必须由服务端根据`dataset_id`查询获得。

## 7. 加减乘除和计算列

例如用户说：

> 增加一列利润，利润等于销售额减去成本。

大模型生成受控计划：

```json
{
  "operation_type": "ADD_CALCULATED_COLUMN",
  "output_column": "利润",
  "expression": {
    "operator": "SUBTRACT",
    "left": {"column": "销售额"},
    "right": {"column": "成本"}
  }
}
```

Polars确定性执行：

```python
result_df = df.with_columns(
    (pl.col("销售额") - pl.col("成本")).alias("利润")
)
```

不允许直接执行大模型生成的任意Python代码：

```python
# 禁止这样做
exec(model_generated_code)
```

应该建立操作符白名单：

```python
ALLOWED_OPERATORS = {
    "ADD",
    "SUBTRACT",
    "MULTIPLY",
    "DIVIDE"
}
```

除法需要处理分母为零：

```python
result_df = df.with_columns(
    pl.when(pl.col("订单量") != 0)
    .then(pl.col("销售额") / pl.col("订单量"))
    .otherwise(None)
    .alias("客单价")
)
```

## 8. 筛选、排序和聚合

### 8.1 筛选

用户说“只保留销售额大于100万的区域”：

```json
{
  "type": "FILTER",
  "conditions": [
    {"column": "销售额", "operator": ">", "value": 1000000}
  ]
}
```

```python
result_df = df.filter(pl.col("销售额") > 1_000_000)
```

### 8.2 排序

```python
result_df = df.sort("销售额", descending=True)
```

### 8.3 分组汇总

```python
result_df = (
    df.group_by("区域")
    .agg([
        pl.col("销售额").sum().alias("销售额合计"),
        pl.col("订单量").sum().alias("订单量合计")
    ])
)
```

### 8.4 环比增长率

```python
result_df = (
    df.sort("月份")
    .with_columns(
        (
            (pl.col("销售额") - pl.col("销售额").shift(1))
            / pl.col("销售额").shift(1)
        ).alias("环比增长率")
    )
)
```

执行环比前需要确认时间排序、时间粒度、上一期是否存在及基期是否为零。

## 9. 两张表如何合并

例如系统已有数据库实际销售数据集`ds_actual`，用户又上传预算Excel并形成`ds_budget`，随后要求：

> 合并实际销售额和预算，计算完成率。

处理流程：

```text
确认两张表
→ 获取字段Schema
→ 推断关联键候选
→ 确认月份、区域等关联键
→ 检查关联基数
→ 检查单位、币种、粒度和快照
→ 执行Join
→ 检查匹配率和数据膨胀
→ 计算派生字段
→ 登记新数据集
```

结构化合并计划：

```json
{
  "operation_type": "JOIN",
  "left_dataset_id": "ds_actual",
  "right_dataset_id": "ds_budget",
  "join_type": "LEFT",
  "join_keys": [
    {"left_column": "月份", "right_column": "月份"},
    {"left_column": "区域编码", "right_column": "区域编码"}
  ]
}
```

执行代码：

```python
merged_df = actual_df.join(
    budget_df,
    on=["月份", "区域编码"],
    how="left"
)

merged_df = merged_df.with_columns(
    pl.when(pl.col("预算销售额") != 0)
    .then(pl.col("实际销售额") / pl.col("预算销售额"))
    .otherwise(None)
    .alias("预算完成率")
)
```

### 9.1 为什么必须检查关联基数

如果左右两表的关联键都重复，直接Join可能造成数据膨胀：

```text
左表2行 × 右表3行 = 6行
```

金额会被重复计算。因此必须检查：

- 一对一。
- 一对多。
- 多对一。
- 多对多。
- 关联键空值。
- 未匹配记录。
- 关联覆盖率。

检测到不明确的多对多关系时，必须追问用户或先按关联键聚合，不能直接合并。

## 10. 派生数据集和数据血缘

每次重要转换都应该生成新的派生数据集，不要直接覆盖原始数据集。

```json
{
  "dataset_id": "ds_20260824_002",
  "parent_dataset_ids": ["ds_20260824_001"],
  "operation": {
    "type": "ADD_CALCULATED_COLUMN",
    "output_column": "利润"
  },
  "storage_ref": "datasets/tenant_001/ds_20260824_002/data.parquet",
  "row_count": 120,
  "created_at": "2026-08-24T10:30:00+09:00"
}
```

最终形成：

```text
数据库查询结果 ds_001
→ 增加利润列 ds_002
→ 按区域汇总 ds_003
→ 与预算表合并 ds_004
→ 生成Excel
```

这样可以追踪每一步、回退错误操作、保留审计记录并避免破坏原始数据。

## 11. “这个表”指哪张表

解析数据集引用的推荐优先级：

1. 用户在当前消息中明确引用的附件。
2. 当前消息绑定的Excel。
3. 当前任务刚刚生成的数据集。
4. 当前会话最近使用的数据集。
5. 用户通过名称明确指出的数据集。
6. 仍有多个候选时进行追问。

例如会话中同时存在销售额明细表、预算表和实际预算对比表，用户只说“给这个表增加一列完成率”，系统不能直接猜测，应展示候选让用户确认。

## 12. 用户修改后重新上传Excel

用户下载并修改的Excel重新上传后，应执行：

```text
文件安全检查
→ Workbook和Sheet扫描
→ 表头与字段类型识别
→ 数据质量检查
→ 转换为新数据集
→ 登记来源和dataset_id
→ 根据需要与旧数据集比较
```

读取Excel示例：

```python
import pandas as pd

sheets = pd.read_excel(
    file_path,
    sheet_name=None,
    engine="openpyxl"
)
```

解析时要处理多Sheet、多行表头、合并单元格、重复列、隐藏Sheet、公式、日期和金额格式、空白行、密码、宏和一对多关系。解析完成后最好转换成Parquet。

## 13. 对应数据智能体节点

本能力主要涉及原架构中的：

```text
03 会话与记忆恢复
04 会话控制识别
05 意图识别与槽位抽取
07 权威语义确认与参数规范化
08 信息完整性判断
09 追问与待办
13 Skill与Tool调度
16 查询结果标准化
17 数据集登记与历史复用
18 数据质量与算法适用性
20 确定性算法分析
21 分析结果交叉校验
22 图表与结构化洞察
```

建议补充专门的`dataset_follow_up`路由节点，用于判断用户是在操作旧数据集、使用旧条件重新查询、操作新上传Excel、合并多个数据集还是开始全新任务。

## 14. 推荐Tool设计

```text
register_dataset
load_dataset
get_dataset_schema
transform_dataset
filter_dataset
sort_dataset
aggregate_dataset
add_calculated_column
join_datasets
compare_datasets
export_dataset_to_excel
expire_dataset
```

`transform_dataset`不能接收任意Python代码，只能接收经过Schema校验的结构化操作：

```json
{
  "dataset_id": "ds_001",
  "operations": [
    {
      "type": "FILTER",
      "column": "销售额",
      "operator": ">",
      "value": 100000
    },
    {
      "type": "GROUP_BY",
      "columns": ["区域"],
      "aggregations": [
        {
          "column": "销售额",
          "function": "SUM",
          "alias": "销售额合计"
        }
      ]
    }
  ]
}
```

## 15. 推荐技术栈

```text
FastAPI：智能体接口
LangGraph：多轮状态与节点编排
Redis：会话、最近dataset_id、PendingTask和幂等状态
PostgreSQL：数据集注册表、操作记录、血缘和审计
MinIO：Parquet、上传Excel和生成Excel
Polars：筛选、计算、聚合和Join
DuckDB：针对Parquet执行复杂SQL分析
Pandas/OpenPyXL：读取和生成Excel
Pydantic：约束数据操作计划
```

## 16. 安全与稳定性要求

- `dataset_id`必须同时绑定租户、用户、应用和权限范围。
- 对象存储的真实路径不能由用户直接提交。
- 数据集必须设置TTL和状态。
- 数据集过期、权限变化或缺少字段时重新查询或追问。
- 禁止直接执行模型生成的Python代码。
- 只允许白名单操作符、聚合函数和Join类型。
- 所有结果记录父数据集、操作参数和算法版本。
- Join后校验行数、匹配率和关键指标守恒。
- Excel公式和宏不能在服务端无控制地执行。
- 数据集输出前执行敏感字段检查和脱敏。

## 17. 总结

Excel只是用户查看和下载的表现形式，系统真正复用的是Excel背后的结构化数据集。推荐将Parquet临时保存在MinIO等对象存储中，Redis保存会话中的`dataset_id`，PostgreSQL记录数据集元数据、操作记录和血缘。

用户继续提问时，系统先定位目标`dataset_id`，再把自然语言转换成受控的筛选、计算、聚合或合并计划，由Polars或DuckDB确定性执行，并把结果登记成新的派生数据集。这样才能保证表格追问既灵活，又安全、准确、可追溯。
