# 多维度功能复测报告（2026-08-26）

## 测试环境

- DataAnalysis Agent：当前工作区源码，隔离端口 8090
- SQL Translator：当前工作区源码，隔离端口 48001
- Oagnet：192.168.1.27:8021
- 数据库、Redis、MinIO：真实联调依赖
- 未停止或覆盖现有 8088、48000 服务

## 自动化回归

| 项目 | 结果 |
|---|---:|
| DataAnalysis Agent | 658 passed，1 warning |
| SQL Translator | 49 passed |
| Oagnet | 82 passed，1 warning |
| 合计 | 789 passed |

warning 均为 Starlette TestClient/httpx 兼容层弃用提示，不是业务失败。

## 真实场景矩阵

15类首轮场景中：12类完整闭环，1类数值正确但展示不完整，2类未完成业务目标。完整闭环率80%；若把错字数值命中算作部分成功，则功能成功/部分成功率86.67%。

并发4执行的观测延迟：P50 7.51s，P95/最大16.45s。样本量较小，不代表容量SLO。

### 已通过

- 单指标：2026年7月销售额=37274.0
- 长周期趋势：2026年1月至7月按月返回，时间字段统一为 order_info.pay_time
- 三指标跨月比较：销售额、订单量、客单价均正确返回并计算变化率
- 单维分组：区域销售额
- 二维分组：区域×渠道销售额，共17组
- Top5排名
- 否定表达：“不要订单明细”不再误触发明细查询
- 缺时间追问
- 指标口径：退款率定义成功
- 指标血缘：销售额定位到 order_info.pay_amount
- 独立多问题：查询+口径均完成
- 依赖DAG：查询→区域拆分→最高/最低→差额，4个任务全部完成
- 越界写请求被拒绝
- 多轮上下文：7月销售额→“那6月呢”，正确返回6月18415.08

### 剩余问题

1. **明细字段完整性（P0）**：用户要求“订单号、金额和状态”，Oagnet生成的ASL只包含订单号和状态，漏掉金额；同时使用create_time后样本结果为空。当前系统没有在SQL前核对“用户要求字段 ⊆ ASL投影字段”。
2. **错字答案标签（P1）**：“销受额”能绑定正确销售额并返回37274.0，但答案列名仍回显“销受额”。查询正确，规范化展示未闭环。
3. **数据质量空集体验（P1）**：检查7月订单空值和重复记录时，按create_time查询为空，系统SAFE_FALLBACK。安全上正确，但应明确区分“范围内无记录”和“分析失败”，并提示当前使用的时间口径。
4. **实体改写服务降级（P1）**：`/vector/entity-attributes/search`仍为404，`entity_rewrite_contract=false`；复杂别名、错别字和实体枚举仍依赖模型偶然纠正。
5. **可靠性普遍LIMITED（P1）**：真实查询数据质量标记为`UNVERIFIED_SOURCE_SNAPSHOT`，即使查询与算法门禁全部通过也不能达到HIGH。
6. **模型总结经常降级（P2）**：Qwen总结多次因不可用或证据校验不通过而退回确定性答案；数值安全未受影响，但表达能力和延迟受影响。

## 本轮修复

- SQL逻辑时间维度和所有已注册时间维度均强制绑定`time_context.anchor`。
- 时间维度替换后同步修正ORDER BY字段，避免别名导致数据库1054。
- 长于等于60天的趋势查询向Oagnet明确要求按月分组。
- “那6月呢”展开为独立问题“查询2026年6月销售额”，不再把上轮7月范围合并进来。
- 新增相应回归测试；DataAnalysis Agent由657增至658项，SQL Translator由48增至49项。

## 发布建议

当前版本可发布用于明确指标、分组、排名、趋势、对比、元数据、多问题及确定性复合运算。明细导出/查询在增加ASL字段覆盖门禁前不应宣称完全闭环；实体别名纠错在向量端点恢复前应继续标记降级。
