# Phase 2.5.1 current code baseline audit

- CURRENT_FACT: Development and test root is `E:/YouoAgent/DataAnalysis_Agent`; version repository is `E:/yy`. Initial repository branch was `main`, HEAD `da3dd6ebe7fcd26644b91caa7f3eefb57c649a9d`, and the worktree was clean. Origin is `https://github.com/dreamyang-1/data.git`.
- CURRENT_FACT: The full tracked-file SHA-256 audit records 346 matches, 6 workspace differences and 1 missing workspace file. The missing file is repository-local `AGENTS.md`; development inherits `E:/YouoAgent/AGENTS.md`. All seven preexisting differences are preserved. See `preexisting_workspace_drift.json`.
- CURRENT_FACT: Full-file static reading/parsing and definition/line/hash inventories cover the requested source, tests, Phase 2/2.5 artifacts and design report. `source_inventory.json` records coverage as `FULL_FILE_STATIC_SCAN`; this is not a claim that every unrelated production branch has been behaviorally proved.
- UNKNOWN: Exhaustive behavioral correctness of the 10,114-line production orchestrator and other unrelated legacy branches is not established by static scanning or the regression suite.
- CURRENT_FACT: Baseline `semantic_v2.models.TimeSeriesPayload` inherited from `GroupedAggregatePayload`, so an ordinary single-metric trend incorrectly required nonempty `group_by`. `ComparisonPayload`, forecast and anomaly shared that restriction.
- CURRENT_FACT: Baseline `apply_slot_operations` interpreted the incoming array as execution order and always incremented task version, including no-op requests. Core slot snapshots used `Any`.
- CURRENT_FACT: Baseline `apply_state_event` set the single `pending` to `None` on topic switching/return. Conversation reference validation and mutation/message idempotency were absent.
- CURRENT_FACT: Baseline adapter marked generic approximations and dataset post-processing `LOSSY_COMPENSATED` with `can_execute_safely=True`, without implemented compensation or proof.
- CURRENT_FACT: Baseline PlanEnvelope mixed parse, candidate, execution and proof fields. Only top-level semantic bindings were checked against allowed business domains. Payload/result refs did not receive uniform recursive authorization or snapshot validation.
- CURRENT_FACT: Baseline filters only checked null operators; free-string comparison/calculation/algorithm identifiers, untyped output columns, unrestricted DialogueAct control actions and optional dataset-operation fields remained.
- CURRENT_FACT: Production `app.intent.structured` uses `StructuredIntentOutput`, a 40-rule SYSTEM_PROMPT, configured response format and temperature 0. `TurnAdmissionGate.evaluate` still has a `current.missing_slots -> AMBIGUOUS_RELATION` branch. This phase does not rewrite either production path.
- CURRENT_FACT: Production parser, orchestrator, storage, API and model configuration hashes remain unchanged from the starting workspace. No production module imports semantic_v2. The semantic_v2 import inventory covers the requested files; repository-wide import checks are also part of closure verification.
- OBSERVED_FAILURE: Frozen offline baseline is 1495 passed, 32 failed, 0 collection errors. Failure node IDs are preserved in `baseline_test_gate.json`; the same IDs are compared individually in `test_gate_comparison.json`.
- OBSERVED_FAILURE: A deterministic legacy probe for `医院名称字段来自哪个表？` yields DATA_LINEAGE with `missing_slots=['metric']`; `sales_order.amount_with_tax上游是什么？` is classified METRIC_QUERY. The current legacy SemanticAdapter.lineage API accepts MetricRef only (`app/adapters/base.py` and `app/adapters/http.py`).
- INFERENCE: Removing the legacy metric requirement alone would expose an unsupported backend path. V2 supplies typed FIELD/COLUMN/TABLE/etc. lineage contracts, but this phase does not invent a working legacy field-lineage service or expand regex rules.
- CURRENT_FACT: `AnalysisEngine._forecast` rejects horizons other than one; `DeterministicForecastSelector.VERSION` is `forecast-selector-v1`. A three-month V2 request is a legal semantic contract, with execution unsupported by the current legacy adapter.
- CURRENT_FACT: `AnalysisEngine._data_quality` implements null and duplicate scanning of returned rows. Other quality enum values describe future contract requests; no live freshness, reconciliation, or completeness backend support is claimed.
- UNKNOWN: Metric/metadata catalog browsing is not established by the inspected backend contracts, so the registry explicitly records it UNSUPPORTED rather than inventing payload/backend capabilities.
- PROPOSAL: Reconcile preexisting workspace/Git source differences in a separately reviewed change before expecting the Git checkout to reproduce the development workspace's complete legacy suite. This phase does not overwrite those files.

Historical Phase 2/2.5 reports are design context. Their old totals and claims do not replace current source or the frozen node-by-node test evidence.
