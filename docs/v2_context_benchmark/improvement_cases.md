# V2 Improvement Cases

仅列出同一冻结输入上 V2 正确且 V1 错误的Case。

## c0765280c4 — 算了，不查这个了，查询各医院等级对应的医院数量。

V1错误继承费森尤斯过滤和默认时间；V2正确建立医院等级分组、医院数量的新任务。

- V1错误类型：CONTEXT_STATE_ERROR
- V2完整问题：查询按医院等级分组的已合作医院数。

## 73bb50b318 — 统计每家医院承接的订单总金额（含税）及订单笔数，并关联医院等级。

V1制造了整句产品过滤；V2正确生成按医院分组的含税金额、订单笔数，并投影医院等级。

- V1错误类型：CANONICAL_MAPPING_ERROR
- V2完整问题：查询按医院分组的订单笔数和含税销售总额。
