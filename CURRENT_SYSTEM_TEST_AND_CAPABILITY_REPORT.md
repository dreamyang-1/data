# 数据分析智能体当前系统测试与能力说明

## 2026-08-26 最新增量结论（以本节为准）

本轮已继续补齐两项此前未闭环的能力：

1. **销售额语义口径已修正并真实复算通过**：语义模型8中，订单销售额统一使用指标 `ent_order_total_pay_amount`、公式 `SUM(order_info.pay_amount)`，统计时间绑定 `order_info.pay_time`，支持日、周、月、季度、年。已消除“销售额”与“商品明细小计总额”的同义词冲突并重建模型8、业务域13的向量索引。真实请求“查询2026年7月销售额”返回 `37274.0`，与直接执行只统计有效订单的人工SQL结果 `37274.00` 完全一致。
2. **同会话运行中取消已实现**：后端无需增加专门接口，继续调用 `/agent_chat` 或 `/agent_chat/stream`，使用相同 `application_id`、`conversation_id` 和新的 `message_id`，问题传“取消”“停止查询”等指令。服务会取消同租户、同用户、同应用、同会话内正在运行的任务，同时清理待澄清状态和未完成DAG。不同用户即使会话ID相同也不能互相取消。

SSE取消终态现在使用独立的 `event: cancelled`，Body中的 `status` 为 `CANCELLED`，不再混在 `completed` 事件里。当前取消能够立即终止智能体对异步上游调用的等待并丢弃迟到结果；如果SQL执行服务已经把查询提交到数据库，是否物理终止数据库线程仍依赖SQL服务后续提供查询句柄和取消能力。

最新全量自动化回归：**652 passed，1 warning**。警告仍是Starlette TestClient兼容层弃用提示，不是业务失败。

## 2026-08-26 本地隔离进程完善复测

本轮使用当前工作区源码启动独立测试进程，不占用公司联调端口：DataAnalysis Agent 使用 `127.0.0.1:8089`，Oagnet 使用 `127.0.0.1:8022`。现有 `8088`、`48000` 服务未停止、未覆盖。

### 本轮真实链路结果

| 检查项 | 结果 | 结论 |
|---|---:|---|
| `GET /ready` | HTTP 200 | `core=true`、`query_pipeline=true`；语义元数据独立能力未就绪，因此 `full_feature=false` |
| 闲聊“你好” | `COMPLETED / CHAT / 0.95` | 正常 |
| 能力询问“你能做什么？” | `COMPLETED / CAPABILITY_HELP / 0.95` | 正常 |
| “查询最近一周销售额” | 18.12 秒，HTTP 200，`SAFE_FALLBACK` | 未伪造数据；准确提示语义模型缺少有效时间字段绑定 |
| SSE 同一业务问题 | 1 accepted、13 progress、2 heartbeat、1 answer_chunk、1 completed | 流式过程真实可见，且只有一个终态事件 |
| SSE 上游错误透传 | `ASL_TIME_ANCHOR_INVALID` | 能定位到语义时间锚点，不再压缩成无法排查的统一错误 |
| 相同消息网络重试 | HTTP 200，`request_id` 相同 | 幂等缓存生效，没有重复执行 |
| 相同 `message_id` 更换问题 | HTTP 409 | 防止把新问题误判为网络重试 |
| 缺少可信身份 Header | HTTP 401 | 身份边界生效 |
| Header/Body 应用 ID 不一致 | HTTP 400 | 应用隔离校验生效 |

### 本轮代码完善

1. 总请求截止时间由 30 秒调整为 120 秒，并新增独立的 ASL 生成超时 90 秒，避免较慢但正常的模型调用被过早终止。
2. Oagnet 的 `/agent/query` 对确定性 4xx/5xx 业务校验错误不再自动重试，避免一次请求重复调用大模型并放大延迟。
3. `ASL_TIME_ANCHOR_INVALID`、`ASL_METRIC_SELECTION_INVALID`、`ASL_DIMENSION_INVALID`、`ASL_SCOPE_INVALID` 使用面向配置人员的精确提示，不再错误建议用户反复重试。
4. SSE 的失败进度保留稳定的 `error_code` 和 `upstream_code`，便于后端、日志和告警系统定位责任服务。

### 当前唯一阻塞该销售额示例的问题

代码已经在 SQL 翻译和执行前拒绝了不可信时间字段。剩余问题属于语义模型配置：模型 8 的销售额指标尚未绑定权威统计时间字段。需要语义层确认该指标应使用 `order_info.order_date` 还是实际支付时间字段，并配置指标与时间维度的绑定后重建相关向量。智能体不应自行猜测字段，否则可能返回数值正确但业务口径错误的结果。

本轮回归结果：DataAnalysis Agent `649 passed, 1 warning`；Oagnet `82 passed, 1 warning`；SQL Translator `46 tests OK`。警告均为 Starlette TestClient 对 httpx 兼容层的弃用提示，不是业务失败。

### SQL结果文件判断补充

SQL执行后的文件交付已经按结果规模分为两条互斥路径：

1. **大结果**：SQL服务返回完整结果的 `result_file_url` 时，智能体直接采用该文件，不再基于空数据或预览数据生成第二份文件。报表请求返回 `COMPLETED`，并在证据中标记 `complete_result_file=true`。
2. **小结果**：SQL服务直接返回结构化行数据。普通查询不生成文件；只有用户明确提出“导出/生成/下载 Excel、XLSX、PDF、Word、DOCX”时，智能体才保存会话数据集、生成文件并上传MinIO，下载地址放在 `files[0].download_url`。
3. **失败隔离**：小结果的MinIO文件生成失败时保留已经完成的查询/分析结果，并明确提示文件生成失败；不得把文件失败伪装成数据查询失败。
4. **格式规则**：用户明确指定PDF或Word时分别生成PDF/DOCX；未指定或要求Excel时生成XLSX。超大明细不转换为Word/PDF，优先使用SQL服务生成的完整结果文件，避免内存和排版风险。

新增回归覆盖：文件交付意图的大小写和自然表达、大结果文件直接复用、小结果持久化后生成文件、第二轮“把刚才结果导出成Excel”直接复用会话数据集，以及MinIO文件生成失败时仍保留有效查询结果。全量测试已更新为 `649 passed`。

### SQL Translator历史缺陷复核

测试文档后部关于“未知指标闭锁、跨表Join基数保护、时间过滤优化、Schema/血缘目录能力可能被新源码覆盖”的记录已经重新验证。当前 `E:\YouoAgent\sql-translator` 执行 `python -m unittest discover -v`，46项全部通过，已覆盖：

- 独立 `/api/translate` 与 `/api/execute`；
- 单条只读SQL门禁、危险语句和文件导出拦截；
- 未知指标、时间类型、粒度和跨表维度失败闭锁；
- 一对多、多跳和反向Join的聚合膨胀保护；
- 指标定义、依赖指标、字段血缘和时间维度映射；
- 半开时间范围、ISO周跨年和安全过滤条件。

因此“当前工作区源码缺失上述能力”已经解决。仍未解决的是部署一致性：公司服务器运行进程是否加载了这份源码，需要运维通过构建指纹和安全探针确认，不能由本地测试代替。

## 文档使用规则与结论有效性

本文保留了多轮历史测试记录，后文会出现“当时未实现”“当时已闭环”等阶段性结论。为避免误读，统一执行以下规则：

1. 结论优先级按文档从上到下排列，越靠前越新；本节及“本地隔离进程完善复测”为当前有效结论。
2. 自动化测试通过只证明代码分支符合测试契约，不等于公司服务器已经部署该版本。
3. `/ready` 返回 HTTP 200 只表示核心运行依赖可用；必须继续检查 `readiness_profiles` 和 `degraded_capabilities`。
4. HTTP 200 不等于业务成功；必须检查 `status`。只有 `COMPLETED` 且证据、可靠性门禁满足要求，才算数据分析成功。
5. `SAFE_FALLBACK` 表示系统正确阻止了不可靠回答，属于安全机制通过，但对应业务用例仍判定为“未闭环”。
6. 单次延迟只能作为功能联调记录，不能作为性能指标。P50、P95、吞吐量必须通过固定并发、固定样本和足够持续时间的压测获得。

## 测试范围、通过标准与证据要求

### 分层测试范围

| 测试层级 | 验证对象 | 当前状态 | 不能替代的测试 |
|---|---|---|---|
| 静态与单元测试 | Schema、算法、状态机、异常分支 | 已完成 | 真实依赖联调 |
| 组件契约测试 | Oagnet、SQL Translator、Redis、MinIO、知识库接口契约 | 大部分完成 | 公司部署版本核验 |
| 本地真实进程测试 | 当前工作区 DataAnalysis Agent 与 Oagnet | 已完成基础链路 | 正式服务器端到端测试 |
| 公司环境联调 | 8088、8021、48000及数据库 | 部分完成 | 容量、容灾、安全验收 |
| 业务验收 | 指标口径、真实数值、分析结论 | 销售额示例未闭环 | 不能由代码测试替代 |
| 非功能验收 | 性能、稳定性、故障恢复、安全 | 尚未完整执行 | 上线前必须补齐 |

### 统一判定标准

| 用例类型 | 通过条件 | 失败条件 |
|---|---|---|
| 闲聊/能力说明 | HTTP 200、意图正确、`COMPLETED`、无无关数据查询 | 误触发查询、错误追问或500 |
| 信息不足 | HTTP 200、`NEEDS_CLARIFICATION`、一次最多5个明确问题、状态可续接 | 猜测缺失信息、重复追问或状态丢失 |
| 数据查询 | `COMPLETED`、存在可审计证据、SQL只读、口径匹配、结果可复算 | 无证据给数值、错误字段、越权或口径不一致 |
| 分析/归因/预测 | 输入数据充足性通过、确定性算法产生结果、大模型只负责表达 | 大模型生成未经算法支持的结论 |
| 安全降级 | 返回稳定错误码和用户可理解提示，不泄露SQL凭据或堆栈 | 编造结果、吞掉错误原因或泄露敏感信息 |
| SSE | 事件顺序合法、可有心跳、答案分片可拼接、恰好一个终态 | 无终态、多个终态、连接静默超过网关阈值 |
| 幂等 | 相同有效载荷复用结果；相同ID不同载荷返回409 | 重复执行或静默覆盖旧请求 |

### 证据分级

- **A级：真实端到端证据**——实际服务、实际数据库或对象存储、可核对请求ID和结果。
- **B级：本地真实进程证据**——当前源码启动真实HTTP进程，但可能使用测试数据或本地配置。
- **C级：自动化契约证据**——pytest或替身依赖验证代码逻辑。
- **D级：代码存在性**——仅确认存在类、函数或接口，不视为功能通过。

文档中的“已实现”至少需要C级证据；“可联调”至少需要B级证据；“业务闭环”必须具备A级证据并通过业务人员数值复核。

## 当前关键用例追踪表

| 用例ID | 场景 | 期望 | 当前结果 | 证据级别 | 结论 |
|---|---|---|---|---|---|
| DA-API-001 | `你好` | CHAT并完成 | `COMPLETED / CHAT / 0.95` | B | 通过 |
| DA-API-002 | `你能做什么？` | 能力说明，不查库 | `COMPLETED / CAPABILITY_HELP / 0.95` | B | 通过 |
| DA-QRY-001 | 最近一周销售额 | 返回真实销售额及证据 | 时间锚点配置缺失，安全降级 | B | 未闭环 |
| DA-SSE-001 | 销售额流式查询 | 过程、心跳、答案、单终态 | 13 progress、2 heartbeat、1 completed | B | 协议通过，业务未闭环 |
| DA-IDEM-001 | 完全相同请求重试 | 不重复执行 | 返回相同 `request_id` | B | 通过 |
| DA-IDEM-002 | 相同消息ID换问题 | 拒绝冲突 | HTTP 409 | B | 通过 |
| DA-AUTH-001 | 缺少可信身份 | 拒绝 | HTTP 401 | B | 通过 |
| DA-SCOPE-001 | Header/Body应用不一致 | 拒绝 | HTTP 400 | B | 通过 |
| DA-XLSX-001 | 平台真实多Sheet文件问数 | 跨Sheet查询并可追问 | 本轮未做真实对象验收 | D/C | 待验收 |
| DA-DAG-001 | 多问题、去重、Join、续跑 | 可审计DAG结果 | 自动化覆盖，未完成本轮真实复杂链路 | C | 待A级验收 |
| DA-FORECAST-001 | 预测分析 | 充足性、回测、区间、算法证据 | 代码和自动化已覆盖 | C | 待业务验收 |

## 尚未完成的测试及执行方案

| 优先级 | 待测项目 | 建议方法 | 通过门槛 | 责任方 |
|---|---|---|---|---|
| P0 | 模型8销售额时间口径 | 语义层绑定权威字段并重建向量后，使用SQL人工复算 | 智能体结果与人工SQL一致，误差为0 | 语义层、数据侧、智能体 |
| P0 | 公司环境源码版本一致性 | 记录构建版本/提交哈希，检查健康接口构建信息 | 8088、8021、48000均可追溯到预期版本 | 运维、各服务负责人 |
| P1 | 真实多Sheet Excel | 平台上传包含公式、空Sheet、重复列、中文列名和大Sheet的文件 | 导入、跨Sheet问数、追问、过期清理全部通过 | 后端、前端、智能体 |
| P1 | 真实复杂DAG | 构造重复查询、依赖查询、Join、部分失败和重启续跑 | 无重复查询；失败隔离；恢复后结果一致 | 智能体、数据侧 |
| P1 | 并发容量 | 分别以1/5/10/20并发持续至少30分钟 | 错误率、P95和资源使用达到上线SLO | 运维、智能体 |
| P1 | Redis/MinIO故障 | 测试环境短暂断开、恢复、对象损坏、TTL过期 | 不编造结果；恢复后状态一致；无脏引用 | 运维、智能体 |
| P1 | 安全测试 | SQL注入、提示注入、SSRF路径、超大请求、跨租户数据 | 全部拒绝或安全降级，无敏感信息泄露 | 安全、后端、智能体 |
| P2 | 长稳测试 | 混合查询持续8至24小时 | 无持续内存增长、任务泄漏或连接池耗尽 | 运维、智能体 |

性能门槛需要产品和运维共同确定。在门槛未确定前，文档只能报告测得数据，不能使用“性能达标”字样。

## 语义层修复后的最小可执行复测

1. 固定 `semantic_model_id=8`，由语义层提供销售额指标编码、权威时间字段、状态过滤条件和业务域范围。
2. 准备一段可人工计算的小样本数据，明确期望订单数、销售额和按日汇总结果。
3. 分别执行指标查询、按日趋势、同比/环比、明细钻取四类请求。
4. 保存智能体 `request_id`、标准化问题、意图、ASL、SQL、SQL参数、数据集ID、证据和最终答案。
5. 用只读SQL客户端独立执行同口径SQL并逐项比对；任何数值不一致都判失败，不能用自然语言解释绕过。
6. 再执行缺字段、空结果、重复数据、取消订单、支付失败订单、时区跨日和月末边界用例。
7. 同一问题通过同步与SSE接口各执行一次，最终结构化结果必须一致。

建议将本节7步固化为发布流水线中的业务冒烟测试，并使用脱敏、可重复初始化的数据集。

## 2026-08-26 当前版本全面多维度复测（历史阶段记录）

### 1. 测试结论

当前数据智能体的代码框架、接口契约、确定性分析算法、会话状态、数据集、DAG和安全降级机制已经具备初版联调基础；公司服务器 `192.168.1.49:8088` 的健康检查、同步接口和新版SSE流式协议也能够正常访问。

但本轮真实请求证明，当前部署环境还不能认定为“业务查询全链路闭环”：实体改写服务和语义元数据能力未就绪，业务域自动选择未形成真实闭环，指标/趋势请求最终进入 `SAFE_FALLBACK`。因此当前适合做接口协议、闲聊、澄清和流式过程展示，不适合向业务方演示真实指标结果。

### 2. 测试环境与原则

本轮将两类结论严格分开：

- **本地当前源码测试**：目录 `E:\YouoAgent\DataAnalysis_Agent`，验证代码逻辑、异常分支和接口契约；外部依赖主要使用替身或模拟响应。
- **公司服务器真实测试**：`http://192.168.1.49:8088`，真实经过已部署智能体及其内网依赖；该结果代表当前联调环境，不用本地测试结果替代。
- 测试日期：2026-08-26。
- Python：`C:\Users\lenovo\miniconda3\python.exe`。
- 未对共享环境执行断网、停Redis、停MinIO、磁盘写满等破坏性故障注入；这些场景仅在自动化替身测试中验证。

### 3. 本地自动化与静态检查

| 检查项 | 结果 | 说明 |
|---|---:|---|
| Python字节码编译 | 通过 | `app`目录无语法错误 |
| Pytest收集 | 637项 | 测试均可正常收集 |
| 全量自动化回归 | **649 passed** | 最新全量复测耗时25.40秒 |
| 问题改写、意图、时间、澄清 | 273 passed | 含100条真实表达、对抗表达、错漏字和时间边界 |
| 分析、预测前置检查、排名 | 68 passed | 含趋势、对比、占比、异常、归因候选、预测模型选择 |
| Redis/长期记忆/MinIO/文件 | 72 passed | 含并发状态、数据集引用、存储路由、报表文件 |
| API、幂等、DAG、动态扩展 | 142 passed | 含422/409、DAG、Skill/Tool/MCP、依赖失败和HTTP适配 |
| 测试告警 | 1项 | Starlette TestClient对httpx的弃用提示，不是业务失败；升级依赖前需处理 |

关联Oagnet在增加细分ASL校验错误码后执行全量回归：`82 passed, 1 warning`。新增`ASL_TIME_ANCHOR_INVALID`、`ASL_METRIC_SELECTION_INVALID`、`ASL_DIMENSION_INVALID`和`ASL_SCOPE_INVALID`，供数据智能体、日志平台和运维告警准确定位失败阶段。

分组数量存在交叉覆盖，不能相加后代替637项全量测试总数。

### 4. 公司服务器真实接口复测

#### 4.1 服务与能力探针

| 项目 | 实测结果 | 判断 |
|---|---|---|
| `GET /live` | HTTP 200，`UP` | 进程存活 |
| `GET /ready` | HTTP 200，`READY` | Redis、MySQL长期记忆、MinIO均为true |
| `GET /openapi.json` | HTTP 200 | 仅暴露表格导入、同步聊天和SSE聊天三个业务接口 |
| ASL/SQL查询契约 | `true` | 接口形态可用，不代表每个查询均能完成 |
| 实体改写契约 | `false` | 别名、错漏和实体规范化当前会降级 |
| 语义元数据契约 | `false` | 指标定义、口径和血缘的独立能力未就绪 |
| 知识服务 | `true` | 健康探针可用，具体问答仍受知识库绑定范围影响 |

`/ready`当前把实体改写和语义元数据标成可选能力，所以仍返回 `READY`；但真实指标查询会受这些能力或其上游链路影响。建议后续把就绪状态拆成“基础就绪”和“业务查询就绪”，避免后端只看HTTP 200误判全功能可用。

#### 4.2 SSE流式输出

新版流式接口已经部署，真实事件顺序包含：

```text
accepted
progress（请求校验、任务规划、上下文恢复、问题改写、意图识别、完整性检查等）
answer_chunk（可多段）
clarification / completed / error（三选一终态）
```

本轮请求实际收到了多个 `progress` 和两个 `answer_chunk`，说明已不再是只在末尾一次性返回结果。网络很快或任务很短时，事件仍可能在很短时间内连续到达，前端必须逐事件消费，不能等连接关闭后一次性渲染。

#### 4.3 真实业务场景

| 输入场景 | 实际结果 | 结论 |
|---|---|---|
| `你好` | `COMPLETED / CHAT / 0.95` | 正常 |
| `你能做什么？` | `COMPLETED / CAPABILITY_HELP / 0.95` | UTF-8字节请求下正常 |
| 不传业务域：`查询最近一周销售额` | `METRIC_QUERY / 0.95`，metric和time_range完整，随后`SAFE_FALLBACK` | AUTO模式理解正常，数据链路失败 |
| 显式传`business_domain_id=13`：同一问题 | `METRIC_QUERY / 0.95`，槽位完整，之后`SAFE_FALLBACK` | 理解成功，数据链路失败 |
| 显式业务域：`分析2026年1月至6月销售额趋势` | `TREND_ANALYSIS / 0.95`，之后`SAFE_FALLBACK` | 理解成功，数据链路失败 |
| 缨少必填Body字段 | HTTP 422并返回字段级错误 | 契约校验正常 |

真实指标和趋势请求的最终可靠性为 `FAIL / 0.0`，无查询证据、无 `dataset_id`，系统正确地没有编造业务数值。逐段探测确认：当前公司8021旧版Oagnet生成了不存在的`order_item_detail.create_time`，SQL翻译成功后数据库执行返回1054未知字段。当前工作区新版Oagnet会在SQL翻译前拒绝该错误时间锚点，但语义模型8仍需配置真实时间字段并重新构建向量，然后部署新版Oagnet。

### 5. 多维度能力状态

| 能力维度 | 代码与自动化 | 当前线上真实状态 |
|---|---|---|
| 同步聊天、参数校验、幂等 | 已实现并通过 | 可用 |
| SSE过程流和答案分片 | 已实现并通过 | 已部署、可用 |
| 闲聊 | 已实现并通过 | 基础闲聊可用；能力问答存在误判 |
| 问题改写、别名、错漏规范化 | 已实现降级机制 | 上游契约false，当前降级保留原问题 |
| 意图识别 | 大量自动化通过 | 显式作用域问题较稳定；无业务域和能力问答存在真实误判 |
| 澄清与多轮状态 | 已实现并通过 | 接口可用；错误意图可能导致错误追问 |
| 业务域自动选择 | 代码已有AUTO模式 | **真实闭环未通过** |
| 指标/明细SQL查询 | 契约和异常分支已覆盖 | 当前真实指标请求安全降级，未拿到数据 |
| 趋势、对比、占比、异常 | 确定性算法已实现 | 依赖真实查询数据；本轮线上未进入算法阶段 |
| 归因 | 数据贡献和候选解释已实现 | 不等同因果推断；本轮线上未闭环 |
| 预测 | 数据充足性、模型选择、回测门禁已实现 | 尚未完成真实线上样本验收和独立算法服务验收 |
| 多问题DAG、去重、Join、续跑 | 自动化已覆盖 | 未做本轮线上复杂DAG验收 |
| Redis长短期记忆 | 自动化已覆盖，ready为true | 未执行真实停机恢复和集群切换演练 |
| MinIO/Parquet数据集 | 自动化已覆盖，ready为true | 本轮指标请求未产生dataset，未完成真实大结果复测 |
| Excel/多Sheet、报表 | 代码和自动化已覆盖 | 未使用真实平台上传对象完成本轮端到端验收 |
| Skill/Tool/MCP动态扩展 | 调度和契约测试通过 | 仍需逐个真实外部工具验收，不能按框架通过视为全部工具可用 |
| 安全降级与证据门禁 | 已实现并通过 | 真实依赖失败时正确返回SAFE_FALLBACK |

### 6. 本轮发现的问题与优先级

#### P0：阻塞真实业务演示

1. 公司8021仍运行旧版Oagnet，会放行不存在的`order_item_detail.create_time`；必须部署当前工作区新版，使错误语义字段在SQL执行前被拒绝。
2. 语义模型8的“销售额”指标缺少可靠时间口径绑定。需要在语义层把该指标绑定到真实存在且业务口径正确的时间字段，并重新构建对应向量索引。

#### P1：准确性和运维可观测性

1. `/ready`原先只有单一READY状态；本轮已增加`core`、`query_pipeline`、`full_feature`三种就绪画像和`degraded_capabilities`，部署后后端应按使用场景判断。
2. 实体改写契约为false，别名、错别字和实体值规范化只能降级；需部署Oagnet实体属性搜索能力后再做真实错漏测试。
3. 原HTTP适配器会把500响应压缩成统一`DEPENDENCY_UNAVAILABLE`；本轮已保留安全的上游错误码，将执行阶段映射为`SQL_EXECUTION_FAILED`，并写入SSE进度及结构化日志。
4. 首轮误判是测试脚本未按UTF-8字节发送中文导致的假象，不是意图模型缺陷。后端必须使用`application/json; charset=utf-8`并发送UTF-8字节。

#### P2：发布前非功能验收

1. 尚未完成真实并发容量、长时间稳定性、网络抖动、Redis/MinIO故障切换和磁盘容量演练。
2. 尚未用平台真实上传的多Sheet Excel完成导入、追问、派生数据集和报表下载全链路验收。
3. 尚未执行真实复杂DAG、跨分支Join、断点续跑及任务取消验收。
4. 需要处理Starlette TestClient/httpx弃用告警，防止后续依赖升级导致测试基础设施失效。

### 7. 当前可演示范围与发布判断

- **可以演示**：接口文档、同步聊天、基础闲聊、参数校验、澄清结构、SSE分析过程与答案分片、安全兜底。
- **暂不建议演示**：真实销售额/趋势结果、实体别名纠错、真实Excel大文件闭环、跨数据集复杂DAG；AUTO业务域的理解已通过，但仍受相同错误语义字段阻塞。
- **初版联调判断**：可以继续与Java后端做协议联调，但后端应暂时显式传入已确认的业务域，并把 `SAFE_FALLBACK` 当失败结果处理。
- **生产发布判断**：不具备。至少解决两个P0问题、完成真实查询数据集闭环和隔离环境非功能测试后再评估。

### 8. 修复后最低复测清单

1. 不传业务域请求“查询最近一周销售额”，应自动选域、识别metric/time_range并返回真实查询证据。
2. 显式业务域请求同一问题，应返回`COMPLETED`、非空`dataset_id`及可靠性门禁结果。
3. “你能做什么？”应命中CHAT/能力说明，不产生指标澄清。
4. 别名、错别字、属性值各至少20条真实请求，确认改写服务不再DEGRADED。
5. SSE在成功、澄清、依赖失败三条路径均只能产生一个终态事件。
6. 使用平台真实多Sheet Excel完成上传、导入、跨Sheet查询、追问和MinIO报表下载。
7. 执行至少一次并发、重启恢复和Redis/MinIO故障切换演练并保存监控数据。

---

以下章节为历史测试记录；如与本节冲突，以本节基于当前部署版本的结果为准。

## 2026-08-26 SQL Translator二次完整审核与修复

本轮不再只验证拆分接口，而是恢复并执行`sql-translator`全部历史加固测试。修复前测试无法收集且专项测试存在`26 failed, 11 passed`；修复后结果为：

```text
SQL Translator：44 passed
DataAnalysis Agent：613 passed, 1 warning
```

本轮修复包括：

- 恢复MySQL权威语义目录、指标解析、指标定义和字段级血缘接口；
- 未知指标、未知实体、未知维度和未知物理字段严格闭锁，不再生成`NULL`或猜测Join；
- 模型ID、AST容器、LIMIT、排序、重复别名和接口错误契约校验；
- 过滤操作符白名单、字符串转义、`IN/BETWEEN/NULL`处理以及全局`OR`条件括号；
- `dim_date`只映射到当前实体绑定的真实日期字段，订单销售使用`order_info.pay_time`；
- 周粒度使用ISO周，年/月/范围过滤使用索引友好的半开区间；
- 一对多、未知基数和多跳Join下的聚合重复累计保护；
- 派生指标仅展开已声明依赖，并使用完整标识符替换；
- 原始SQL执行入口拒绝多语句、写操作、文件导出、锁、延时函数、系统变量和子查询；
- 明细意图强制采用行级字段投影，清除Oagnet偶发附加的“明细聚合指标”；明细可靠性不再错误要求指标口径证明；
- `/api/health`增加源码SHA-256部署指纹，用于确认远端进程实际加载的文件版本。

使用本工作区原文件在临时端口真实连接Redis、MySQL和MinIO验证：

- 2026年7月销售额：`46259.0`；
- 2026年订单明细：`631`行，生成XLSX并成功下载；
- 销售额口径接口：HTTP 200，返回定义、公式、别名和业务域；
- 销售额血缘接口：HTTP 200，返回`order_info.pay_amount`且字段注册校验通过；
- 4路并发指标查询全部`COMPLETED`；
- DataAnalysis Agent `/ready=READY`，`semantic_metadata_contract=true`；
- `SELECT @@version`返回`SQL_SAFETY_REJECTED`。

以上是当前工作区源码的验证结果。正式`192.168.1.49:48000`仍需重新部署本轮文件；重启后必须核对`/api/health.build`中的两个哈希，并重新执行安全探针和明细查询，才能确认正式进程已加载该版本。

## 2026-08-26 48000重启后真实复测

重启后的正式服务已经暴露`/api/translate`和`/api/execute`，DataAnalysis Agent的`/ready`恢复为`READY`。真实闭环结果：

- 指标查询：2026年7月销售额返回`46259.0`并登记`dataset_id`；
- 趋势分析：2025年1月至2026年7月共19个月数据完成确定性趋势分析；
- 月度对比：修复单值聚合后，正确返回2026年6月`49440.8`、7月`46259.0`，下降`3181.8（6.44%）`；
- 大结果：201行结果返回`data=null + download_url`，XLSX下载HTTP 200；
- 澄清：缺时间的问题会追问；“查询2026年订单明细”现能识别全年，只追问缺少的字段；
- 明细追问：补齐字段后，最新版SQL源码已真实生成无`GROUP BY`、带`LIMIT 10000`的明细SQL，并查询631行后生成MinIO下载文件。

本轮修复包括完整年份/年份范围解析、对比分析必须返回至少两个分组、无指标明细投影、多线程SQL HTTP服务和稳定的翻译/执行业务错误响应。DataAnalysis Agent完整测试为`612 passed`，SQL拆分专项测试为`7 passed`。

2026-08-26 15:00后再次复测正式48000：

- `/api/health`、`/api/translate`、`/api/execute`均在线；
- 2026年7月销售额查询成功，返回`46259.0`；
- 2026年7月与6月对比成功，返回6月`49440.82`、7月`46259.0`，下降`3181.82（6.44%）`；
- 4个独立月份的指标请求并发执行全部`COMPLETED`，总墙钟时间约`5.16s`，此前的单线程排队问题本轮未复现；
- 201行查询按契约返回`data=null + download_url`，下载XLSX为HTTP 200、9333字节；
- `/ready`为`READY`，Redis、MySQL长期记忆和MinIO均为可用；实体改写搜索和语义元数据独立接口仍为可选降级状态。

但正式48000仍未完整加载当前工作区的两个关键修复：

1. Oagnet能够正确生成`metrics=[]`、5个投影维度的订单明细ASL；正式`/api/translate`仍返回HTTP 500：`AST中未包含指标信息`。因此“补齐字段后查询2026年订单明细”在正式链路中仍会降级为`DEPENDENCY_UNAVAILABLE`。当前工作区源码及专项测试已经支持无指标明细投影，说明运行服务与工作区代码版本不一致。
2. `SELECT @@version AS value`仍被正式`/api/execute`执行成功；当前工作区`validate_read_only_sql()`已明确拒绝`@@`。这同样说明安全门禁未部署到当前运行进程。

因此本轮结论是：**指标查询、趋势/对比分析、并发查询和大结果下载可以演示；明细查询暂不能宣称闭环。需要在192.168.1.49上确认48000的启动目录/构建产物确实来自当前`sql-translator`，重新部署后以“无指标明细翻译成功”和“`@@`探针被`SQL_SAFETY_REJECTED`拒绝”作为验收标准。**

## 2026-08-26 SQL翻译/执行拆分适配复核

数据智能体已停止调用一步式`/api/ast-to-sql`，正式链路调整为`Oagnet生成ASL → /api/translate → SQL只读安全检查 → /api/execute`。翻译和执行具有独立错误码及幂等键；执行阶段不做盲目自动重试。新SQL服务超过200行时返回`data=null + download_url`的契约也已兼容，查询响应会保留完整行数和下载地址。

DataAnalysis Agent完整自动化测试结果为`607 passed`。新增覆盖包括：翻译成功后才允许执行、翻译失败阻断执行、执行失败分类、拆分接口未部署的精确诊断、危险SQL阻断、大结果下载文件以及旧嵌套响应兼容。

本次拿到的sql-translator源码不是在原加固版本上增量合并，而是覆盖了部分已有能力。其旧回归测试当前仍发现未知指标闭锁、跨表Join基数保护、时间过滤优化、Schema/血缘目录等能力缺失。因此当前结论是：**数据智能体已完成新接口适配，但sql-translator本身尚未达到原加固版的完整回归基线；完成合并修复前不应发布到生产。** 本轮已经在独立执行入口补充只读SQL门禁和精确路由匹配，但未用临时补丁伪造缺失的语义目录能力。

## 2026-08-26 全维度复测（历史阶段记录）

### 一、是否已经完成基本框架

结论：**面向受控初版演示的基本框架已经完成，但面向生产发布的完整产品闭环尚未完成。**

当前已经形成以下主链路：

```text
HTTP接入 → UTF-8与契约校验 → 问题改写 → 意图识别 → 上下文/澄清
→ 多任务DAG → Oagnet生成ASL → SQL Translator翻译SQL → 只读校验 → 独立执行SQL
→ 结果数据集登记 → Oagnet/语义层指标校验 → 确定性分析/预测
→ 可选业务知识补充 → Qwen受约束总结 → 可靠性判断 → 结构化响应/流式响应
```

其中问题改写、意图识别、澄清、真实SQL查询、MinIO数据集、DAG查询去重、跨分支受控Join、DAG检查点、确定性分析、幂等和安全降级均已有实现及自动化测试。

不能称为生产完整闭环的原因：

1. 指标真实性、编码和口径由 Oagnet/语义层负责，文档知识库不再承担指标口径门禁。知识库未绑定时，指标查询和确定性分析仍可完成；但不会生成没有数据或文档证据的业务事件解释。
2. Excel/CSV及多Sheet问数、XLSX/DOCX/PDF对话内报表已经实现；数据库与上传文件的自动跨来源Join仍要求明确关联键，不能猜测关联关系。
3. 独立Policy权限服务、行列级权限和生产脱敏尚未验收。
4. 定时报表和消息推送尚未实现。
5. 查询取消、完整异步任务管理、大查询进度反馈尚未完成。
6. 尚未完成生产容量压测、长时间稳定性测试、Redis/MinIO集群切换和灾备演练。
7. 归因只提供数据支持的候选解释，不构成严格因果结论；预测尚未接入独立算法服务和复杂外生变量。

### 二、本轮测试结果

#### 1. 全项目自动化回归

```text
597 passed, 1 warning
耗时：22.57秒
```

唯一警告是Starlette测试客户端对`httpx`的弃用提醒，不是业务失败。

#### 2. 分维度测试

| 测试维度 | 数量 | 结果 | 覆盖重点 |
|---|---:|---|---|
| 确定性分析与预测 | 44 | 全部通过 | 空数据、异常点、趋势、对比、占比、归因候选、预测前置检查、滚动回测、排名一致性 |
| 故障恢复、状态和数据集 | 79 | 全部通过 | 超时、依赖失败、并发幂等、Redis状态、MinIO过期/损坏、Parquet、Join基数、DAG部分失败和断点恢复 |
| API、适配器和知识检索 | 86 | 全部通过 | 422/409契约、流式错误、上游503、缓存击穿保护、知识去重和跨作用域拒绝 |
| 改写、意图、澄清、时间和记忆 | 270 | 全部通过 | 口语、否定句、错别字、歧义、上下文覆盖、危险输入、多轮纠正、时间边界、长期记忆并发 |
| 全项目合计 | 597 | 全部通过 | 包含工作流、节点架构、综合编排、统一接口、动态Tool/MCP、文件问数、对话内报表、滚动时间窗口、空聚合和数据集持久化回归测试 |

#### 3. 真实Qwen意图识别

```text
测试集：200条真实及长尾表达
模型：qwen3.6-plus
Accuracy：100%
Macro F1：1.0000
Schema合法率：100%
模型错误：0
P50：2861.9ms
P95：3633.3ms
最大延迟：4455.5ms
```

规则模式在相同200条上的Accuracy为87.5%、Macro F1为89.33%。生产路径采用“强规则和安全规则优先，模糊表达走Qwen，模型输出再经过Schema及业务约束”的混合模式。测试集100%不代表未知线上问题永久100%，仍需持续收集脱敏误判样本。

### 三、真实非理想端到端测试

#### 1. 服务连通性

| 服务 | 实测结果 |
|---|---|
| DataAnalysis Agent `127.0.0.1:8088` | OpenAPI HTTP 200 |
| Oagnet内网 `192.168.1.27:8021` | `/agent/query`可用，但当前运行进程仍是旧版本，尚未暴露实体属性搜索接口 |
| Oagnet修订代码临时验证 `127.0.0.1:8022` | OpenAPI正常，实体属性搜索/同步/状态接口均已加载，79项自动化测试通过 |
| SQL Translator `192.168.1.49:48000/api/ast-to-sql` | 使用非法空请求探测返回HTTP 400，证明路由在线；40项自动化测试通过 |
| DataAnalysis Agent `/ready` | HTTP 200；核心查询、Redis、MySQL、MinIO、知识服务均就绪；实体改写与指标口径/血缘接口因上游仍运行旧版本而单独标记为降级 |

Oagnet代码已经包含实体属性向量搜索接口，但正式占用8021端口的仍是旧进程。当前智能体会保留原问题并继续查询，不会因可选改写能力不可用而整体判为不就绪；要恢复别名和错漏规范化，仍需将8021切换到修订版Oagnet进程。

SQL Translator修订代码已经在临时端口完成真实语义库验证：销售额能够解析为`6:actual_payment_amount`，并能返回指标定义、公式、绑定实体以及`order_info.pay_amount`字段级血缘。但正式`192.168.1.49:48000`当前仍只暴露旧的ASL转SQL接口，需由该服务器维护人员重启修订版后，`semantic_metadata_contract`才会变为`true`。这不影响普通问数，但会影响“销售额口径是什么”“销售额来自哪个表字段”等元数据问题。

#### 2. API真实场景

| 场景 | 实测结果 |
|---|---|
| “你好” | `COMPLETED / CHAT` |
| “删除数据库里的全部订单” | `COMPLETED / OUT_OF_SCOPE`，未执行写操作 |
| “帮我分析数据” | `NEEDS_CLARIFICATION`，缺少指标和时间 |
| “分析本月数据”后补充“销售额” | 澄清状态正确合并，进入真实查询 |
| 同一消息重复请求 | 返回同一`request_id`，没有重复执行 |
| 同一`message_id`更换请求内容 | HTTP 409 `MESSAGE_ID_REUSE_CONFLICT` |
| `/agent_chat/stream`真实SSE请求 | HTTP 200，`text/event-stream`，包含`accepted`和`completed`事件 |
| “查询最近一周销售额” | 能识别滚动7日范围；无匹配订单时明确返回“没有有效数据”，不再输出`None` |
| “查询2026年7月销售额” | `COMPLETED`，返回`销售额=46259.0`并成功登记MinIO数据集 |
| 中文JSON未按UTF-8发送 | 客户端会造成中文乱码和误识别；正式接口必须使用`application/json; charset=utf-8`并按UTF-8编码字节 |

#### 3. 真实查询与知识门禁

“查询2026年7月销售额”和“分析2025年1月至2026年7月销售额趋势”均已成功执行到 SQL 和 MinIO 数据集登记，并能从 Oagnet 生成的 ASL 中取得作用域内的规范指标编码。现在这份结构化语义证据直接用于指标可靠性校验，不再因 `knowledge_base_names` 为空而触发 `SAFE_FALLBACK`。

文档知识库只在应用已绑定范围时按需检索业务规则、制度和已知事件；检索不可用或无绑定时，不阻断查询和算法分析。

修改后真实接口复测（语义模型 6、业务域 7、`knowledge_base_names=[]`）：

- 问题：`查询2026年7月销售额`；
- 状态：`COMPLETED`；
- 结果：`销售额=46259.0`；
- 证据：`QUERY_RESULT` + `SEMANTIC_METRIC_RESOLUTION`；
- 三项可靠性门禁全部通过：查询成功、指标已绑定、语义指标已校验；
- 可靠性等级暂为 `LIMITED`，仅因 SQL 上游尚未返回完整的 `snapshot_id/data_as_of/quality_status`，与指标口径和知识库无关。

#### 4. 历史数据集追问缺陷及修复

非理想真实测试发现：“刚才销售额前1名”原先会先触发普通查询的时间和维度缺失检查，导致MinIO数据集追问没有执行机会。本轮已修改编排顺序：

1. 明确FOLLOW_UP时先尝试受控数据集操作；
2. 能安全识别时直接读取MinIO并生成不可变派生数据集；
3. 无法安全识别时才回退普通澄清或重新查询。

修复后实测：原数据集`ds-b233...`成功生成不同的新派生数据集`ds-87b5...`，没有再次要求时间和维度，证明历史结果追问的数据链路已闭环。最终答案仍受知识库门禁约束。

#### 5. 多问题和查询去重

真实请求同时包含两个完全相同的销售额查询和一个趋势分析，共返回3个任务结果。两个重复查询复用了同一个`dataset_id`，证明同构查询只执行一次；第三个趋势分支使用独立数据集。由于没有知识库绑定，各子任务均安全降级，DAG仍完成结果汇总和失败隔离。

### 四、极端和故障场景覆盖

自动化测试已经覆盖：

- 空结果、单行不足、截断结果和超过最大处理行数；
- 空值、重复行、字段缺失、未知字段、NaN、正负Infinity和非法Decimal；
- 时间范围倒置、非法日期、跨年、闰年、月末和时区边界；
- 预测样本不足、时间不连续、粒度错误、缺失值过多和模型回测失败；
- 排名分母不一致、比例重算不一致、排序错误和TopN数量异常；
- Join关联键缺失、作用域不一致、右表键重复和多对多膨胀风险；
- MinIO对象过期、对象缺失、校验和损坏、Parquet分片损坏和超大数据路由；
- Redis并发状态冲突、消息ID冲突、长期记忆并发确认和候选过期；
- Oagnet/SQL/知识库503、超时、非法响应和部分能力不可用；
- Qwen超时、低置信、Schema不合法、与强规则冲突和安全降级；
- 多任务部分失败、依赖失败跳过、重复任务合并、服务中断后的DAG检查点恢复；
- 知识检索重复块、跨知识库越权结果、缓存并发击穿和低相关结果；
- SQL写操作、危险指令、非法业务数值修改和超长/非法API输入。

没有在共享环境中主动关闭Redis、MinIO、Oagnet或SQL Translator做破坏式故障注入；这些故障通过适配器和存储替身自动化验证。生产发布前仍必须在隔离环境进行真实进程终止、网络分区、限速、磁盘满和集群切换演练。

### 五、最新能力状态

#### 已实现并可用于受控联调

- 问题改写、实体规范化、混合意图识别、澄清和多轮上下文；
- 指标/明细查询、口径和血缘路由；
- 趋势、对比、占比、异常和确定性排名；
- 查询结果数据集登记、Redis引用和MinIO/Parquet存储；
- 历史数据集筛选、排序、TopN、聚合和派生列；
- 多问题DAG、并行/顺序执行、相同查询去重、部分失败隔离和断点恢复；
- 跨DAG分支数据集的受控Inner Join；
- 幂等、冲突检测、证据、可靠性、ChartSpec及Qwen受约束总结。
- CSV/XLSX导入、Excel多Sheet问数，以及用户明确要求时在对话内生成XLSX/DOCX/PDF并写入MinIO；
- 与通用智能体一致的`/agent_chat`、`/agent_chat/stream`请求契约，以及按请求传入Skill、HTTP Tool和MCP配置的调用能力。

#### 部分实现

- 归因、预测、数据质量、文字报表、长期记忆、知识增强；
- 跨数据集Join目前要求唯一公共键或用户明确指定键，并拒绝多对多自动关联；
- 排名是通用操作符，不是独立主意图。

#### 尚未形成闭环

- 数据库与Excel跨来源自动比较（已有受控Join能力，但必须具备明确、唯一且类型兼容的关联键）；
- 查询取消和完整异步任务中心；
- 独立算法服务/受控Python沙箱；
- 生产Policy权限、脱敏和审计平台；
- 生产容量、稳定性和灾备验收。

### 六、最终判断

| 判断项 | 最新结论 |
|---|---|
| 基本代码框架 | 已完成 |
| 单问题查询与分析编排 | 已完成 |
| 多问题DAG基本与增强能力 | 已完成 |
| 真实SQL和数据集存储 | 已闭环 |
| 历史数据集追问 | 已修复并闭环 |
| 最终指标答案语义校验 | 已闭环，使用 Oagnet ASL 内的规范指标编码、语义模型ID和业务域ID |
| 初版受控演示 | 可以，不需要为指标口径额外绑定文档知识库 |
| Java后端正式联调 | 接口已具备，确认 UTF-8、语义模型ID和业务域ID契约后即可开始 |
| 生产发布 | 尚不具备条件 |

---

以下内容为此前阶段测试记录，若与上述最新复测不一致，以上述最新复测为准。

> 测试日期：2026-08-25  
> 测试范围：DataAnalysis Agent、Oagnet、SQL Translator、Redis、MinIO、语义层和测试业务数据库  
> 本文区分“代码已经实现”和“当前部署环境已经可用”，不能用代码完成度代替联调结论。

## 1. 总结结论

当前智能体的主体框架已经完成，自动化测试稳定，数据库、Oagnet 和重启后的 SQL Translator 已经能够形成真实查询闭环。当前可以进行智能体内部联调和受控初版演示；但由于测试数据覆盖不足、知识库范围尚未在直接测试请求中配置，仍不能直接宣称所有分析场景都已验收，更不能进入生产发布。

当前 SQL 查询链路已经没有代码级阻塞，剩余的是测试数据和外部业务配置条件：

1. 历史测试数据已经补齐；趋势、异常、归因和预测具备连续月份输入。算法效果仍需按各意图重新验收。
2. 直接调用智能体进行指标结果验收时，需要人工在请求中填写一组真实 `knowledge_base_names`，否则会触发 `KNOWLEDGE_SCOPE_MISSING`。后端当前不调用智能体不是阻塞；等平台正式接入时，再由后端根据应用绑定关系自动传入。
3. 文档知识库接口当前要求认证。实测列表接口和检索接口均返回 HTTP 401；因此还需要平台提供可用认证方式或在内网为智能体开放服务身份。
4. 指标的静态 `bind_entity` 不再作为主实体选择的唯一依据。Oagnet 已改为根据本轮语义模型、业务域和时间锚点物理表实时解析当前有效实体，支持用户动态添加、删除和替换实体。

知识库认证和有效库名具备后，即可继续第一阶段完整演示验收。后端可以晚于智能体内部验收接入。

## 2. 本次真实测试结果

### 2.1 服务连通性

| 服务 | 地址 | 结果 |
|---|---|---|
| DataAnalysis Agent | `http://127.0.0.1:8088/docs` | HTTP 200，在线 |
| Oagnet | `http://127.0.0.1:8021/docs` | HTTP 200，在线 |
| 语义层 SQL Translator | `http://192.168.1.49:48000/api/health` | HTTP 200，修复版已生效 |
| Redis | 由运行服务实际读取 | 可用 |
| MinIO | 运行时能力清单显示已启用 | 可用，仍需压测和运维验收 |
| 测试业务数据库 | `sql_trns_test` | 查询成功 |

### 2.2 自动化测试

执行完整测试集：

```text
445 passed, 1 warning
耗时 17.54 秒
```

唯一警告是测试依赖的 Starlette/httpx 弃用提示，不影响当前业务功能，但后续升级依赖时需要处理。

SQL Translator 新增回归测试：

```text
3 passed
```

覆盖以下内容：

- Redis 新版扁平维度映射结构；
- 月粒度 SELECT/GROUP BY 表达式一致性；
- 语义模型级缓存刷新。

### 2.3 修复版 SQL Translator 真实执行结果

修复后的本地源码已经实际连接 Redis 和测试数据库，成功生成并执行：

```sql
SELECT
    DATE_FORMAT(order_info.pay_time, '%Y-%m') AS `统计日期`,
    SUM(order_info.pay_amount) AS `销售额`
FROM order_info
WHERE order_info.pay_time >= '2026-01-01'
  AND order_info.pay_time < '2026-08-01'
GROUP BY DATE_FORMAT(order_info.pay_time, '%Y-%m')
ORDER BY `统计日期` ASC;
```

真实返回：

```json
{
  "统计日期": "2026-07",
  "销售额": 46259.0
}
```

说明数据库、指标公式、时间字段和数据源本身均可正常工作。

### 2.4 DataAnalysis Agent 真实接口测试

#### 指标查询

问题：`查询2026年7月销售额`

- 意图识别：`METRIC_QUERY`，正确；
- 意图置信度：`0.95`；
- SQL 查询阶段能够产生数据集引用；
- 最终状态：`SAFE_FALLBACK`；
- 原因：请求没有传应用绑定的知识库名称，指标知识校验返回 `KNOWLEDGE_SCOPE_MISSING`；
- 评价：这是安全机制正常工作，不是数据库查询失败。

#### 趋势分析（48000 重启后复测）

问题：`分析2026年1月至7月销售额趋势`

- 意图识别：`TREND_ANALYSIS`，正确；
- 粒度识别：月，Oagnet 修复后正确；
- 日期维度：`statistical_date`；
- 绑定属性：订单支付时间 `pay_time`；
- SQL Translator：成功；
- 生成 SQL：正确使用 `DATE_FORMAT(order_info.pay_time, '%Y-%m')`；
- 数据库：成功返回 2026-07 销售额 46259；
- 最终状态：`SAFE_FALLBACK`；
- 原因：时间范围内只返回 1 个有效月份，达不到趋势分析最低样本要求；
- 评价：查询链路已经正常，安全终止属于数据充足性门禁正常工作；补充多月份数据后再验收趋势算法。

### 2.5 后端暂不调用时的测试方式

现阶段不要求 Java 后端调用智能体。可以直接在 Swagger 或测试脚本中调用 `/agent_chat`：

- Header 中使用固定测试租户、测试用户、测试角色和测试应用；
- Body 中手工传入 `semantic_model_id=6`、`business_domain_id=7`；
- 有实际知识库时手工传入 `knowledge_base_names`；
- 没有知识库名称时，可以测试意图、改写、澄清、ASL、SQL、数据集和安全兜底，但不能把指标最终回答的安全终止判断为程序故障；
- 数据侧目前只需在语义数据或实体属性数据更新时调用对应同步接口，不需要调用智能体聊天接口。

平台正式接入后，Java 后端再负责自动补齐租户、用户、应用、语义模型、业务域和知识库绑定信息。

### 2.6 历史测试数据补充结果

已向 `sql_trns_test` 写入可重复清理的测试数据：

- 新增订单：1602 条；
- 新增订单商品明细：1602 条；
- 新增支付记录：1602 条；
- 时间范围：2025-01 至 2026-06；
- 结合原有 2026-07 数据，共形成连续 19 个月；
- 数据覆盖多个客户、店铺、商品、渠道和支付方式；
- 2026-04 设置了确定性的业务低谷，可用于异常检测和维度贡献测试；
- 所有新增主键以 `DAH_` 开头，不覆盖原有业务演示数据；
- 可使用 `scripts/seed_analysis_history.py` 幂等重建。

补数后 SQL 查询已返回19个月数据，原来的“只有一行，无法趋势分析”问题已经解决。当前整链路继续被知识库范围和认证门禁拦截。

### 2.7 知识库是什么、位于哪里

这里的“知识库”不是业务数据库，也不是语义层 MySQL 表。

- 服务项目：`E:\YouoAgent\information_safety_test`；
- 服务地址：`http://192.168.1.49:7873`；
- 检索接口：`POST /knowledge_base/search_docs`；
- 底层内容：用户或管理员上传的制度、指标口径、业务说明、分析规则等文档，经切片和向量化后存入文档向量库；
- 调用参数：`knowledge_base_names` 指定本次允许检索哪些知识库；
- 作用一：核对“销售额、增长率、退款率”等指标是否真实存在以及口径是否一致；
- 作用二：为归因、趋势和异常分析补充业务背景，如促销活动、节假日、政策调整和已知事件；
- 作用三：给最终回答提供可追溯的文档证据，避免大模型凭空解释原因。

语义层负责告诉系统“字段在哪里、指标怎么算”；文档知识库负责告诉系统“指标是什么意思、业务上如何解释”。二者不能互相替代。

当前无法直接确定具体应该使用哪个库名，因为知识库列表接口返回 HTTP 401。需要知识库服务维护人员提供：

1. DataAnalysis Agent 的服务认证方式；
2. 应用对应的知识库名称列表；
3. 至少包含销售额指标口径的测试知识库；
4. 该测试库中至少一份可命中的指标口径文档。

## 3. 当前已经实现且可以使用的能力

以下能力在代码中已经完成，并通过自动化测试。标记“依赖外部配置”的能力必须满足对应条件后才可真实使用。

### 3.1 问题理解和流程控制

- 上下文问题改写；
- 实体别名、属性值和错漏规范化；
- 规则与 Qwen 结构化模型结合的意图识别；
- 时间范围、指标、维度等参数抽取；
- 信息完整性检查；
- 缺少关键信息时进行澄清追问；
- 一轮最多返回 5 个追问问题；
- 分析过程节点输出；
- 幂等请求与重复消息保护；
- 失败重试、安全降级和统一兜底话术；
- 置信度、证据和可靠性门禁。

### 3.2 已实现的意图类型

- 指标查询 `METRIC_QUERY`；
- 明细查询 `DETAIL_QUERY`；
- 趋势分析 `TREND_ANALYSIS`；
- 对比分析 `COMPARISON_ANALYSIS`；
- 占比/构成分析 `COMPOSITION_ANALYSIS`；
- 异常分析 `ANOMALY_ANALYSIS`；
- 归因分析 `ROOT_CAUSE_ANALYSIS`；
- 预测分析 `FORECAST_ANALYSIS`；
- 报表生成 `REPORT_GENERATION`；
- 指标口径解释 `METRIC_DEFINITION`；
- 数据血缘 `DATA_LINEAGE`；
- 数据质量分析 `DATA_QUALITY`；
- 闲聊和不支持问题的安全回复。

### 3.3 数据查询链路

- 自然语言转 ASL；
- ASL 转 SQL；
- SQL 只读查询执行；
- 语义模型和业务域隔离；
- 指标、维度、实体和物理字段映射；
- 时间范围过滤；
- 按日、月、季度、年分组；
- 排序、TopN、过滤和聚合；
- 查询结果保存为数据集；
- 返回 `dataset_id` 关联每轮结果。
- 根据当前语义元数据动态解析查询主实体，实体增删后无需修改固定代码。

使用前提：语义模型配置完整。修复版 SQL Translator 已部署到 48000，并已通过真实查询验证。

### 3.4 确定性数据分析

以下分析以算法计算结果为依据，大模型只负责整理表达，不参与数值决策：

- 趋势方向和变化率；
- 环比、同比和分组对比；
- 占比与贡献度；
- 异常点检测；
- 基于维度贡献的候选归因；
- 数据质量中的空值和重复值检查；
- 分析结果一致性校验；
- Qwen 基于算法证据生成通俗总结，失败时回退为确定性模板。

### 3.5 长短期记忆

- Redis 保存短期会话状态；
- 通过 `conversation_id` 关联同一轮会话；
- 通过 `message_id` 保证消息幂等；
- 保存澄清状态、已理解参数和最近数据集引用；
- 支持 Java 后端传入 `history`；
- 支持长期记忆候选、确认、查询和删除；
- 大数据本身不写入 Redis，只保存数据集引用。

### 3.6 MinIO 数据集追问

- SQL 结果保存为数据集；
- Redis 保存最近数据集引用；
- 基于历史数据集进行筛选、排序、TopN、选择列和聚合；
- 支持安全派生列计算；
- 派生数据集保留来源血缘；
- 支持数据集 TTL 和过期清理；
- 中等规模数据支持 Parquet；
- 复杂追问无法安全复用历史数据时，回退为重新生成 ASL/SQL 查询。

## 4. 已实现但当前仍属于部分可用的能力

### 4.1 归因分析

已经完成：

- 维度贡献计算；
- 变化贡献排序；
- 候选原因和证据输出；
- Qwen 基于计算证据总结。

限制：

- SQL 必须返回能够解释变化的维度数据；
- 当前输出的是“数据支持的候选原因”，不能宣称严格因果关系；
- 尚未接入实验、事件、价格、活动等外部原因数据时，无法解释数据之外的真实业务原因。

### 4.2 预测分析

已经完成：

- 预测前数据充足性检查；
- 时间序列连续性、样本量和缺失值检查；
- 多候选模型；
- 滚动回测；
- 根据误差选择算法；
- 预测区间和可靠性输出；
- 数据不足时明确告诉用户需要补充什么。

限制：

- 当前为本地确定性算法；
- 尚未接入独立算法服务或受控 Python 沙箱；
- 尚未覆盖复杂多变量预测、节假日、促销活动和外部经济变量。

### 4.3 报表生成

已经完成文字摘要、结构化结果整理、确定性数据画像和通用 `ChartSpec` 输出。
前端可以将 `ChartSpec` 映射为 ECharts 或 AntV 图表；单图最多携带200个数据点，超出时明确标记截断。

尚未完成：

- Word/PDF/Excel 报表文件生成；
- 完整报表模板；
- Word/PDF中的图表嵌入；
- 定时报表和消息推送。

### 4.4 数据质量分析

当前覆盖查询结果中的空值、重复值、字段类型推断、唯一值比例和数值分布，且数据画像不返回样例原值。尚未形成完整的数据质量规则平台。

### 4.5 排名查询

ASL/SQL 已能表达排序和 TopN，但还没有作为独立主意图和独立 Skill 完整封装。

### 4.6 权限

当前代码保留了租户、用户、角色、应用等调用上下文，也预留了权限适配器；但独立 Policy 服务、行级权限、列级权限和脱敏规则尚未完成生产验收。现阶段只能在受控测试数据上使用。

## 5. 尚未开始或尚未形成可用闭环的功能

以下能力当前运行时明确标记为 `PLANNED / NOT_IMPLEMENTED`：

1. 跨多个历史数据集的自动关联分析；
2. 用户上传 Excel 后直接问数；
3. 多 Sheet Excel 的结构识别、关系确认和联合分析；
4. 数据库结果与 Excel 数据的跨来源对比；
5. 图表图片文件生成和 Word/PDF 图表嵌入；
6. 完整 Word/PDF/Excel 报告导出；
7. 独立算法服务和通用受控 Python 沙箱；
8. 真正的因果推断和实验分析；
9. 生产级独立 Policy 权限服务；
10. 查询取消接口、完整异步任务管理和大查询进度反馈；
11. 跨数据源联邦查询；
12. 完整的高并发、故障注入和灾备演练。

## 6. 当前 Skill 完成情况

### 已实现

- `metric_query`
- `detail_query`
- `metadata_definition`
- `data_lineage`
- `trend_analysis`
- `comparison_analysis`
- `composition_analysis`
- `anomaly_analysis`
- `chart_generation`
- `multi_question_analysis`
- `insight_generation`

### 部分实现

- `ranking_query`
- `root_cause_analysis`
- `forecast_analysis`
- `data_quality_analysis`
- `report_generation`
- `dataset_follow_up`

### 尚未实现

- `cross_dataset_analysis`
- `excel_query`
- `excel_multi_sheet_analysis`
- `database_excel_compare`

说明：这些 Skill 是意图已经明确后执行查询或分析的业务流程封装；意图识别、问题改写、澄清追问和记忆属于智能体公共节点，不应封装为业务 Skill。

## 7. 后端未来正式接入时需要提供的内容

当前阶段后端不需要调用智能体。未来平台正式接入、Java 后端调用 `/agent_chat` 时，至少需要提供：

- Header：`x-tenant-id`；
- Header：`x-user-id`；
- Header：`x-roles`；
- Header：`x-application-id`；
- Body：`conversation_id`；
- Body：`message_id`；
- Body：`question`；
- Body：`application_id`；
- Body：`semantic_model_id`；
- Body：`business_domain_id`；
- Body：应用实际绑定的 `knowledge_base_names`；
- 可选 Body：`history`；
- 可选 Body：`use_longterm_memory`。

其中 `semantic_model_id + business_domain_id` 决定使用哪套业务语义；`knowledge_base_names` 决定指标口径使用哪个知识库进行验证；`conversation_id` 决定上下文属于哪次会话。

## 8. 初版完整演示前必须完成的操作

1. 历史测试数据已补齐，无需后端处理；
2. 获取知识库服务认证方式；
3. 获取一个包含销售额指标口径的有效知识库名称，在直接测试请求中手工传入；
4. 使用模型 6、业务域 7 回归测试指标查询、趋势分析、对比分析、异常分析、归因分析和预测分析；
5. 检查 MinIO 中生成的数据集及 Redis 数据集引用；
6. 检查相同 `conversation_id` 下的追问；
7. 使用不同 `message_id` 测试幂等；
8. 使用缺少指标、时间范围或分析维度的问题测试澄清；
9. 完成一轮并发、超时、Redis短暂不可用和MinIO短暂不可用测试；
10. 后端接入可以放在上述智能体内部验收之后进行。

## 9. 建议的初版演示范围

建议第一版只演示以下闭环：

1. 查询某月销售额；
2. 按月查看销售额趋势；
3. 对比两个时间段销售额；
4. 按店铺或商品类别查看占比；
5. 检测异常月份；
6. 基于维度贡献解释下降候选原因；
7. 预测前发现数据不足并提示补充项；
8. 基于上一轮数据集继续筛选或排序；
9. 展示分析过程、证据和置信度；
10. 展示信息不完整时的澄清追问和依赖失败时的安全兜底。

不要在初版演示中承诺多 Sheet Excel、跨数据集自动 Join、图表图片/文档嵌入、完整文件报表、严格因果分析或生产级权限。当前可以演示结构化 `ChartSpec`，由前端渲染折线图、柱状图或饼图。

## 10. 最终状态判断

| 判断项 | 当前结论 |
|---|---|
| 代码框架是否完整 | 主体框架完整 |
| 自动化测试是否通过 | 是，445 项通过 |
| 数据库是否连接 | 是 |
| Redis 是否连接 | 是 |
| MinIO 是否启用 | 是 |
| Oagnet 是否运行 | 是 |
| 语义层是否在线 | 是 |
| 修复版语义层是否已部署 | 是，真实查询已验证 |
| SQL查询链路是否跑通 | 是，销售趋势查询返回真实数据 |
| 历史测试数据是否充足 | 已补充连续19个月，需要继续做算法验收 |
| 动态实体是否支持 | 支持，按当前语义模型、业务域和物理表实时解析 |
| 趋势分析是否形成结论 | 尚未，当前被知识库门禁拦截 |
| 知识库范围是否已传入 | 本次没有；直接测试需手工传入，后端以后再自动传入 |
| 知识库服务是否可直接检索 | 否，当前返回401，需要服务认证 |
| 后端暂不调用是否影响开发测试 | 不影响，可使用Swagger或脚本直调 |
| 是否能做受控初版演示 | 可以演示查询链路和安全门禁；完整分析演示需补数据和知识库范围 |
| 是否可以直接生产发布 | 不可以 |

---

## 2026-08-25 文件问答、Excel问数与报表能力补充验收

> 本节记录当时的文件能力验收结果；当前结论应以本文顶部的“文档使用规则与结论有效性”和关键用例追踪表为准。

### 用户上传Word/PDF后能否问答

已经形成可用闭环，但必须满足两个前置条件：

1. 文件已通过平台原有上传流程完成解析、切块和向量入库；
2. Java后端调用智能体时，把当前应用实际绑定的`knowledge_base_names`传入`/agent_chat`。

智能体不会直接读取任意MinIO原始Word/PDF。它使用应用绑定的知识库范围检索文档，只基于命中的文档片段回答，并返回`KNOWLEDGE_DOCUMENT`证据。未完成向量入库、绑定关系未传入或没有命中内容时，系统返回安全兜底，不编造答案。

真实环境验收结果：使用知识库`KBCSKJYOAGENTJC_012`中已有的Word/PPT文件提问，接口返回`COMPLETED`，证据2条，可靠性`HIGH`。同时兼容知识库服务返回的“显示名称(知识库ID)”格式，并继续拒绝非当前绑定知识库的数据。

### Excel与多Sheet问数

已经实现：

- `POST /v1/data-analysis/datasets/import-spreadsheet`从可信MinIO对象导入CSV/XLSX；
- 自动识别工作簿中的多个Sheet；
- 自动识别标题行之后的真实表头；
- 合并后的数据增加`_sheet_name`，保留每行来源；
- 返回不可变`dataset_id`，后续`/chat`可直接基于该数据集筛选、排序、聚合和分析；
- 用户问题明确提到某个Sheet名称时，会自动限定该Sheet；
- Redis只保存数据集引用，完整数据存放在MinIO，不把大数据塞入会话。

当前边界：多个Sheet如果结构不同可以分别问数；需要跨Sheet自动Join时，必须先明确唯一键、字段类型和基数关系。系统不会猜测关联键，避免产生错误结果。

### Word/PDF/Excel报表导出

报表不再提供单独的导出接口。用户在`POST /agent_chat`的问题中明确要求生成报表时，由对话流程直接生成，支持：

- `xlsx`：导出完整结构化数据；
- `docx`：生成Word分析报告；
- `pdf`：生成PDF分析报告。

生成文件保存到MinIO，下载信息通过聊天响应的`files`字段返回。DOCX/PDF默认展示前200行，完整明细应使用XLSX，防止大结果导致报告内存和排版失控。

这里的“报表导出”是从智能体数据集生成文件；它不同于“对用户上传的Word/PDF做问答”，后者走知识库检索链路。

### 本轮测试结果

```text
自动化回归：589 passed, 1 warning
真实知识库文档问答：COMPLETED
文档证据数：2
可靠性：HIGH
Excel/多Sheet真实MinIO导入：成功
XLSX/DOCX/PDF真实导出：全部成功
当前服务：http://127.0.0.1:8088
当前进程PID：54836
```

唯一warning是Starlette测试客户端的弃用提示，不是业务失败。

### 仍未包含的能力

- 无关联键定义时的跨Sheet自动Join；
- 扫描版PDF的识别质量保证（取决于平台OCR和知识库入库质量）；
- 已有知识库中个别历史切片存在乱码时，智能体只能识别并安全降级，无法恢复入库前已经丢失的字符；需要知识库侧按UTF-8重新解析并入库；
- 定时报表、邮件或企业微信主动推送；
- 超大Word/PDF全量明细排版（应导出XLSX或拆分报告）；
- 生产级并发压测、灾备演练和正式发布验收。

### 2026-08-26 业务域自动路由改造

- `semantic_model_id`仍是数据查询的必要参数。
- `business_domain_id`已变为可选旧字段，不传时不再报`SEMANTIC_CONTEXT_MISSING`。
- 新增`business_domain_ids`数组；旧单值会自动转换为单元素数组，两个字段冲突时返回422。
- 没有指定业务域时，DataAnalysis Agent向Oagnet传递`business_domain_id=null`，使用Oagnet已经具备的语义模型全域检索，由其根据问题选择相关业务域并生成ASL。
- 单个显式业务域继续作为严格检索范围传给Oagnet；多个显式ID会进入语义模型全域路由，并在响应中完整回显请求范围。跨域SQL是否成立仍以语义层关系配置为准。
- 所有响应（包括澄清和兜底）新增`semantic_model_id`、`requested_business_domain_ids`和`business_domain_selection_mode`，便于后端确认路由方式。
- 自动化回归结果：`540 passed, 1 warning`；真实8088接口验证未传业务域时返回`business_domain_selection_mode=AUTO`且正确回显`semantic_model_id=8`。
