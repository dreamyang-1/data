# 真实问数能力专项评测（2026-08-26）

## 结论

当前版本适合单指标、明确时间、简单分组/TopN 查询；复杂复合问句和否定表达尚不适合按完整需求直接交付。

- 全量自动化回归：652 passed，1 warning，26.66s。
- 既有 200 条规则意图集：Accuracy 87.50%，Macro F1 89.33%，P50/P95 0.1ms。
- 正式 8088 服务真实链路抽样：10 个高频/对抗场景中 4 个完整达到预期，抽样闭环准确率 40%。该样本较小，只用于暴露缺陷，不能当生产总体准确率。
- 10 条并发请求观测延迟：P50 约 5.66s，P95/最大约 17.09s；澄清类最快约 0.12~0.16s，模型/查询链路约 2.45~17.09s。

## 真实链路结果

| 场景 | 结果 | 耗时 | 判定 |
|---|---|---:|---|
| 2026年7月销售额 | 返回 37274.0 | 4.96s | 通过 |
| “销受额”错字 | 能查到 37274.0，但回显仍为“销受额” | 7.98s | 通过（展示有瑕疵） |
| 只说“销售额” | 正确追问时间范围 | 2.45s | 通过 |
| 销售额+订单量，再对比6月并算环比 | SAFE_FALLBACK | 7.85s | 失败 |
| 按区域拆分，再找极值并计算差额 | 只返回8行分组，漏掉极值和差额 | 5.67s | 失败 |
| 华东销售额怎么样 | DEPENDENCY_UNAVAILABLE | 5.65s | 未闭环 |
| 查销售额，不要订单明细 | 错判 DETAIL_QUERY，并追问明细字段 | 0.16s | 失败 |
| 三指标跨月比较并找最大变化 | DEPENDENCY_UNAVAILABLE | 7.50s | 未闭环 |
| 上月各区销额 top5 | 正确返回Top5 | 17.09s | 通过 |
| 销售额+退款率口径+导出明细 | 未拆任务，只追问明细字段 | 0.12s | 失败 |

## 主要问题

### P0：多问题/复合运算会漏执行

任务拆分候选器要求至少命中两次有限的动作词。`找出最高和最低，再算差额` 中“算”不在候选动作词中；`销售额是多少，另外退款率口径，再导出明细` 中“导出”也未被候选器完整覆盖。因此整句会被压成一个意图，后续只完成其中一部分。

### P0：否定范围识别错误

`不要订单明细` 仍因“明细”关键词命中 DETAIL_QUERY，说明主分类链路的否定保护没有覆盖该真实表述。系统随后追问字段，方向与用户要求相反。

### P0：多指标比较的数据形状不稳定

销售额与订单量跨月比较返回 SAFE_FALLBACK；三指标融合直接出现上游不可用。当前比较分析门禁要求固定的两行结果，但复合指标/复合维度返回形状不能稳定满足该假设。

### P1：问题拆解只按标点和少量连接词，不能表达计算依赖

现有规则只识别“基于上一步/从中/其中”等依赖表达，不能把“按区域拆分→找极值→算差额”构造成依赖 DAG。即使第一步数据正确，后两步仍被静默遗漏。

### P1：规则分类过度回落到 METRIC_QUERY

200 条固定集有 25 条误判；METRIC_QUERY 的 recall 为 100%，precision 仅 53.19%。趋势、对比、归因、预测、口径、血缘、越界请求等弱表述大量被吞为普通指标查询。

### P1：错别字“能执行”不等于“已规范化”

“销受额”得到了销售额数值，但答案标签仍回显错字，可靠性为 LIMITED。需要分别统计实体归一化准确率、查询正确率和答案展示正确率。

### P1：端到端性能波动较大

规则澄清约百毫秒，普通真实查询约5秒，口语 TopN 达17秒。当前仅有10路抽样，不能给出容量结论；发布前应固定数据集，分别做并发1/5/10/20的P50/P95/P99与错误率测试。

## 评测口径建议

后续不要再用单一“意图准确率”代表问数准确率。至少分开统计：

1. 意图准确率与 Macro F1；
2. 槽位准确率（指标、时间、维度、过滤、字段、排序、TopN）；
3. 追问正确率与多轮状态继承率；
4. 任务拆解完整率、依赖边准确率、子任务漏执行率；
5. SQL/ASL 结构正确率和安全拒绝召回率；
6. 最终数值与人工 SQL 复算一致率；
7. P50/P95/P99、SAFE_FALLBACK率和依赖失败率。

## 复现命令

```powershell
C:\Users\lenovo\miniconda3\python.exe -m pytest -q
C:\Users\lenovo\miniconda3\python.exe -B evals\run_intent_eval.py --mode rule --dataset intent_realistic_100.json intent_realistic_extra_100.json
```

真实链路使用 `POST http://127.0.0.1:8088/agent_chat`，固定 `semantic_model_id=8`，10条请求以独立会话并发发出。SQL服务 `192.168.1.49:48000/api/health` 正常；8088 `/ready` 为 READY，但 `entity_rewrite_contract=false`、`semantic_metadata_contract=false`，因此部分能力处于降级状态。
