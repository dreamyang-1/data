# 数据智能体调用规则讲解

> 适用对象：Java 后端、前端、运维和联调人员  
> 文档依据：当前 `DataAnalysis_Agent/app/api.py`、`app/main.py`、`app/domain/models.py` 代码  
> 更新时间：2026-08-26

## 联调前先读：调用超时与错误处理

- 同步 `/agent_chat` 的服务端总处理截止时间当前为 120 秒；ASL 生成单阶段超时为 90 秒。网关和 Java HTTP 客户端的读取超时建议设置为至少 130 秒。
- 面向用户实时展示时优先调用 `/agent_chat/stream`。长任务期间会持续返回 `progress` 和 `heartbeat`，后端不要因为暂时没有 `answer_chunk` 就关闭连接。
- `SAFE_FALLBACK`、`NEEDS_CLARIFICATION` 都是合法业务结果，HTTP 状态仍可能为 200，必须读取响应体中的 `status`。
- `ASL_TIME_ANCHOR_INVALID`、`ASL_METRIC_SELECTION_INVALID`、`ASL_DIMENSION_INVALID`、`ASL_SCOPE_INVALID` 表示语义配置问题，不应立即自动重试；应记录语义模型 ID、业务域 ID、请求 ID 和错误码并通知语义层维护人员。
- HTTP 504 才表示本次处理超过总截止时间。Java 后端可以使用完全相同的请求体和 `message_id` 做有限次数重试；服务端会按幂等规则避免重复执行。
- 每条新的用户消息必须生成新的 `message_id`。旧 `message_id` 携带不同请求内容会返回 HTTP 409。

## 1. 数据智能体能做什么

数据智能体接收用户的自然语言问题，完成问题改写、意图识别、信息澄清、语义模型选择、ASL/SQL 查询、数据分析、结果校验和答案生成。

当前主要支持：

- 指标查询和明细查询；
- 趋势、对比、占比、异常、归因和预测分析；
- 指标口径、数据血缘和数据质量查询；
- 多问题拆分、任务 DAG 执行和结果汇总；
- 基于历史 SQL 结果数据集继续追问；
- 基于 MinIO 中的 CSV、Excel 和多 Sheet Excel 问数；
- 按需生成 XLSX、DOCX、PDF 文件；
- 调用平台传入的 Skill、HTTP Tool 和 MCP Tool；
- 短期记忆和可选长期记忆；
- 返回置信度、证据、分析过程、图表和文件结果。

## 2. 当前接口清单

| 方法 | 路径 | 功能 | 主要调用方 |
|---|---|---|---|
| POST | `/agent_chat` | 同步执行一次数据问答或分析 | Java 后端 |
| POST | `/agent_chat/stream` | 实时返回执行进度、答案分块和最终结果 | Java 后端、前端网关 |
| POST | `/v1/data-analysis/datasets/import-spreadsheet` | 将 MinIO 中的 CSV/XLSX 注册为可追问数据集 | Java 后端 |
| GET | `/live` | 判断服务进程是否存活 | 运维、Kubernetes |
| GET | `/ready` | 判断核心依赖是否已经就绪 | 运维、Kubernetes |
| GET | `/docs` | Swagger 调试页面 | 开发、联调人员 |
| GET | `/openapi.json` | OpenAPI 接口定义 | Java 后端、代码生成工具 |

## 3. 统一请求头

对话接口和表格导入接口都使用以下请求头：

| Header | 是否必填 | 说明 |
|---|---:|---|
| `Content-Type` | 是 | 固定为 `application/json` |
| `X-Tenant-Id` | 是 | 租户 ID，用于隔离会话、缓存和数据集 |
| `X-User-Id` | 是 | 当前登录用户 ID |
| `X-Application-Id` | 生产环境是 | 应用 ID，必须与 Body 的 `application_id` 一致 |
| `X-Roles` | 否 | 用户角色，多个角色使用英文逗号分隔 |

示例：

```http
Content-Type: application/json
X-Tenant-Id: tenant-001
X-User-Id: user-001
X-Application-Id: app-001
X-Roles: analyst,manager
```

注意：

- `X-Tenant-Id` 和 `X-User-Id` 应由可信 Java 网关注入，不应直接相信浏览器提交的身份。
- `X-Application-Id` 与 Body 中的 `application_id` 不一致时返回 HTTP 400。
- 当前数据智能体自身不校验 Bearer Token，应部署在可信内网，并由平台网关负责入口保护。

## 4. 同步对话接口

### 4.1 接口地址

```http
POST /agent_chat
```

该接口在分析完成后一次性返回完整 JSON，最适合 Java 后端首次联调。

### 4.2 最小请求示例

```json
{
  "application_id": "app-001",
  "conversation_id": "conversation-001",
  "message_id": "message-001",
  "question": "查询最近一周销售额",
  "semantic_model_id": 8
}
```

### 4.3 完整请求示例

```json
{
  "application_id": "app-001",
  "conversation_id": "conversation-001",
  "message_id": "message-001",
  "question": "分析最近六个月销售额趋势并说明异常月份",
  "semantic_model_id": 8,
  "database_id": 5,
  "business_domain_ids": [],
  "knowledge_base_names": [],
  "history": [],
  "use_longterm_memory": false,
  "tools": [],
  "skills": [],
  "mcp": [],
  "temp_file_paths": [],
  "department": "运营部",
  "dataset_id": null,
  "dag_resume_token": null,
  "task_answers": {}
}
```

## 5. 对话请求字段说明

### 5.1 基础字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `application_id` | string | 是 | 当前智能体应用 ID，长度 1～100 |
| `conversation_id` | string | 是 | 会话 ID，同一次连续对话保持不变，长度 1～128 |
| `message_id` | string | 是 | 当前消息唯一 ID，每条新消息必须更换，长度 1～128 |
| `question` | string | 是 | 用户问题，长度 1～4000 |
| `semantic_model_id` | integer/null | 数据问答通常是 | 当前使用的语义模型 ID，必须是正整数 |
| `database_id` | integer/null | 否 | 平台已经注册的数据库 ID；不传时使用语义模型默认数据源 |
| `department` | string | 否 | 用户部门，最长 100 字符 |

### 5.2 业务域字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `business_domain_ids` | integer[] | 否 | 一个或多个业务域 ID，最多 50 个 |
| `business_domain_id` | integer/null | 否 | 旧版兼容字段，新调用不要再使用 |

推荐规则：

- 后端明确知道业务域时，传 `business_domain_ids`，例如 `[7]` 或 `[7, 9]`。
- 后端不知道、问题可能跨域或希望智能体自主选择时，传空数组 `[]` 或不传。
- 智能体会在 `semantic_model_id` 对应的语义模型范围内自动识别相关业务域。
- 新代码不要同时传 `business_domain_id` 和 `business_domain_ids`。

示例：

```json
{
  "semantic_model_id": 8,
  "business_domain_ids": [7, 9]
}
```

### 5.3 知识库字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `knowledge_base_names` | string[] | 否 | 当前应用绑定的真实知识库名称，最多 50 个 |

知识库主要用于用户上传的制度、说明书、业务文档、PDF、Word 等内容问答，不用于验证指标口径。指标定义、公式、时间字段和血缘以 Oagnet/语义层为准。

示例：

```json
{
  "knowledge_base_names": [
    "KB_SALES_POLICY",
    "KB_PRODUCT_MANUAL"
  ]
}
```

如果用户没有绑定知识库，传空数组 `[]`。

### 5.4 历史消息字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `history` | object[] | 否 | Java 后端额外提供的最近历史消息，最多 20 条 |
| `use_longterm_memory` | boolean | 否 | 是否允许使用长期记忆，默认 `false` |

单条历史消息：

```json
{
  "role": "user",
  "content": "上个月销售额是多少？",
  "message_id": "history-message-001",
  "created_at": "2026-08-26T08:00:00+08:00"
}
```

历史消息字段：

| 字段 | 规则 |
|---|---|
| `role` | 只允许 `user` 或 `assistant` |
| `content` | 必填，1～8000 字符 |
| `message_id` | 可选，不得重复，也不能等于当前消息 ID |
| `created_at` | 可选；提供时必须包含时区 |

历史必须从旧到新排列。所有消息要么都提供 `created_at`，要么都不提供，不能混用。

数据智能体自身也会通过 Redis 保存短期会话状态。Java 后端有可靠历史时可以传 `history`；没有时传 `[]`，不要虚构历史。

### 5.5 数据集和文件字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `dataset_id` | string/null | 否 | 历史查询结果或导入表格的数据集 ID |
| `temp_file_paths` | string[] | 否 | 已上传到 MinIO 的临时文件对象路径，最多 10 个 |

使用原则：

- 用户基于上轮查询结果继续筛选、排序、计算或合并时，传上轮响应的 `dataset_id`。
- 用户上传 CSV/XLSX 后，可以先调用表格导入接口获得 `dataset_id`。
- 也可以在对话请求中通过 `temp_file_paths` 直接传一个 CSV/XLSX 对象路径，智能体会自动导入。
- 当前一个原子问题只能直接绑定一个 CSV/XLSX；多个文件应拆分问题或先合并。
- PDF、Word 等文档不要放进 `temp_file_paths` 做表格问数，应先进入知识库，再传 `knowledge_base_names`。
- 后端传的是 MinIO 对象名或相对路径，不是 Windows 本地路径，也不是用户电脑路径。

基于历史数据集追问示例：

```json
{
  "application_id": "app-001",
  "conversation_id": "conversation-001",
  "message_id": "message-002",
  "question": "只保留华东区，再按销售额从高到低排序",
  "semantic_model_id": 8,
  "dataset_id": "ds-example-001"
}
```

### 5.6 Skill 字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `skills` | object[] | 否 | 本轮允许智能体加载的 Skill，最多 30 个 |

单个 Skill：

```json
{
  "code": "data-analysis",
  "slug": "trend-analysis",
  "authority": "运营部,管理层"
}
```

| 子字段 | 说明 |
|---|---|
| `code` | Skill 所属空间或目录标识 |
| `slug` | Skill 唯一标识，对应平台配置的 Skill 内容 |
| `authority` | 平台传入的部门或权限描述 |

调用规则：

- 平台本轮传哪个 Skill，智能体就加载哪个 Skill。
- Skill 内容在平台更新后，后续请求按新的内容加载，不需要在数据智能体中维护固定业务白名单。
- `slug` 禁止包含 `/`、`\`、`.`、`..` 等危险路径内容。
- Skill 是分析操作流程，不负责问题改写、意图识别和澄清追问。

### 5.7 HTTP Tool 字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `tools` | object[] | 否 | 平台为本轮请求提供的 HTTP 工具，最多 30 个 |

示例：

```json
{
  "tools": [
    {
      "url": "http://tool-service/api/run",
      "headers": {
        "X-Token": "token-value"
      },
      "http_method": "post",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dataset_id": {
            "type": "string"
          }
        }
      },
      "name": "calculate_dataset",
      "description": "对指定数据集执行受控计算",
      "type": "tool",
      "timeout": 30
    }
  ]
}
```

| 子字段 | 规则 |
|---|---|
| `url` | 必填，绝对 HTTP/HTTPS 地址 |
| `headers` | 可选，由平台提供的调用头 |
| `http_method` | `get` 或 `post`，默认 `post` |
| `inputSchema` | 可选，工具输入 JSON Schema |
| `name` | 必填，安全英文标识符 |
| `description` | 可选，说明工具用途和适用场景 |
| `type` | `tool`、`flow` 或空字符串 |
| `timeout` | 1～180 秒，默认 15 秒 |

### 5.8 MCP 字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `mcp` | object[] | 否 | 本轮允许连接的 MCP 服务，最多 10 个 |

示例：

```json
{
  "mcp": [
    {
      "mcp_server_url": "http://mcp-service/mcp",
      "connect_type": "streamable_http",
      "headers": {}
    }
  ]
}
```

| 子字段 | 说明 |
|---|---|
| `mcp_server_url` | MCP 服务绝对 HTTP/HTTPS 地址 |
| `connect_type` | 当前数据智能体实际调用必须使用 `streamable_http` |
| `headers` | 可选，MCP 服务调用头 |

请求模型为兼容平台格式仍可识别 `sse`，但当前数据智能体执行器不支持 SSE MCP。后端接入时应明确传 `streamable_http`。

### 5.9 多问题 DAG 恢复字段

| 字段 | 类型 | 是否必填 | 说明 |
|---|---|---:|---|
| `dag_resume_token` | string/null | 恢复时是 | 多任务待补充时，服务端返回的不透明恢复令牌 |
| `task_answers` | object | 恢复时按需 | 按任务 ID 提交补充答案，最多 5 项 |

当一条问题被拆成多个任务，且部分任务缺少信息时，响应会返回：

- `dag_resume_token`；
- `awaiting_task_ids`；
- 各任务需要补充的问题。

下一轮示例：

```json
{
  "application_id": "app-001",
  "conversation_id": "conversation-001",
  "message_id": "message-003",
  "question": "补充刚才缺少的信息",
  "semantic_model_id": 8,
  "dag_resume_token": "上轮返回的完整令牌",
  "task_answers": {
    "task-1": "时间范围是2026年7月",
    "task-2": "对比华东和华南"
  }
}
```

规则：

- `task_answers` 的 Key 只能是 `task-1`～`task-5`。
- 有 `task_answers` 时必须同时传 `dag_resume_token`。
- 令牌必须原样回传，后端不得解析、拼接或修改。
- 恢复请求仍然必须使用新的 `message_id`。

## 6. 同步响应说明

### 6.1 完整响应示例

```json
{
  "request_id": "9e1de8a4-1494-4fb3-8cbd-8d7515ad70fa",
  "conversation_id": "conversation-001",
  "status": "COMPLETED",
  "intent": "TREND_ANALYSIS",
  "intent_source": "STRUCTURED_MODEL",
  "intent_confidence": 0.96,
  "answer": "最近六个月销售额整体呈上升趋势，其中7月增幅最大。",
  "clarification_questions": [],
  "remaining_question_count": 0,
  "missing_slots": [],
  "understood_slots": {
    "metric": "销售额",
    "time_range": "最近六个月"
  },
  "clarification_round": null,
  "evidence": [],
  "reliability": {
    "level": "HIGH",
    "score": 0.95,
    "gates": {
      "semantic_metric_verified": true,
      "query_result_valid": true
    },
    "warnings": []
  },
  "requirements": [],
  "analysis_process": [],
  "chart_specs": [],
  "files": [],
  "skills_used": [
    "trend-analysis"
  ],
  "extension_executions": [],
  "task_plan": null,
  "task_results": [],
  "dag_resume_token": null,
  "awaiting_task_ids": [],
  "dataset_id": "ds-example-001",
  "result_file_url": null,
  "semantic_model_id": 8,
  "database_id": 5,
  "requested_business_domain_ids": [],
  "business_domain_selection_mode": "AUTO",
  "created_at": "2026-08-26T08:30:00Z"
}
```

### 6.2 响应字段说明

| 字段 | 说明 |
|---|---|
| `request_id` | 服务端请求 UUID，排查日志时使用 |
| `conversation_id` | 当前会话 ID |
| `status` | 本轮业务状态 |
| `intent` | 识别出的主要意图 |
| `intent_source` | 意图识别来源 |
| `intent_confidence` | 意图置信度，0～1 |
| `answer` | 返回给用户的答案或兜底说明 |
| `clarification_questions` | 需要用户补充的问题 |
| `remaining_question_count` | 仍未展示的追问数量 |
| `missing_slots` | 当前缺少的信息字段 |
| `understood_slots` | 已经识别和规范化的信息 |
| `clarification_round` | 当前追问轮次 |
| `evidence` | 数据、查询和语义证据 |
| `reliability` | 可靠性等级、分数、门禁和警告 |
| `requirements` | 执行分析或预测需要满足的数据条件 |
| `analysis_process` | 可以向用户展示的分析步骤 |
| `chart_specs` | 前端图表配置及图表数据 |
| `files` | 按需生成的 XLSX、DOCX、PDF 文件 |
| `skills_used` | 本轮实际使用的 Skill |
| `extension_executions` | HTTP Tool/MCP Tool 执行结果 |
| `task_plan` | 多问题拆分后的 DAG 计划 |
| `task_results` | 每个子任务的执行结果 |
| `dag_resume_token` | 多任务待补充时的恢复令牌 |
| `awaiting_task_ids` | 等待用户补充的任务 ID |
| `dataset_id` | 本轮结果数据集 ID，后续追问时使用 |
| `result_file_url` | SQL 服务返回的大结果集 MinIO URL |

### SQL查询结果与文件生成规则

后端不要同时期待 `result_file_url` 和 `files` 一定都有值，两者代表不同来源：

| 场景 | 智能体行为 | 后端读取字段 |
|---|---|---|
| 小结果，用户未要求文件 | 直接返回结构化结果，不创建文件 | `answer`、`evidence`、`dataset_id` |
| 小结果，用户明确要求导出/生成/下载文件 | 智能体从本轮数据集生成文件并上传MinIO | `files[0].download_url` |
| 大结果，SQL服务已经生成完整文件 | 智能体直接使用SQL服务文件，不重复生成 | `result_file_url` |
| 文件生成失败 | 查询结果仍可返回，并在答案中说明文件失败 | 先按 `status` 处理，再检查文件字段 |

支持的用户表达包括“导出Excel”“下载为xlsx”“生成PDF”“导出Word”等，不区分英文后缀大小写。用户没有明确要求文件时，小结果不会浪费存储生成文件。

用户也可以在查询完成后的同一会话中单独发送“把刚才结果导出成Excel”。此时后端保持相同的 `conversation_id`，使用新的 `message_id`，并继续传相同的应用、语义模型和业务域范围。智能体会优先复用当前会话最近的同范围不可变数据集，不会重新执行原SQL。若后端已经持有准确的 `dataset_id`，也可以显式回传以避免“刚才”所指数据不明确。

对于大结果，SQL服务返回的文件是权威完整结果；智能体不会拿少量预览行再次生成文件，否则生成的文件可能缺数据。前端下载按钮应优先按以下顺序选择：

1. `files[0].download_url`：智能体按用户要求生成的文件；
2. `result_file_url`：SQL服务生成的大结果文件；
3. 两者都为空：本轮没有文件，直接展示结构化答案。
| `semantic_model_id` | 本轮使用的语义模型 ID |
| `database_id` | 本轮显式选择的数据库 ID |
| `requested_business_domain_ids` | 调用方明确指定的业务域 |
| `business_domain_selection_mode` | `AUTO` 或 `EXPLICIT` |
| `created_at` | UTC 响应时间 |

### 6.3 业务状态

| `status` | 含义 | 前端处理 |
|---|---|---|
| `COMPLETED` | 成功完成 | 展示答案、图表和文件 |
| `NEEDS_CLARIFICATION` | 用户信息不足 | 展示 `clarification_questions` 并等待补充 |
| `SAFE_FALLBACK` | 无法给出可靠结论 | 展示安全兜底话术和警告 |
| `CANCELLED` | 用户取消或流程取消 | 结束当前任务 |

`NEEDS_CLARIFICATION` 和 `SAFE_FALLBACK` 都是正常业务响应，HTTP 状态仍然是 200。后端不能只看 HTTP 200，必须继续判断 Body 的 `status`。

### 6.4 主要意图值

| 意图 | 含义 |
|---|---|
| `METRIC_QUERY` | 指标查询 |
| `DETAIL_QUERY` | 明细查询 |
| `TREND_ANALYSIS` | 趋势分析 |
| `COMPARISON_ANALYSIS` | 对比分析 |
| `COMPOSITION_ANALYSIS` | 占比或构成分析 |
| `ANOMALY_ANALYSIS` | 异常分析 |
| `ROOT_CAUSE_ANALYSIS` | 归因分析 |
| `FORECAST_ANALYSIS` | 预测分析 |
| `REPORT_GENERATION` | 报表生成 |
| `METRIC_DEFINITION` | 指标口径解释 |
| `DATA_LINEAGE` | 数据血缘 |
| `DATA_QUALITY` | 数据质量 |
| `CAPABILITY_HELP` | 能力和使用帮助 |
| `CHAT` | 闲聊 |
| `OUT_OF_SCOPE` | 超出数据智能体范围 |

## 7. 流式对话接口

### 7.1 接口地址

```http
POST /agent_chat/stream
```

请求头和 Body 与同步 `/agent_chat` 完全相同，响应类型为：

```http
Content-Type: text/event-stream; charset=utf-8
Cache-Control: no-cache
X-Accel-Buffering: no
```

### 7.2 输入示例

Linux、服务器终端或支持 `curl` 的环境可以直接执行：

```bash
curl -N -X POST "http://192.168.1.49:8088/agent_chat/stream" \
  -H "Content-Type: application/json" \
  -H "Accept: text/event-stream" \
  -H "X-Tenant-Id: tenant-001" \
  -H "X-User-Id: user-001" \
  -H "X-Application-Id: app-001" \
  -H "X-Roles: analyst" \
  -d '{
    "application_id": "app-001",
    "conversation_id": "conversation-001",
    "message_id": "message-001",
    "question": "分析最近六个月销售额趋势",
    "semantic_model_id": 8,
    "database_id": null,
    "business_domain_ids": [],
    "knowledge_base_names": [],
    "history": [],
    "use_longterm_memory": false,
    "tools": [],
    "skills": [],
    "mcp": [],
    "temp_file_paths": [],
    "department": "运营部",
    "dataset_id": null,
    "dag_resume_token": null,
    "task_answers": {}
  }'
```

`curl` 必须增加 `-N`，否则客户端可能缓存内容，看起来仍像一次性输出。

### 7.3 平台统一流式格式

当前接口已经与 `New_Agent` 的传输格式对齐：每个SSE事件只有一行 `data: JSON`，不再输出 `event:` 行。事件类型统一读取JSON中的 `type` 字段。

```text
data: {"step":"","type":"updata_state","data":"accepted","message_id":"message-001"}

data: {"step":"intent_recognition","type":"message_chunk","content":"已识别为趋势分析。","role":"assistant","node":"","meta":{"stage":"INTENT_RECOGNITION","status":"COMPLETED"}}

data: {"step":"output","type":"message_chunk","content":"最近六个月销售额整体上升。","role":"assistant","node":"data_analysis","index":0,"is_last":true}

data: {"step":"output","type":"answer","content":"最近六个月销售额整体上升。","status":"COMPLETED"}

data: {"step":"output","type":"complete","content":"","status":"COMPLETED","answer":"最近六个月销售额整体上升。"}
```

### 7.4 事件类型

| `type` | 用途 | 前端处理 |
|---|---|---|
| `updata_state` | 阶段切换、accepted、heartbeat | 更新加载阶段，不追加到最终答案 |
| `message_chunk` | 公开分析进度或最终答案分块 | `step=output`时增量追加答案；其他step展示在分析过程区域 |
| `tool_result` | Skill、HTTP Tool或MCP执行结果 | 展示工具状态；不要直接当最终结论 |
| `answer` | 已校验的完整答案 | 用于校准分块结果，不要重复追加 |
| `complete` | 唯一业务终态 | 读取完整AgentResponse及业务`status` |
| `error` | 流建立后的异常 | 停止加载，根据`retryable`决定是否重试 |

`complete.status`可能为：`COMPLETED`、`NEEDS_CLARIFICATION`、`SAFE_FALLBACK`或`CANCELLED`。因此不能只看到`type=complete`就认为业务查询成功。

### 7.5 数据分析专用扩展字段

为了不丢失数据智能体能力，`complete`在平台通用字段之外继续携带完整的结构化结果，包括：

- `intent`、`intent_confidence`；
- `clarification_questions`、`missing_slots`、`understood_slots`；
- `evidence`、`reliability`、`analysis_process`、`analysis_plan`；
- `chart_specs`、`files`、`dataset_id`、`result_file_url`；
- `task_plan`、`task_results`、`dag_resume_token`；
- `semantic_model_id`、`database_id`、业务域选择结果。

这些是通用Envelope上的业务扩展，不影响通用智能体前端按`type`消费。

### 7.6 前端处理规则

1. 逐个解析以空行分隔的SSE块，每块只读取`data:`后的JSON。
2. 不再读取SSE的`event`字段，也不要等待`event: complete`。
3. `message_chunk.step=output`才追加到最终答案；其他`message_chunk`属于可公开分析过程。
4. 收到`answer`时用其`content`校准已拼接答案，不重复追加。
5. 收到`complete`时结束本轮，并使用其中完整结构化字段更新图表、文件、证据和可靠性。
6. 收到`error`时结束本轮；SSE连接本身通常已经是HTTP 200。
7. 身份、Body格式和可提前发现的`message_id`冲突仍使用HTTP 400、401、409或422，不建立事件流。

完整的10种输出示例见 `数据智能体流式输出完整响应示例.md`。

### 7.7 取消正在运行的查询

不新增取消接口。用户点击“停止”后，Java后端仍调用普通聊天接口，并满足以下规则：

- `application_id`、`conversation_id`、身份Header必须与正在运行的请求一致；
- `message_id`必须重新生成，不能复用原查询的消息ID；
- `question`传“取消”“停止查询”“停止分析”“不用了”或“算了”；
- 同步查询和SSE查询均使用这一规则，不需要等待固定时间间隔。

示例：

```http
POST /agent_chat
X-Tenant-Id: tenant-001
X-User-Id: user-001
Content-Type: application/json
```

```json
{
  "application_id": "app-001",
  "conversation_id": "conversation-001",
  "message_id": "message-cancel-002",
  "question": "停止查询",
  "semantic_model_id": 8,
  "business_domain_ids": []
}
```

有运行中任务时返回：

```json
{
  "status": "CANCELLED",
  "answer": "已停止当前正在执行的查询或分析任务。",
  "reliability": {
    "level": "HIGH",
    "score": 1.0,
    "gates": {
      "running_request_cancelled": true
    },
    "warnings": []
  }
}
```

如果原请求使用SSE，原连接最终收到：

```text
event: cancelled
data: {"status":"CANCELLED","answer":"任务已根据本会话的取消指令停止。"}
```

取消只作用于当前可信身份和当前会话，不能取消其他用户的任务。当前版本可终止智能体任务和对异步上游结果的等待；数据库内部已经开始执行的SQL要实现物理终止，仍需SQL执行服务提供查询句柄及取消接口。

### 7.8 完整重新生成回答

用户点击回答刷新按钮时，应调用刷新专用接口：

```http
POST /agent_chat/refresh/stream
```

同步调用对应 `POST /agent_chat/refresh`。请求体与普通问数接口相同，也可以在普通接口请求体中传任一兼容字段：`regenerate=true`、`refresh=true` 或 `force_regenerate=true`。

刷新执行遵循以下规则：

- 外部 `application_id`、`conversation_id`、`message_id` 可以保持原值；
- 服务内部自动创建隔离的执行会话和消息 ID，不读取原回答幂等缓存；
- 如果 `history` 中包含被刷新的问题及旧答案，服务会从该问题处截断，旧答案不会参与本次生成；
- 意图识别、任务规划、数据查询、校验和答案输出均重新执行；
- 最终响应继续回显调用方原始 `conversation_id`，页面会话关系不变。

## 8. 表格导入接口

### 8.1 接口地址

```http
POST /v1/data-analysis/datasets/import-spreadsheet
```

用途：用户已经把 CSV/XLSX 上传到 MinIO，Java 后端调用该接口，把文件注册为可以问数和连续追问的数据集。

### 8.2 请求示例

```json
{
  "application_id": "app-001",
  "conversation_id": "conversation-001",
  "object_name": "temporary/tenant-001/user-001/sales.xlsx"
}
```

字段说明：

| 字段 | 是否必填 | 说明 |
|---|---:|---|
| `application_id` | 是 | 当前应用 ID |
| `conversation_id` | 是 | 文件所绑定的会话 ID |
| `object_name` | 是 | MinIO 中的 CSV/XLSX 对象名称 |

### 8.3 成功响应

成功时返回 HTTP 201：

```json
{
  "success": true,
  "dataset_id": "ds-example-001",
  "source_object": "temporary/tenant-001/user-001/sales.xlsx",
  "sheets": [
    "销售明细",
    "商品信息"
  ],
  "columns": [
    "sheet_name",
    "订单日期",
    "销售额"
  ],
  "row_count": 1200,
  "expires_at": "2026-08-27T10:00:00+00:00"
}
```

后端应保存 `dataset_id`。用户基于该文件继续提问时，把它放入 `/agent_chat` 的 `dataset_id` 字段。

## 9. 健康检查接口

### 9.1 存活检查

```http
GET /live
```

成功响应：

```json
{
  "status": "UP"
}
```

该接口只表示服务进程还能响应，不代表所有依赖可用。

### 9.2 就绪检查

```http
GET /ready
```

该接口检查：

- Redis 短期记忆；
- MySQL 长期记忆；
- MinIO 数据集存储；
- ASL 翻译和 SQL 执行契约；
- 实体属性改写服务；
- 语义元数据服务；
- 知识库服务。

示例：

```json
{
  "status": "READY",
  "adapter_mode": "http",
  "intent_classifier": "STRUCTURED_MODEL",
  "checks": {
    "redis_short_memory": true,
    "mysql_long_memory": true,
    "minio_dataset_store": true
  },
  "capabilities": {
    "asl_sql_query_contract": true,
    "entity_rewrite_contract": false,
    "semantic_metadata_contract": false,
    "knowledge_service": true
  },
  "readiness_profiles": {
    "core": true,
    "query_pipeline": true,
    "full_feature": false
  },
  "degraded_capabilities": [
    "entity_rewrite_contract",
    "semantic_metadata_contract"
  ]
}
```

字段解释：

- `readiness_profiles.core`：进程、Redis、MySQL、MinIO等核心运行依赖是否就绪；
- `readiness_profiles.query_pipeline`：核心依赖及ASL/SQL查询接口契约是否就绪；
- `readiness_profiles.full_feature`：所有已探测增强能力是否全部就绪；
- `degraded_capabilities`：当前不可用、但允许安全降级的能力列表。

核心依赖没有就绪时返回 HTTP 503，运维不应继续把新流量转发给该实例。HTTP 200只代表指定就绪画像中的核心条件成立，不代表每个具体语义模型的字段配置都正确；业务验收仍须执行一条真实查询。

## 10. HTTP 状态码

| 状态码 | 含义 | 调用方处理方式 |
|---:|---|---|
| 200 | 对话请求形成业务结果 | 继续检查 Body `status` |
| 201 | 表格数据集导入成功 | 保存 `dataset_id` |
| 400 | Header 应用 ID 与 Body 不一致 | 修正参数，不要直接重试 |
| 401 | 缺少可信身份或应用请求头 | 检查 Java 网关注入逻辑 |
| 409 | 相同 `message_id` 被用于不同请求 | 新消息生成新的 ID |
| 422 | 字段、历史、文件或约束不合法 | 根据 `detail` 修正请求 |
| 500 | 服务内部异常 | 记录 `request_id` 和日志并告警 |
| 502 | 文件或上游调用失败 | 有限重试，持续失败时告警 |
| 503 | 服务或核心依赖未就绪 | 暂停转发并等待恢复 |
| 504 | 整体分析超时 | 使用相同请求及相同 `message_id` 幂等重试 |

### 10.1 `message_id` 幂等规则

- 每条新的用户消息都要生成新的 `message_id`。
- 同一次网络超时重试，可以使用相同 `message_id` 和完全相同的请求体。
- 相同 `message_id` 携带不同内容会返回 HTTP 409。
- 不能通过修改问题但继续复用旧 `message_id` 的方式实现“重新提问”。

## 11. Java 后端接入流程

### 11.1 应用配置阶段

Java 后端需要为数据智能体应用保存或获取：

- `application_id`；
- `semantic_model_id`；
- 可选 `database_id`；
- 可选业务域范围；
- 应用绑定的真实知识库名称列表；
- 应用配置的 Skill、HTTP Tool 和 MCP 信息。

### 11.2 用户发送问题

Java 后端需要：

1. 获取当前租户、用户、应用和会话。
2. 为当前新消息生成唯一 `message_id`。
3. 在 Header 中写入租户、用户和应用身份。
4. 在 Body 中写入问题、会话、语义模型及可选能力配置。
5. 调用 `/agent_chat` 或 `/agent_chat/stream`。
6. 根据响应 `status` 决定展示答案、追问还是兜底提示。
7. 保存响应中的 `dataset_id` 和可选 `dag_resume_token`。

### 11.3 用户继续追问

| 追问类型 | 后端需要做什么 |
|---|---|
| 普通上下文追问 | 保持 `conversation_id`，生成新 `message_id` |
| 基于上轮表格计算 | 额外回传上轮 `dataset_id` |
| 回答澄清问题 | 保持会话，生成新消息并提交自然语言答案 |
| 多任务分别补充 | 回传 `dag_resume_token` 和 `task_answers` |
| 用户修改问题 | 当成一条新消息，不能复用旧 `message_id` |

## 12. 推荐联调顺序

1. 访问 `/live`，确认进程存活。
2. 访问 `/ready`，确认核心依赖就绪。
3. 使用 Swagger `/docs` 测试最小 `/agent_chat` 请求。
4. 测试缺少指标或时间范围时的澄清响应。
5. 测试指标查询、趋势分析、归因分析和预测分析。
6. 测试同一 `conversation_id` 的连续追问。
7. 测试 `dataset_id` 历史数据集追问。
8. 测试 CSV、单 Sheet Excel 和多 Sheet Excel 导入问数。
9. 测试多问题拆分及 DAG 恢复。
10. 最后测试 Skill、HTTP Tool、MCP 和 SSE 接口。

## 13. 后端最小可用请求模板

```bash
curl -X POST "http://127.0.0.1:8088/agent_chat" \
  -H "Content-Type: application/json" \
  -H "X-Tenant-Id: tenant-001" \
  -H "X-User-Id: user-001" \
  -H "X-Application-Id: app-001" \
  -d '{
    "application_id": "app-001",
    "conversation_id": "conversation-001",
    "message_id": "message-001",
    "question": "查询最近一周销售额",
    "semantic_model_id": 8,
    "business_domain_ids": [],
    "knowledge_base_names": [],
    "history": [],
    "use_longterm_memory": false,
    "tools": [],
    "skills": [],
    "mcp": [],
    "temp_file_paths": [],
    "task_answers": {}
  }'
```

实际服务地址和端口由运维部署配置决定，后端不要在业务代码中写死本地 `127.0.0.1` 地址。
