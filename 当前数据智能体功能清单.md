# 数据智能体当前功能清单

> 更新时间：2026-08-31  
> 适用代码：`E:\YouoAgent\DataAnalysis_Agent` 当前工作区版本  
> 说明：本文描述当前源码实际具备的能力。历史测试过程与旧结论请查阅 `CURRENT_SYSTEM_TEST_AND_CAPABILITY_REPORT.md`，如有冲突以本文和最新实测结果为准。

## 1. 当前定位

当前系统已经不是单纯的“自然语言转 SQL”服务，而是一个基于 Python、FastAPI、LangGraph、Skill 和 Tool 的通用数据分析智能体，能够完成：

```text
用户问题
→ 上下文恢复与问题补全
→ 意图识别与参数抽取
→ 任务拆分与执行规划
→ 语义查询与只读 SQL
→ 数据质量及证据校验
→ 确定性数据分析
→ 业务洞察解释
→ 答案规划
→ 流式回答、表格或文件交付
```

系统坚持“先取得可信数据，再生成结论”。查询失败、数据质量不合格或证据不足时会追问或安全降级，不会伪造数据。

## 2. 已验证可用的基础能力

### 2.1 对话与意图识别

- 区分闲聊、能力咨询、指标查询、明细查询、趋势分析、对比分析、占比分析、异常分析、归因分析、预测分析、报告生成、元数据说明和取消任务等意图。
- 提取指标、实体、维度、字段、时间范围、筛选条件、排序方向、Top-N、粒度和输出格式。
- 支持常见错别字、漏写、简称、别名和口语化表达的规范化。
- 能识别“不是这个指标”“改成按月”“只看上海”“前五个”等修改、纠正和省略表达。
- 对置信度不足或关键条件缺失的问题进行一次性、针对性澄清，不盲目猜测业务口径。

### 2.2 多轮上下文与问题补全

- 根据当前会话历史恢复上一轮的指标、实体、维度、时间、筛选条件和排序条件。
- 支持追问增量修改，例如“按月”“只看上海”“再加订单数”“展示前20条”。
- 支持基于上一轮结果做排序、筛选、聚合、平均值、差值及导出。
- 能区分延续问题、纠正问题、回答澄清和显式新任务，降低旧上下文污染。
- 保存结构化任务帧、待澄清状态、数据集引用和未完成 DAG，而不是只依赖原始聊天文本。
- 支持较长历史的压缩与结构化记忆；长期记忆是否启用由请求参数和部署配置控制。

### 2.3 文件感知与解析

- 在意图识别阶段判断用户是否上传文件，以及当前问题是否基于文件。
- 文件解析节点位于意图识别之后，展示文件发现、读取、结构识别和可用性检查结果。
- 支持 CSV、XLS 和 XLSX 数据文件的读取与基础分析。
- 检查文件是否存在、格式是否支持、是否成功读取、列名、Sheet、行数及数据质量。
- 文件无法读取、为空、结构歧义或用户指代不清时进行针对性追问。
- 没有文件或问题与文件无关时，继续使用授权业务数据源，不强制进入文件链路。

## 3. 数据查询能力

### 3.1 智能语义查询器

当前已将以下步骤封装为一个统一的“智能语义查询器”：

```text
自然语言问题
→ 指标与维度绑定
→ 结构化 ASL 生成
→ ASL 契约校验
→ 只读 SQL 翻译
→ SQL 安全校验
```

对外展示为一次工具调用，内部仍保留各步骤的输入、输出、耗时和错误码，便于审计与排障。

### 3.2 支持的查询形态

- 单指标和多指标查询。
- 明细列表、分组汇总、排序、Top-N、去重和分页/截断预览。
- 按日、周、月、季度和年度统计。
- 同比、环比、增长率、占比、平均值、最大值、最小值和差值。
- 多维度组合查询，例如地区 × 产品 × 经销商 × 医院 × 时间。
- 多实体关系查询，例如商品—品牌—厂家—经销商—医院。
- 聚合后过滤、排名和复合运算。
- 大结果集使用 SQL 服务生成的完整结果文件，小结果集直接返回结构化表格。

### 3.3 当前已打通的模型 81 医疗场景

在语义模型 81 配置范围内，当前已实际跑通以下类型：

- 医疗产品按月销售趋势。
- 指定地区、品牌和商品筛选经销商。
- 经销商销售规模排序。
- 经销商合作时长和合作次数。
- 医院订单金额、订单笔数、医院等级和所在城市关联。
- 国药母公司销售额及省市信息。
- 商品适用科室、品牌、厂家、经销商和医院关系查询，前提是相应关系已在语义模型中登记。

## 4. 复杂任务拆分与执行

- 判断问题是否需要拆成多个独立或有依赖关系的分析任务。
- 生成结构化任务 DAG，记录任务类型、输入、依赖、执行状态和结果引用。
- 支持独立任务并行、有依赖任务串行。
- 对相同查询去重并复用数据集，避免重复执行 SQL。
- 支持部分任务失败隔离，能够保留其他成功任务的结果。
- 支持多任务结果汇总、跨结果对比和综合报告。
- 支持待补充任务的续跑令牌和结构化答案回填。
- 支持同会话取消；取消会清理待澄清状态和未完成任务。

## 5. 数据分析能力

### 5.1 趋势和时间序列分析

- 总体上涨、下降、平稳和波动判断。
- 阶段划分，例如连续下降阶段、恢复阶段和高位震荡阶段。
- 拐点、峰值、低点和方向变化识别。
- 当前状态判断，例如“低位修复”“持续走弱”“高位回落”。
- 回撤后的恢复程度计算。
- 异常单期变化识别。
- 内部保留斜率、方向一致性、波动率等算法诊断，但默认不直接展示给用户。

### 5.2 对比、结构和异常分析

- 时间段、地区、产品、品牌、经销商、医院等对象间对比。
- 同比、环比、差值、增长率和排名变化。
- 构成占比、集中度及头部贡献。
- 基于确定性规则的异常点和异常变化检测。
- 对样本不足、时间不连续和结果被截断的情况主动声明限制。

### 5.3 归因分析

- 按候选维度拆解变化贡献。
- 识别主要正向贡献、主要负向拖累和结构性变化。
- 支持多维度归因候选排序和证据保留。
- 支持数据血缘、指标依赖和查询来源说明。
- 当前归因属于观察性贡献分析，不会把相关关系写成严格因果关系。

### 5.4 预测分析

- 检查历史长度、缺失值、时间连续性和预测目标是否满足要求。
- 支持基线模型选择、回测、误差指标和预测区间。
- 数据不足时拒绝输出伪预测，并说明需要补充的历史范围。
- 预测能力已具备代码和自动化测试，仍需要针对每个正式业务模型做真实样本验收。

## 6. 洞察与报告生成

当前回答链路已经升级为：

```text
Raw Result
→ Analysis Engine
→ Structured Analysis Result
→ Insight Interpretation Layer
→ Answer Planner
→ Final Answer
```

### 6.1 结构化分析结果

保存总体趋势、阶段、拐点、当前状态、恢复程度、异常变化、业务优先级和内部诊断，作为可审计的中间结果。

### 6.2 洞察解释层

- 把统计结果转换成业务语言。
- 明确区分 `FACT`、`INTERPRETATION`、`PRIORITY` 和 `LIMITATION`。
- 不把算法内部字段机械输出给用户。
- 不根据时间变化擅自编造促销、政策、渠道等业务原因。

### 6.3 答案规划器

- 决定哪些事实值得展示，而不是输出所有计算指标。
- 优先输出结论、关键事实、业务判断、建议关注点和分析边界。
- 隐藏 `slope`、`direction_consistency`、`coefficient_of_variation`、`max/min` 等内部诊断字段。
- 即使大模型润色服务不可用，也能回退到确定性的业务分析回答。

### 6.4 综合报告

- 支持趋势、覆盖、排名、异常、归因和建议等多个章节。
- 支持多任务结果合并及部分成功说明。
- 明确数据水位、证据、可靠性、限制和待补充信息。
- 支持 Markdown 表格、图表描述和结果文件附件。

## 7. Skill、Tool 和 MCP

- Skill、HTTP Tool 和 MCP 都是可选能力，不会因为请求中传入就强制调用。
- 智能体根据用户意图、工具描述、输入 Schema、权限和当前证据判断是否调用。
- 支持 Skill 激活、工具白名单、参数校验、超时、有限重试、失败降级和调用结果记录。
- 支持工具依赖关系及工具结果进入任务 DAG。
- 外部工具不能绕过只读查询、安全检查和证据门禁。
- 联网搜索接口已预留，但当前业务演示链路暂不把联网能力视为稳定的默认能力。

## 8. 输出、流式协议与文件交付

- 支持同步 `/agent_chat` 和流式 `/agent_chat/stream`。
- 流式标签与 `New_Agent` 对齐，核心类型包括 `thinking`、`tool_result`、`answer`、`message_chunk` 和 `complete`。
- 支持阶段 `stage`、状态 `status`、步骤 `step` 和结构化 `meta`。
- 思考内容可以分片输出，最终答案仍保持完整可拼接，既有标签和字段不变。
- 支持查询结果表格、XLSX、PDF、DOCX 和 SQL 服务生成的大结果文件。
- 只有用户明确要求导出时才生成小结果文件；大结果优先复用上游完整文件。

## 9. 可靠性、安全与可审计性

- SQL 只允许只读查询，拦截 DDL、DML、多语句和危险表达。
- 校验指标、维度、时间字段、筛选条件和 Join 关系。
- 检查空结果、截断结果、重复数据、聚合粒度和数据质量状态。
- 保留请求 ID、规范化问题、ASL、SQL、数据集、证据和结构化分析结果。
- 通过 `snapshot_id + data_as_of + quality_status` 控制可靠性；证据不足时保持 `LIMITED`，不会伪造为 `HIGH`。
- 支持幂等消息、防止相同消息重复执行和不同载荷覆盖。
- 支持安全降级、明确的上游错误码和面向用户的非技术错误说明。

### 9.1 链路追踪与评测

- 每次请求生成内部 Trace 汇总，关联请求、核心语义查询、扩展工具、确定性分析步骤、结果校验和最终回答。
- 记录端到端、工具、分析步骤和校验的真实耗时；没有可靠计时来源的阶段不伪造耗时。
- 可按 Trace 计算完成率、部分成功率、澄清率、工具成功率、澄清恢复率、证据覆盖率及 P50/P95 延迟。
- Trace 只保存受控结构和非敏感元数据，不保存原始大结果、凭证或隐藏推理过程。
- 观测存储异常不会阻断业务请求；当前使用内部 Trace/Session Event Log，尚未绑定 Langfuse。

### 9.2 已校验查询召回（Phase 7）

- 仅保存语法、语义、业务三层校验全部通过，执行成功、结果非空且数据质量为 `PASS` 的查询。
- 按租户、应用、语义模型和业务域隔离，使用指纹去重和数量上限控制。
- 只召回语义结构范例，不保存或召回历史 SQL、ASL、结果行和凭据。
- 当前查询始终重新生成 ASL/SQL，并重新执行只读安全校验与三层校验，禁止直接复制历史 SQL。
- Redis 召回或写入失败只影响优化能力，不会让已成功的业务查询失败。

## 10. 当前部署依赖

本机当前完整链路为：

| 服务 | 地址 | 职责 |
|---|---|---|
| Java 后端 | `192.168.1.27:21000` | 平台请求组装与前端 SSE 转发 |
| 数据智能体 | `192.168.1.27:8088` | LangGraph 编排与分析 |
| Oagnet/ASL | `192.168.1.27:8021` | 语义实体检索和 ASL 生成 |
| SQL Translator | `192.168.1.27:48000` | ASL 翻译、只读 SQL 校验与执行 |

注意：`192.168.1.112:9529` 当前前端的 `config.js` 指向 `192.168.1.32:21000`，不是上述本机 Java 后端。使用该前端不能验证本工作区的最新代码，除非修改前端 API 地址或把当前版本部署到 `.32`。

## 11. 当前边界和未完成项

以下能力不能表述为“所有场景均已稳定”：

- 语义模型没有配置的指标、字段、实体或关系无法安全查询，需要先完善元数据。
- 联系方式、科室、医院等级等字段只有在具体模型登记并存在真实数据时才能返回。
- 联网搜索当前不作为默认稳定链路。
- 预测和复杂多维归因仍需要按具体业务模型进行人工数值验收。
- 文件上传需要平台正确传递 `temp_file_paths`，且对象存储中的文件可访问。
- 上游模型服务仍可能出现 429 或超时；当前已有降级和有限重试，但不能消除上游容量限制。
- 完整测试集当前结果为 `1080 passed, 27 failed`。剩余失败仍集中在历史时间追问策略、身份头契约和部分旧乱码断言，不能宣称全量回归已经清零。
- 正式部署版本必须和当前工作区版本一致；仅本地代码通过不代表 `.32` 或其他服务器已经更新。

## 12. 推荐演示问题

当前建议使用语义模型 81 演示：

1. 分析上海市紫杉醇释放冠脉球囊导管的整体销售趋势。
2. 查询上海地区销售振德医疗品牌医用外科口罩的经销商，并按整体业务规模从高到低排序。
3. 查询累计销售额排名前5的经销商，并显示合作时长和合作次数。
4. 查询每个国药公司（母公司）的含税销售总额，并显示公司名称和所在省市。
5. 统计每家医院承接的订单总金额、订单笔数，并关联医院等级和所在城市。
6. 查询空心纤维血液透析器产品的含税销售总额和订单笔数。
7. 查询最近一年销售过费森尤斯产品的经销商名单。
8. 分析上海地区费森尤斯产品近一年的销售趋势。

演示请求必须携带正确的 `semantic_model_id=81`，并确认前端连接的是部署了当前代码的后端。
