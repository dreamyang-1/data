# 实体抽取影子模式全链路对比报告

测试日期：2026-08-31  
测试集合：`数据智能体60题全链路测试文档.md`（实际 74 题/轮）  
语义模型：81  
接口：`POST /agent_chat`  
并发：2（多轮场景内部保持顺序）

## 1. 总体结果

| 指标 | 关闭实体抽取（off） | 影子模式（shadow） | 差异 |
| --- | ---: | ---: | ---: |
| 问题数 | 74 | 74 | 0 |
| HTTP/基础验收通过 | 74 | 74 | 0 |
| COMPLETED | 51 | 52 | +1 |
| PARTIAL_SUCCESS | 1 | 0 | -1 |
| NEEDS_CLARIFICATION | 11 | 11 | 0 |
| SAFE_FALLBACK | 11 | 11 | 0 |
| 文件 | 7 | 7 | 0 |
| 平均耗时 | 12.155 秒 | 11.419 秒 | -0.736 秒 |
| P95 | 26.319 秒 | 23.275 秒 | -3.044 秒 |

影子模式不会把模型候选加入问题、过滤条件、ASL 或 SQL，因此不应直接提升回答准确率。本次影子模式平均耗时更低是 Oagnet、SQL、并发调度和缓存波动造成，不能解释为 GLiNER 带来了加速。

## 2. 逐题配对一致性

| 配对项 | 一致题数 | 一致率 |
| --- | ---: | ---: |
| HTTP 状态 | 74/74 | 100% |
| 意图 | 74/74 | 100% |
| 业务状态 | 72/74 | 97.30% |
| 缺失槽位 | 72/74 | 97.30% |
| 排除随机下载链接后的答案 | 69/74 | 93.24% |

影子相对关闭模式的逐题耗时差：均值 -0.736 秒，中位数 -0.104 秒；39 题更快、35 题更慢。正负分布接近，说明上游自然波动明显，无法从一次 A/B 运行证明影子模式对总耗时有稳定影响。

两处业务状态波动：

1. “查询单价最高的三个产品，并同时查询销售额最高的三个产品”：`PARTIAL_SUCCESS` 与 `NEEDS_CLARIFICATION` 之间波动。
2. “比较销售额前五名经销商的平均客单价”：`NEEDS_CLARIFICATION` 与 `COMPLETED` 之间波动。

因为影子候选没有进入语义搜索，这两处不是 GLiNER 改写结果；它们反映的是多任务规划/Oagnet 输出的非确定性，影子请求增加的时序差异可能间接改变并发调度。

## 3. 实体候选召回

对 74 个原始问题直接执行相同 GLiNER2.5 抽取：

- 61 题存在至少一个候选，问题覆盖率 82.43%。
- 共返回 114 个候选。
- 标签分布：商品分类 50、商品名称 35、医院 13、经销商 7、地区 6、品牌 3。
- 正向示例：空心纤维血液透析器、费森尤斯、上海地区、上海东松医疗科技股份有限公司、医用外科口罩。
- 明显误报：把“含税销售总额”“订单”“2026年第二季度”等指标、通用词和时间短语识别成商品名称或商品分类。

覆盖率较高不等于准确率高。当前模型适合继续作为候选召回器，但不适合直接形成查询约束。

## 4. 结论

1. 影子模式安全：意图 100% 一致，未直接改变业务查询和答案。
2. 本轮没有证明回答准确率提升；这是 shadow 的预期行为，而不是实现失效。
3. 额外模型调用没有表现出可稳定归因的总耗时恶化，但单次 CPU 推理约 0.7 秒，真实成本仍然存在。
4. 当前候选误报率不可忽略，暂时不能切换到 `assist`。
5. 现有 11 个澄清和 11 个安全降级主要来自时间水位、复杂计算契约、跨结果集运算和多任务语义歧义，实体抽取无法单独解决。

## 5. 下一步升级门槛

在切换 `assist` 前至少完成：

1. 排除已注册指标、时间表达式、排名词、运算词和通用查询词。
2. 商品/品牌/公司候选必须通过语义模型 81 的目录精确或高置信匹配。
3. 用人工标注集计算各标签 precision、recall、F1，不能只看“有候选的问题比例”。
4. 连续多次 A/B 运行，确认状态一致率不低于当前基线，且 P95 增量可接受。
5. `assist` 首次上线仍需保留开关和自动降级，候选检索失败不能影响主问题检索。

## 6. 测试产物

- `evals/results/entity-shadow-20260831.json`
- `evals/results/entity-off-20260831.json`
- `evals/compare_entity_shadow.py`
- `evals/evaluate_entity_candidates.py`
