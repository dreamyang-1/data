# 当前数据智能体架构分析

> 阶段：Harness 化升级 Phase 1（现状扫描与设计）  
> 扫描范围：`app/`、`tests/`、当前 FastAPI/LangGraph/Skill/Tool/Analysis/Session/Memory 实现  
> 约束：本阶段不修改业务运行代码，不替换现有 Python + LangGraph 架构。

## 1. 结论摘要

当前项目已经不是一个简单的“LLM 调函数”原型，而是具备语义查询、只读 SQL、确定性分析、复合任务 DAG、文件数据集、多轮恢复、长短期记忆、证据校验、SSE 输出和失败降级的较完整数据智能体。

当前最主要的问题不是缺少能力，而是这些能力没有形成统一的 Harness 运行面：

1. LangGraph 的 24 个业务节点目前多数是观测占位节点，真实业务在 `skill_dispatch` 一次性进入约 298 KB 的 `orchestrator.py`；图结构和实际执行结构不一致。
2. HTTP、SQL、HTTP Tool、MCP、Skill 各自具有一部分 timeout、retry、安全检查和结果封装，但缺少统一的 `ToolContext / ToolRequest / ToolResult / ToolError` 契约。
3. 已有 `AnalysisPlan`、任务 DAG、并行执行和确定性分析算子，但复杂分析运行逻辑仍嵌在编排器中，尚未形成独立、受控、可测试的 Analysis Runtime。
4. Redis Session 已能保存待追问状态、任务帧、数据集引用、DAG 检查点和幂等结果，但缺少可持久复盘的 append-only Session Event Log。
5. 历史压缩已有“最近消息 + 修正/澄清锚点”策略，但没有统一 Context Builder、token 预算、结构化会话摘要与 `COMPACTION` 事件。
6. 长期记忆的治理、确认、去重和软删除基础较强，不应推翻；需要补强统一召回注入、冲突更新审计和与 Context Builder 的连接。
7. 结果验证已经覆盖 SQL 只读安全、分析数据契约、证据引用和因果约束，但验证逻辑分散，缺少统一 Result Validator 报告。
8. 当前没有发现应用内直接接入 Langfuse；只有标准日志、`node_trace`、`audit_events` 字段和 SSE 过程事件，无法完整追踪一次请求内的计划、工具、耗时、重试和验证关系。

因此，最合适的升级方式是：保留现有业务实现，在其外侧逐步增加薄而统一的 Harness 层，并按阶段把现有逻辑接入，而不是再造第二套 Agent Runtime。

## 2. 当前目录与职责

```text
DataAnalysis_Agent/
├─ app/
│  ├─ adapters/        # 上游语义、ASL、SQL、知识服务 HTTP 适配与安全校验
│  ├─ analysis/        # 分析计划、数据契约、确定性算子、预测、归因、洞察与表达
│  ├─ domain/          # Pydantic 领域模型和对外稳定契约
│  ├─ graph/           # LangGraph State、节点和路由
│  ├─ intent/          # 意图、实体、时间、问题规范化
│  ├─ planning/        # 多问题拆分、任务 DAG、依赖和执行层
│  ├─ services/        # 主编排、Tool 选择、扩展调用、上下文与业务服务
│  ├─ skills/          # 内置能力注册和动态 SKILL.md 加载
│  ├─ stores/          # Session、Redis、长期记忆、数据集存储
│  ├─ tools/           # 知识库、数据库加载等显式工具
│  ├─ api.py           # 同步与 SSE 接口
│  ├─ dependencies.py  # 运行依赖装配
│  ├─ config.py        # Pydantic Settings
│  └─ main.py          # FastAPI 生命周期、健康检查
├─ tests/              # 59 个测试文件，覆盖核心业务与运行边界
├─ evals/              # 场景评测数据与运行入口
└─ scripts/            # 服务、语义模型和回归辅助脚本
```

结构本身合理，不需要大规模迁移目录。Harness 新模块应靠近现有职责放置，避免新增笼统的 `Manager/Factory/Handler` 层。

## 3. 18 项现状扫描

### 3.1 LangGraph Graph 构建方式

`app/graph/workflow.py` 使用 `StateGraph(AgentState)` 构建固定节点链，并通过 `skill_dispatch` 后的条件路由进入澄清、安全终止、直接记忆审计或 SQL 后续阶段。

当前问题：

- `api_access`、`request_validation` 有少量真实逻辑；其余大多数节点仅追加 `node_trace`。
- 真正的理解、规划、查询、分析和回答都在 `DataAnalysisOrchestrator.handle()` 内完成。
- 图编译时未使用 LangGraph checkpointer；恢复依赖自研 `SessionStore`。
- 文档上的 24 节点链与真实调用栈不一致，节点耗时和错误归属不准确。

判断：LangGraph 保留。后续只把新 Runtime 的关键边界逐步变成真实节点或真实 span，不立即拆散整个编排器。

### 3.2 State 定义

`app/graph/state.py` 的 `AgentState` 已覆盖身份、会话上下文、意图、语义候选、标准请求、完整性、任务计划、ASL、查询结果、数据集、知识、分析事实、验证、图表、洞察、可靠性、审计和最终响应。

优点：状态字段覆盖完整，适合增量演进。

缺陷：

- 多数字段当前没有被对应图节点真正写入。
- `audit_events` 只是内存字段，没有持久化协议。
- 缺少贯穿状态的 `trace_id`、当前 `analysis_execution_id` 和结构化 Runtime 结果引用。

### 3.3 Node 定义

共有 24 个主要业务节点以及 `memory_audit`、`final_output`、`safe_fallback` 三个框架节点。命名和目标业务链路清楚，但多数仍处于兼容迁移阶段。

可复用：稳定节点名、条件路由和现有 SSE stage 映射。

需升级：让 Tool Runtime、Analysis Runtime、Validator、Context Build 和 Event Log 形成真实执行边界；不建议一次把 298 KB 编排器机械切成 24 个类。

### 3.4 Tool 注册与调用

当前有三类工具来源：

1. 固定内部能力：语义查询、知识库、数据库加载、文件数据集。
2. 请求携带的 HTTP Tool：由 `ExtensionDispatcher` 发现、筛选和调用。
3. MCP Tool：运行时发现 `tools/list`，再按能力选择调用。

已有能力：JSON Schema 输入约束、候选安全筛选、参数可满足性检查、最大并发、1 MiB 输入限制、输出限幅、HTTP/MCP 超时、部分错误封装、可选智能选择。

缺陷：内部 Adapter、HTTP Tool 和 MCP Tool 的执行元数据与异常模型不统一；`ExtensionExecution` 不包含 execution/trace/session/latency/retry/result metadata；hook、权限守卫和结果验证无法复用。

### 3.5 Skill / Prompt 机制

`app/skills/registry.py` 注册了查询、分析、元数据、数据集、文件和输出能力，并标注成熟度、运行模式和限制。`DynamicSkillLoader` 支持请求级 SKILL.md、路径隔离、大小限制、哈希版本和 last-known-good 缓存。

现有 Skill 边界基本正确：Skill 表示业务招式，Tool 表示执行能力。无需把每个 Skill 变成 Agent。

需升级：Skill 调用应通过统一 Runtime 获得执行上下文、事件、校验和追踪；动态指令仍不得覆盖 SQL 安全、租户范围和证据规则。

### 3.6 SQL / 数据查询模块

`app/adapters/http.py` 已实现语义解析、ASL 生成与修复、依赖约束、SQL 翻译、只读校验、SQL 执行和数据结果验证。核心安全策略包括：

- 只允许单条 `SELECT/WITH`。
- 拒绝危险关键字。
- 强制过滤条件和依赖约束。
- ASL/SQL 服务具有有限重试和退避。
- SQL 执行对有歧义的超时不自动重试，避免重复执行风险。
- 大结果集通过数据集/文件引用处理，不直接全部交给模型。

这部分是成熟资产，不应重写。应由 Tool Runtime 兼容包装，并为查询行数、空结果、快照、水位和异常值生成统一 metadata。

### 3.7 Python 分析模块

`app/analysis/` 已提供：趋势、比较、构成、异常、预测、量价、漏斗、结构变化、多维归因、画像、图表、洞察发现、结构化解释和答案规划。

`AnalysisOperatorRegistry` 采用受控算子注册，不存在任意 Python 代码执行入口，符合安全 Runtime 方向。

需升级：将算子调用、中间产物、依赖、部分失败和验证从编排器抽到 Analysis Runtime；继续使用白名单算子，不引入通用 `eval/exec` 或任意代码沙箱。

### 3.8 多轮对话

现有能力包括问题改写、指代恢复、待补充状态、澄清轮次、task frame、最近请求、数据集追问、复合任务恢复和幂等响应。

问题主要来自上下文来源分散：请求 history、Session pending、task frame、长期记忆和数据集引用分别在不同位置读取，缺少统一优先级和预算。追问时可能选中错误任务帧，或将上一轮分析结果和当前新问题错误融合。

### 3.9 Redis / Checkpointer / Session

`app/stores/session.py` 的 SessionStore 已支持：待追问状态、缓存响应、请求指纹、任务帧历史、数据集引用、DAG checkpoint/pending、执行 claim/release、报告引用和 CAS/version 语义。实现包括内存与 Redis。

当前没有 LangGraph checkpointer。现有自研 SessionStore 已覆盖业务恢复，第一阶段没有必要再引入另一套状态存储；应先通过 Event Log 和统一 checkpoint 引用补足可复盘性。

### 3.10 长短期记忆

短期记忆由 Redis Session、history compaction、task frame、pending state 和 dataset reference 共同实现。

长期记忆模块已支持：作用域隔离、候选/生效/删除状态、用户确认、允许键治理、敏感与临时信息拦截、逻辑去重、软删除、有效期和 MySQL/内存实现。

不应替换长期记忆存储。后续补强点是：统一 Memory Extraction 入口、相似/逻辑冲突更新规则、召回排序、注入预算，以及 `MEMORY_RECALL/MEMORY_WRITE` 事件。

### 3.11 SSE 流式响应

`app/api.py` 以具名 JSON `type` 输出 `updata_state`、`message_chunk`、`tool_result`、`answer`、`complete`，并保留与 New_Agent 一致的标签结构；支持思考阶段排序、回答分片和异常终止。

这属于稳定对外协议，Harness 改造不得改变标签或字段语义。新增 tracing/event 信息应放在兼容的 `meta` 中或仅服务端持久化。

### 3.12 FastAPI 接口

同步与流式接口、OpenAPI 示例、存活与就绪检查已经存在。依赖在 `dependencies.py` 集中装配，`main.py` 管理生命周期和依赖健康。

不需要新增第二套 API。Runtime 通过现有依赖装配注入，并保持当前请求体、响应体和 SSE 合约兼容。

### 3.13 日志系统

当前主要使用 Python 标准 logging，Adapter 会记录上游 stage、code、status、retryable 和 details。`node_trace` 可显示节点完成情况。

缺陷：日志缺少统一 JSON 字段，无法稳定按 session/trace/execution/tool 检索；部分日志只有异常类型；没有完整的计划—工具—验证—答案关联。

### 3.14 Langfuse / tracing

本次代码扫描未发现应用内 Langfuse SDK 或直接埋点。当前只有 `node_trace`、`audit_events` 预留、标准日志和 SSE 过程事件。

因此不能声称当前已接入 Langfuse。Phase 9 应先定义内部轻量 Trace/Span 协议；若部署环境提供 Langfuse，再增加可选 exporter，不能把业务正确性绑定到观测平台可用性。

### 3.15 配置管理

`app/config.py` 使用 Pydantic Settings，已覆盖服务地址、模型、超时、重试、Redis、MySQL、MinIO、文件限制、动态 Skill、工具选择和并发相关设置。

配置基础完备。后续只增加必要且有安全默认值的 Harness 配置；不修改现有 `.env` 和已有配置含义。

### 3.16 异常处理

HTTP Adapter 已有结构化 dependency error，包含 error kind、source、status、retryable；编排器区分显式数据集不可用、依赖约束、上游错误和安全降级；API 层能输出流式错误。

缺陷：Extension Tool、MCP、分析算子和主查询 Adapter 的错误类型并不统一，部分路径返回业务模型，部分抛异常，部分只写字符串。需要统一为 ToolError/RuntimeErrorResult，但保持原异常兼容映射。

### 3.17 Validator

当前已有多层验证：Pydantic 请求模型、ASL schema、强制筛选、依赖约束、只读 SQL、分析 Data Contract、最小数据量、结果可靠性、证据 ID、数值落地、因果表述限制、假设与警告规则。

已有能力很强，但结果分散在 Adapter、Analysis Engine、Synthesizer 和 Orchestrator。Phase 4 应组合而非重写，产出统一 `ValidationReport`，明确 passed/warnings/errors/confirmed findings/hypotheses。

### 3.18 Retry / Timeout

`HttpClient` 已支持有限次数重试、指数退避和请求超时；ASL/语义等幂等调用可重试，SQL 执行谨慎禁止模糊重试；Tool/MCP 有独立超时；任务 claim 有 TTL 和等待截止时间。

缺陷：策略分散，无法从一次执行结果看见 attempt、总耗时和最终失败阶段。Tool Runtime 应统一策略和元数据，但必须保留“非幂等/结果不确定时不重试”的现有原则。

## 4. 当前已经具备的业务能力

- 指标、明细、排名、元数据定义和血缘查询。
- 趋势、同比/环比、比较、构成、异常、预测和数据质量分析。
- 量价、漏斗、结构变化、多维贡献和受证据约束的归因分析。
- 报告、Excel/Word/PDF、ChartSpec 和大结果集下载。
- CSV/XLSX、多 Sheet、数据集追问和受控跨数据集 Join。
- 多问题拆分、2–5 个原子任务 DAG、分层并行、部分结果和 Redis 断点恢复。
- 意图识别、问题补全、上下文指代、澄清和多轮任务帧。
- HTTP Tool、MCP、动态 Skill、可选智能调用和联网能力入口。
- SQL 安全、数据契约、证据可靠性、空结果和依赖错误降级。
- 结构化分析结果、业务洞察解释与答案信息筛选。

## 5. 与 Harness 思路的对应关系

| Harness 思路 | 当前已有基础 | 判断 |
|---|---|---|
| LangGraph 状态与路由 | StateGraph、AgentState、条件路由 | 保留，节点逐步实化 |
| Skill | 内置能力注册、动态 SKILL.md | 直接复用 |
| Tool Runtime | HttpClient、ExtensionDispatcher、ToolSelector | 能力分散，需要统一包装 |
| Analysis Plan | Pydantic AnalysisPlan、TaskPlan | 直接扩展为可执行 Step |
| Analysis Runtime | DAG 分层并行、确定性算子 | 抽出受控运行边界 |
| Validator | SQL/契约/证据/可靠性校验 | 组合成统一报告 |
| Session Event Log | node_trace、audit_events 字段 | 缺少持久 append-only 实现 |
| Context Builder | 多处手工拼接上下文 | 需要新增统一入口 |
| Context Compaction | 锚点式 history compaction | 需要结构化摘要和预算 |
| Memory | Redis Session + MySQL 长期记忆 | 强基础，增量优化 |
| Tracing | logging、SSE stage、node_trace | 缺少端到端 trace/span |
| Evaluation | 59 个测试文件、evals | 扩充 Harness 场景指标 |

## 6. 真正值得升级的部分

按收益和依赖关系排序：

1. **统一 Tool Runtime**：解决所有执行路径的超时、有限重试、错误码、耗时和 trace 不一致。
2. **可执行 Analysis Plan + Analysis Runtime**：把现有 DAG 和算子从大编排器中形成受控执行面，支持并行和部分成功。
3. **统一 Result Validator**：组合现有验证器，阻止无证据归因和口径不一致。
4. **Session Event Log**：支持过程复盘、任务恢复和故障定位。
5. **Context Builder**：统一历史、计划、记忆、工具摘要和 token 预算。
6. **结构化 Compaction**：长会话保留目标、口径、修正、结论和未完成任务。
7. **Memory 连接与冲突审计**：复用现有存储，改善召回和更新。
8. **Tracing/Evaluation**：形成端到端 execution/span 和可量化回归。

## 7. 没有必要修改的部分

- 不引入第二套 Agent Runtime，也不迁移到其他 Agent 框架。
- 不推翻 LangGraph、FastAPI、Redis Session、MySQL 长期记忆、现有 SSE 协议。
- 不重写成熟的 ASL/SQL 安全链路、分析算子、文件存储和报告导出。
- 不把所有 Skill 拆成 Agent，不在第一阶段建设多 Agent。
- 不允许模型执行任意 Python 代码；继续使用白名单确定性算子。
- 不为了架构外观创建大量 Manager、Factory、Handler 或一层转发类。
- 暂不引入 LangGraph checkpointer；先让现有 SessionStore 与 Event Log 形成可靠恢复闭环。
- 不把原始大查询结果、完整工具输出或全部历史直接注入 LLM。

## 8. 目标增量架构

```text
User / Existing FastAPI + SSE
              │
              ▼
      LangGraph Main Data Agent
      ├─ Query Understanding
      ├─ Context Builder
      ├─ Planner
      └─ Conditional Routing
           │
     ┌─────┴──────────────┐
     │ simple             │ complex
     ▼                    ▼
   Skill             Executable AnalysisPlan
     │                    │
     └──────┬─────────────┘
            ▼
       Analysis Runtime
       ├─ dependency / parallel group
       ├─ partial success / checkpoint
       └─ controlled analysis operators
            │
            ▼
         Tool Runtime
       validate → guard → hook → execute
       → timeout/retry → validate → trace
            │
     SQL / Metric / Metadata / HTTP / MCP / File / Chart
            │
            ▼
       Result Validator
            │
       Structured Result
            │
       Insight + Answer Planner
            │
          Answer

旁路：Session Event Log ─ Context Compaction ─ Short/Long Memory
      Trace/Span ─ Evaluation ─ Existing Redis/MySQL/MinIO
```

## 9. 分阶段实施设计

### Phase 2：Tool Runtime

建议新增 `app/runtime/tool_runtime.py` 和相应领域模型。统一执行上下文、参数验证、安全守卫、超时、有限重试、结果校验、hook、事件和 trace。先兼容包装 ExtensionDispatcher 与内部语义查询工具，不改变对外 Tool API。

### Phase 3：Analysis Plan + Analysis Runtime

扩展现有 `AnalysisPlan`，增加可执行 `AnalysisStep`：`id/action/tool/inputs/depends_on/parallel_group/expected_output`。新增 `app/runtime/analysis_runtime.py`，复用 TaskPlan 的依赖校验与并发思路、AnalysisOperatorRegistry 和 Session checkpoint。

### Phase 4：Result Validator

新增统一 `ValidationReport`，组合 SQL、数据契约、时间/口径、总分对账、同比环比、贡献度、异常值和最终证据验证。不要删除现有局部校验。

### Phase 5：Session Event Log

在 `app/stores/` 增加轻量 EventStore 协议与 Redis/内存实现。事件 payload 有大小和敏感字段限制，按 session append，支持按 trace 回放。第一版不是完整 Event Sourcing。

### Phase 6：Context Builder

新增单一 Context Builder，输入当前问题、task frame、AnalysisPlan、最近重要消息、结构化摘要、长期记忆和必要工具摘要，并执行预算控制。所有 LLM 调用逐步迁移，禁止大结果原文进入上下文。

### Phase 7：Context Compaction

保留现有锚点策略，增加结构化 `ConversationSummary`：目标、已确认指标、时间、筛选、重要结论、修正和未完成任务；达到消息/token 阈值后更新摘要并记录 COMPACTION 事件。

### Phase 8：Memory 优化

复用现有 LongTermMemoryStore，补统一 extraction/dedup/update/retrieval 服务、冲突策略和事件。Session 与 Memory 继续分离。

### Phase 9：Tracing / Evaluation

内部先统一 trace/span 数据；Langfuse 作为可选 exporter。扩展 eval 场景，衡量完成率、澄清率、工具成功率、部分成功率、恢复成功率、证据覆盖率和 P50/P95 延迟。

## 10. 实施风险与控制

| 风险 | 控制方式 |
|---|---|
| 包装 Runtime 后改变现有响应 | 保持 AgentResponse 和 SSE 标签不变，添加契约回归测试 |
| 自动重试 SQL 引发歧义 | 沿用当前幂等判断，SQL 执行默认不自动重试 |
| Event payload 泄露敏感数据 | 字段白名单、脱敏、大小限制，不保存原始大结果 |
| Context 摘要丢失用户修正 | 修正/口径/未完成任务作为强制保留字段 |
| Analysis 并行压垮上游 | 有界并发、单步 timeout、失败隔离、部分成功 |
| 大编排器一次拆分引发回归 | 兼容包装、逐路径迁移，每 Phase 独立测试 |
| 新 tracing 影响业务 | exporter 可选且失败不阻断主流程 |

## 11. Phase 1 验收结论

现有架构可以进行 Harness 化增量升级，且不需要更换技术栈。最合理的切入点是统一 Tool Runtime；它能为后续 Analysis Runtime、Event Log、Validator 和 Tracing 提供共同执行标识与结果契约。

Phase 1 只形成设计结论，未修改 `.env`、`app/config.py` 或任何业务执行代码。进入 Phase 2 前，应以当前接口与测试作为兼容基线。

## 12. Phase 2 实施记录：统一 Tool Runtime

Phase 2 已完成第一版兼容式落地：

- 新增 `app/runtime/tool_runtime.py`，提供 `ToolContext`、`ToolRequest`、`ToolResult`、`ToolError` 和 `ToolRuntime`。
- 执行链统一为：参数 Schema 校验 → Guard → Pre Hook → Execute → Timeout → 有限重试 → Result Validator → Post Hook → ToolResult。
- 每次执行均记录 `execution_id`、`trace_id`、`session_id`、`tool_name`、`attempts`、`latency_ms`、状态和结构化错误。
- 重试最多三次；实际 HTTP/MCP 兼容接入采用最多两次。只有超时、网络异常及 408/429/502/503/504 才允许重试。
- `idempotent=False` 时即使错误标记可重试也只执行一次，防止不确定结果的重复副作用。
- Tool 输入根据已有 `inputSchema` 校验 required 和基础 JSON 类型，校验失败不会调用下游。
- Post Hook/观测失败不改变已经产生的工具结果。
- `ExtensionDispatcher` 的 HTTP 和 MCP 调用已接入 Tool Runtime；现有 HTTP/MCP 请求格式、工具选择逻辑和 `ExtensionExecution` 对外结构保持兼容，仅增加可选运行元数据字段。
- 没有修改 `.env`、`app/config.py`、SSE 标签或核心 SQL 执行策略。

测试结果：

- Tool Runtime、ExtensionDispatcher、联网路由和 API 针对性回归：`54 passed`。
- 全量回归：`1045 passed, 27 failed`。27 项失败与本阶段修改前的已知基线一致，集中在旧澄清时间策略、OpenAPI 身份头历史断言、旧答案文本格式和部分历史编码测试；本阶段相关测试全部通过，未新增失败项。

## 13. Phase 3 实施记录：Analysis Plan + Analysis Runtime

Phase 3 已完成第一版受控运行能力：

- 在现有 `AnalysisPlan` 上增量增加 `task_type`、`steps`、`need_clarification` 和 `clarification_question`，未删除任何原字段。
- 新增结构化 `AnalysisStep`，包含 `id/action/tool/inputs/depends_on/parallel_group/expected_output/timeout/max_attempts/required`。
- `AnalysisPlanner` 对当前确定性分析生成实际可执行的 `deterministic-analysis` 步骤。
- 新增 `app/runtime/analysis_runtime.py`，支持顺序依赖、DAG 分层、有界并行、超时、有限重试、依赖跳过、失败隔离和部分成功。
- Analysis Runtime 只执行调用方显式注册的 executor；计划中的工具名称不会触发动态 import，禁止任意 Python 执行。
- 现有确定性分析真实调用路径已迁移到 Analysis Runtime，继续复用 `AnalysisEngine`、`AnalysisOperatorRegistry`、洞察解释和 Answer Planner。
- `AnalysisError.requirements` 通过结构化错误详情跨 Runtime 边界保留，原有“数据不足时返回需要补充条件”的业务行为不丢失。
- 多问题查询仍复用现有 TaskPlan/Redis checkpoint；本阶段没有重复开发第二个业务 DAG 引擎。Analysis Runtime 负责分析步骤，TaskPlan 继续负责用户问题级任务。

测试覆盖：并行步骤、依赖结果传递、可选维度失败后的部分成功、必选步骤失败、依赖跳过、循环依赖、未知依赖和未注册执行器拒绝。

测试结果：

- 分析规划、Runtime、分析引擎、组合算子和洞察相关回归：`92 passed`。
- 全量回归：`1050 passed, 27 failed`。通过数比 Phase 2 增加 5 项；失败集合仍为既有 27 项，本阶段未新增失败。

## 14. Phase 4 实施记录：统一 Result Validator

Phase 4 已完成统一验证报告和分析主链路接入：

- 新增 `ValidationCheck` 与 `ResultValidationReport`，统一输出 `PASS/WARN/FAIL/NOT_APPLICABLE`、证据、详情、已确认发现、候选假设、警告和错误。
- 新增 `app/analysis/result_validator.py`，组合已有数据契约和证据治理思路，不替换原 SQL/ASL/Synthesis 校验。
- 校验范围包括：扩展工具成功状态、必需/可选工具区别、空数据、快照与数据质量、业务数据水位、指标可追踪性、NaN/Infinity、变化率复算、对账残差、查询证据、确定性决策锁和因果越界。
- 必需工具失败为 `FAIL`；可选补充工具失败为 `WARN`，不会让联网画像等补充能力拖垮核心数据结论。
- 对 `base/current/change_rate` 进行独立复算；对 `reconciliation_residual`、`share_change_residual`、additivity status 等执行总分对账。
- 当 `causality_established=false` 时，禁止最终表述使用“导致、造成、根本原因”等确定性因果措辞。
- Validator 已放在确定性分析完成、Insight Interpretation 开始之前。强制失败会安全终止，验证报告作为 `VALIDATION_RESULT` 证据保留。
- `WARN` 只进入结构化验证报告，不机械拼接到用户答案；已有 Reliability Gate 仍负责面向用户的可靠性披露。

测试结果：

- Validator、分析编排、合成和洞察针对性回归：`33 passed`。
- 全量回归：`1056 passed, 27 failed`。比 Phase 3 增加 6 项；失败集合仍为既有 27 项，本阶段未新增失败。

## 15. Phase 5 实施记录：Session Event Log

Phase 5 已完成轻量 append-only Session Event Log：

- 新增 `app/stores/events.py`，定义 `SessionEventType`、`SessionEvent`、`SessionEventStore`、内存实现和 Redis 实现。
- 每条事件包含 `event_id/session_id/user_id/tenant_id/application_id/message_id/event_type/timestamp/trace_id/payload`。
- 已接入 `USER_QUERY`、`QUERY_REWRITE`、`INTENT_RESULT`、`CLARIFICATION`、`ANALYSIS_PLAN`、`TOOL_CALL`、`TOOL_RESULT`、`PYTHON_ANALYSIS`、`VALIDATION_RESULT`、`FINAL_INSIGHT` 和 `ERROR`。Memory 与 Compaction 事件将在对应 Phase 接入。
- 同一请求使用 `message_id` 作为当前 trace 关联键，可按租户、用户、应用、会话和 trace 回放。
- Redis 使用有界 List、事务 pipeline、`LTRIM` 和会话 TTL；默认每会话最多保留 500 条事件，不建设复杂 Event Sourcing。
- payload 在写入前统一清洗：认证头、token、password、secret、API key、cookie 等敏感字段自动脱敏；原始 rows 只记录数量；单事件超过 32 KiB 时只保留结构摘要。
- 事件存储与 SessionStore 分离，不替代待追问状态、任务帧、幂等缓存或 DAG checkpoint。
- Event Store 写入异常采用 fail-open：记录服务告警但绝不改变业务响应。
- `dependencies.py` 按现有部署模式自动选择 Redis 或内存 Event Store，未增加新的外部依赖和配置项。

测试结果：

- Event Log、Redis、API 和 Workflow 针对性回归：`39 passed`，另有 1 项既有澄清断言失败。
- 全量回归：`1061 passed, 27 failed`。比 Phase 4 增加 5 项；失败集合仍为既有 27 项，本阶段未新增失败。

## 16. Phase 6 实施记录：统一 Context Builder

Phase 6 已完成统一、预算化、模型安全的上下文构建能力：

- 新增 `app/services/context_builder.py`，定义 `ContextEnvelope` 与 `ContextBuilder`，统一组织系统指令、当前问题、任务状态、分析计划、近期历史、会话摘要、长期记忆、工具结果摘要和业务上下文。
- 采用确定性优先级和字符预算；无论预算是否紧张，当前问题、任务状态和分析计划均不会被静默丢弃。
- 历史消息继续复用现有 `compact_history`，并显式为早期任务、用户纠错和助手澄清锚点预留窗口容量，避免最近消息占满后丢失“不是订单量，改成销售额”等关键口径。
- 工具结果仅向模型提供工具名、状态、执行标识、耗时、结果键、行数、总数和短摘要；`rows/data/raw_rows/raw_result` 等原始大结果不会进入模型上下文。
- 对嵌套业务状态实施深度、条数和字符串长度限制；超过预算的历史、记忆和工具摘要会在 `omitted` 中明确计数，而不是无提示截断。
- 编排器的 DAG 子任务历史选择已统一经过 Context Builder；最终分析合成也已接收结构化 `agent_context`。
- 合成器保持向后兼容：只有支持 `context` 参数的实现才会收到新上下文，既有测试桩和外部响应/SSE 契约均未改变。
- 未修改 `.env`、`app/config.py`、SSE 标签、SQL 执行策略或用户可见响应结构。

测试结果：

- Context Builder、历史压缩、分析合成和编排专项回归：`32 passed`。
- 全量回归：`1065 passed, 27 failed`。比 Phase 5 增加 4 项；失败集合仍为已知基线 27 项，本阶段未新增失败。

## 17. Phase 7 实施记录：结构化 Context Compaction

Phase 7 已完成面向长对话的确定性结构化压缩：

- 新增 `app/services/context_compaction.py`，定义 `ConversationSummary` 与 `ContextCompactor`。
- 摘要字段包括：当前目标、意图、已确认指标、维度、筛选条件、时间范围、用户纠错、关键结论、未解决事项、源消息数和已压缩消息数。
- 默认历史超过 24 条时触发压缩，保留最近 12 条原始消息；旧消息不再整体进入模型上下文。
- 当前 `CanonicalAnalysisRequest` 是摘要事实主干。指标、维度、筛选和时间均来自已完成的结构化识别，不依赖模型重新猜测。
- 旧历史只通过明确规则提取用户纠错、助手澄清和带有结论特征的已回答内容；普通寒暄和无结论文本不会写入关键结论。
- 当当前结构化请求已经补齐必需参数时，自动清除旧的“请补充时间范围”等过期澄清，避免状态污染后续回答。
- 纠错和结论去重并设置数量、长度上限，防止同一信息在长对话中无限膨胀。
- 编排器在分析合成前执行压缩，将结构化摘要交给统一 Context Builder，并记录 `COMPACTION` 会话事件，支持诊断和回放。
- 压缩过程不增加 LLM 调用，因此不会引入额外 429、延迟或摘要幻觉风险。
- 未修改 `.env`、`app/config.py`、外部请求体、响应结构和 SSE 标签。

测试结果：

- Context Compaction、Context Builder、历史压缩、分析合成、编排和事件日志专项回归：`42 passed`。
- 全量回归：`1070 passed, 27 failed`。比 Phase 6 增加 5 项；失败集合仍为既有 27 项，本阶段未新增失败。

## 18. Phase 8 实施记录：Memory 治理优化

Phase 8 已在现有 LongTermMemoryStore 上补齐运行时治理，没有创建第二套记忆数据库：

- 新增 `app/services/memory_manager.py`，负责从明确用户指令中提取受治理的长期默认偏好。
- 只有请求开启 `use_longterm_memory=true`，且用户明确使用“记住”“以后默认”“今后默认”或“设为默认”时才允许写入。
- 当前支持受控提取：默认指标、默认维度、默认比较口径、默认时间范围和默认输出格式；写入值必须继续通过现有 `MemoryCandidate` 白名单与敏感字段校验。
- 普通查询、查询结果、SQL、原始聊天历史、提示词和推理过程不会写入长期记忆。
- 明确记忆指令自动经历 candidate 创建与用户本人确认；相同消息幂等，同作用域同逻辑键的新值会将旧值标记为 `SUPERSEDED`。
- 提取范围限制在明确记忆指令所在分句。例如“以后默认销售额，查询本月销售额”只记住销售额，不会把本次查询的“本月”误存为默认时间。
- 记忆读取仍严格限制在 `tenant_id/user_id/application_id`，并继续遵守“当前明确表达优先，默认值只补空槽”的原则。
- 写入和读取分别记录 `MEMORY_WRITE`、`MEMORY_RECALL` 事件；事件只保存 memory id、逻辑键、应用数量和作用域说明，不保存敏感原始内容。
- 记忆存储异常采用 fail-open：记录 `LONG_TERM_MEMORY_WRITE_UNAVAILABLE` 或现有读取不可用假设，但不阻断正常数据查询。
- 未修改 `.env`、`app/config.py`、外部 API、SSE 标签和现有 MySQL 记忆表结构。

测试结果：

- Memory Manager、长期记忆存储、作用域边界、事件日志和 Context Builder 专项回归：`28 passed`。
- 全量回归：`1075 passed, 27 failed`。比 Phase 7 增加 5 项；失败集合仍为既有 27 项，本阶段未新增失败。

## 19. Phase 9 实施记录：Tracing / Evaluation

Phase 9 已完成内部轻量追踪和确定性评测聚合，没有把业务运行绑定到外部观测平台：

- 新增 `app/observability/tracing.py`，定义 `TraceSpan`、`TraceSummary`、`EvaluationReport` 和 `HarnessEvaluator`。
- 每个请求以 `message_id` 作为 `trace_id`，记录请求状态、意图、端到端耗时、澄清轮次、证据覆盖和恢复结果。
- 核心“智能语义查询器”现在记录真实 `TOOL_CALL/TOOL_RESULT`，包含执行状态、尝试次数、精确耗时和非敏感结果元数据；不再只统计请求携带的 HTTP/MCP 扩展工具。
- 确定性分析 Runtime 的每个步骤转为 `ANALYSIS` Span，复用现有 `AnalysisStepResult.latency_ms`，不根据界面文字或事件间隔猜测耗时。
- Result Validator 记录精确执行耗时及 warning/error 数量，并形成独立验证 Span。
- Trace 汇总在 `FINAL_INSIGHT` 之后写入 `TRACE_SUMMARY` 事件，可按租户、用户、应用、会话和 trace 回放。
- 评测器聚合完成率、部分成功率、澄清率、工具成功率、澄清恢复成功率、证据覆盖率、意图分布及端到端 P50/P95 延迟。
- 无工具调用时 `tool_success_rate=None`，无恢复样本时 `recovery_success_rate=None`，避免把“不适用”伪装成 0% 或 100%。
- Event Store 读取或 Trace 写入异常继续采用 fail-open，只记录告警，不改变业务答案、HTTP 状态和 SSE 标签。
- 当前没有引入或宣称接入 Langfuse。后续如部署环境需要，可基于内部 Trace 协议增加可选 exporter。
- 合并语义查询工具后遗漏的 `health/rewrite_health` 契约已同步补齐；`/live`、`/ready` 和 POST SSE 均已恢复正常。
- 未修改 `.env`、`app/config.py`、外部请求体、`AgentResponse` 或现有 SSE 标签。

测试结果：

- Tracing、Evaluation、Session Event、Tool Runtime、Analysis Runtime、Validator 和组合语义查询专项回归：`23 passed`。
- 全量回归：`1080 passed, 27 failed`。比 Phase 8 增加 5 项；失败集合仍为既有 27 项，本阶段未新增失败。

## 20. Phase 10 实施记录：计划级执行预算

在 Phase 9 的请求追踪基础上，补齐复杂分析任务的总执行时间边界：

- `AnalysisRuntime` 默认给每个分析计划设置 120 秒硬截止时间，避免多层 DAG 将单步超时累加成无界占用。
- 截止时间覆盖整个计划，而不是为每一步重新计时；并行层只使用剩余预算。
- 预算耗尽时立即取消当前执行层，尚未开始的后续步骤不再调度。
- 当前被取消的步骤标记为 `FAILED`，未启动步骤标记为 `SKIPPED`，统一错误码为 `ANALYSIS_BUDGET_EXHAUSTED`。
- 已完成步骤及其结果继续保留；只有可选补充步骤超时时，结果可安全返回 `PARTIAL_SUCCESS`，必需步骤超时则返回 `FAILED`。
- `AnalysisRuntimeResult` 增加内部审计字段 `budget_exhausted` 和 `budget_seconds`，便于 Trace、告警和离线评测区分业务失败与资源预算终止。
- 保持外部请求体、SSE 事件标签、答案结构、工具选择和现有配置文件不变。

测试结果：

- Tool Runtime、Analysis Runtime、Tracing、Validator 和 Runtime 回归：`33 passed`。
- 编排器、DAG、事件日志和 API 组合回归：`89 passed, 8 failed`；8 项均属于当前仓库既有测试基线中的澄清、历史追问和旧答案文本断言，未涉及本阶段修改的 Runtime 预算路径。
